# CLAUDE.md

The agent contract for this subtree is **`AGENTS.md`** (the **Code Reviewer Agent** contract) — follow it plus
the root `AGENTS.md`. It is **role-scoped**: it applies when reviewing changes anywhere in the repository, not
only when editing files under `.github/`.

Report findings with the `ReportFindings` tool when the active review instructions ask for it; otherwise follow
whatever output format those instructions specify.

@AGENTS.md
