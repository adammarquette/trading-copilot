# Integration Test Suite Audit & Ticket Backlog Report

**Author**: Primary QA / SDET  
**Date**: July 23, 2026 · **Realigned to the route map**: 2026-07-24 (gh#160)  
**Target Repository**: `trading-copilot` (`src/MarqSpec.TradingCopilot.IntegrationTests`)  
**Status**: Living inventory — suites land incrementally; **per-issue status lives in the tracker**, not here
(gh#616) · reconciled 2026-07-30 (gh#490: added 5 shipped suites the inventory had missed — send-as-is, news ingestion/relevance/salience, the pre-merge Prometheus-name guard)  

---

## Contents

| § | Section | What it is |
| :--- | :--- | :--- |
| 1 | [Executive Summary & Audit Context](#1-executive-summary--audit-context) | ~2.6K chars — the scope of the audit and its findings as first written |
| **2** | [**Current Integration Test Inventory**](#2-current-integration-test-inventory) | **~58K chars — 64% of this file.** The heavyweight: one row per suite. **Open a subsection below rather than the whole section** |
| 3 | [High-Value Test Opportunities & Risk Analysis](#3-high-value-test-opportunities--risk-analysis) | ~1.7K chars — the original audit's three high-risk execution paths |
| 4 | [What the suites found — live status is the tracker](#4-what-the-suites-found--live-status-is-the-tracker) | the defects these suites caught, and the known coverage gaps. **Live per-issue status is the tracker, not this file** |
| 5 | [Route-map realignment log (gh#160)](#5-route-map-realignment-log-gh160) | ~1.5K chars — the gh#160 realignment record |

**Inside §2 —** the inventory is grouped by area under test, so a reader after one subsystem opens only its block:
[Onboarding, authorization and account scoping](#onboarding-authorization-and-account-scoping) ·
[The send path, the staged ladder and the production smoke](#the-send-path-the-staged-ladder-and-the-production-smoke) ·
[Storage tier — the event log and the database's own guards](#storage-tier--the-event-log-and-the-databases-own-guards) ·
[Tenancy, risk-profile and connection lifecycle](#tenancy-risk-profile-and-connection-lifecycle) ·
[Staging tier — the pre-live venue gates](#staging-tier--the-pre-live-venue-gates) ·
[Market data — bars, indicators and gap recovery](#market-data--bars-indicators-and-gap-recovery) ·
[Cross-asset context prices](#cross-asset-context-prices) ·
[The background engines — conditional firing and auto-flatten's three tiers](#the-background-engines--conditional-firing-and-auto-flattens-three-tiers) ·
[The outbound seams — the durable pager and the agent-review route](#the-outbound-seams--the-durable-pager-and-the-agent-review-route) ·
[Overrides, connection loss and restart recovery](#overrides-connection-loss-and-restart-recovery) ·
[The working-order family and the gate's last mile](#the-working-order-family-and-the-gates-last-mile) ·
[Reconcile and the fill backfill — recovering a stranded send](#reconcile-and-the-fill-backfill--recovering-a-stranded-send) ·
[The trade journal — FIFO pairing, write ordering and the flat-before-fill window](#the-trade-journal--fifo-pairing-write-ordering-and-the-flat-before-fill-window) ·
[Observability, the venue-truth read and the send-as-is path](#observability-the-venue-truth-read-and-the-send-as-is-path) ·
[The §9 audit trail — the two safety write sites and its migration](#the-9-audit-trail--the-two-safety-write-sites-and-its-migration) ·
[News → relevance → salience](#news--relevance--salience) ·
[The news-embedding tree — semantic retrieval and its GC](#the-news-embedding-tree--semantic-retrieval-and-its-gc) ·
[The suggestion spine — the take, the throttle and the headroom read](#the-suggestion-spine--the-take-the-throttle-and-the-headroom-read) ·
[Metric names, alert rules, alerting and the trigger-confirmation gate](#metric-names-alert-rules-alerting-and-the-trigger-confirmation-gate) ·
[The co-pilot chat — the grounded turn and its read-only tool layer](#the-co-pilot-chat--the-grounded-turn-and-its-read-only-tool-layer)

---

## 1. Executive Summary & Audit Context

As the primary QA/SDET for **Trading Copilot**, a comprehensive audit was conducted across the API endpoints, service layer, domain guards, multi-tenancy model (ADR-0017 / R-20), and database persistence layer (`TradingCopilotDbContext` & DB constraint triggers).

The objective of this audit is to identify high-value integration testing gaps, quantify risk coverage, and define actionable tickets to raise the quality ceiling of the platform.

### Key Audit Findings (as first written; §2 below is the live inventory — per-issue status is the tracker)
1. **Existing Integration Coverage**: Firm Onboarding, Risk Profile Declaration, User Invitation / Primary-Operator Auth Guard, the unauthenticated challenge, order execution (direct + staged ladder), the staged-stop plan, the event backbone, the synthetic conditional-entry firing engine, and the production smoke suite — see §2 for the full inventory.
2. **Gaps — delivered (✅) and remaining (⬜):**
   - ✅ **Staged Send Path & Order Execution (`arm` → `edit` → `take` → `cancel`)** — direct send (gh#130), the staged ladder (gh#157), and the stop-plan DB guards (gh#158) all landed against Testcontainers Postgres.
   - ✅ **R-14 Database Mode & Environment Enforcement** — the `Order.mode == Account.mode` trigger and mode × environment refusals are exercised in the order suites (gh#130, gh#157).
   - ✅ **Multi-Tenant / Multi-Operator Data Isolation (R-20 / ADR-0017)** — **gh#132**: collection-scoping, cross-tenant `404`s on accounts/orders, and gate-decision isolation proven at the `DbContext` (§2).
   - ✅ **Connection Lifecycle & Account Stage** — creation, discovery, and stage resolution/override covered (**gh#142**). Credential *rotation* and connection *soft-delete* remain **missing features**, not missing tests → **gh#210** for the Coding Agent.
   - ✅ **Production Read-Only Smoke Test Suite** — gh#131; the lazy-fixture fix so a deployed-target run starts no container is gh#152.
   - ✅ **Event Backbone Storage** — the append-only Timescale log at the storage tier (gh#161).
   - ✅ **Synthetic Conditional-Entry Firing Engine (ADR-0007 / R-11)** — arm (persist `Pending`, transmit nothing) → a quote crossing the trigger fires it to a **working native order** through the **authoritative fire-time re-gate** (R-5 / R-16 / R-12: a conditional valid at arm but violating the gate at fire is refused, not sent); cancel-if drift, expiry-outranks-fire, fired-then-replayed idempotency, a live-host `market.quote` backbone consume, and the `CK_ConditionalOrders_*` DB guards — **gh#180**, pairing the #198 watcher.

---

## 2. Current Integration Test Inventory

Below is the inventory of current integration tests under [`src/MarqSpec.TradingCopilot.IntegrationTests`](../src/MarqSpec.TradingCopilot.IntegrationTests) — `Api/` for HTTP-driven suites, `Data/` for storage-tier ones, and the per-area folders that have since grown out of them (`Ai/`, `MarketData/`, `Observability/`, `Relevance/`, `Signals/`, `Suggestions/`, `Alerting/`). Rows are grouped by **area under test**, not by folder, so a suite's migration half sits beside the behaviour it migrates.

There are **116** suite files, and every one has a row here. That count is not a note — [`scripts/check-integration-test-inventory.sh`](../scripts/check-integration-test-inventory.sh) fails CI when it, or the rows, stop matching the files on disk (gh#862). The reason it is enforced rather than asked for: this section drifted to 22 missing rows by the 2026-08-14 audit and 27 by two days later, having already been reconciled by hand under gh#490 — a map that silently omits a quarter of the territory answers *"is this path already covered?"* **wrong**, in the direction of writing a suite that exists or leaving a real gap invisible.

### Onboarding, authorization and account scoping

| Integration Test Suite | Covered Endpoints & Features | Current Status |
| :--- | :--- | :--- |
| **`FirmOnboardingEndpointsIntegrationTests.cs`** | `POST /firms`, `GET /firms`, `PUT /firms/{id}/conventions`, connection creation & account discovery | **Active** (Passing) |
| **`RiskEndpointsIntegrationTests.cs`** | `PUT /accounts/{id}/risk`, `GET /accounts/{id}/risk` | **Active** (Passing) |
| **`UserInvitationEndpointsIntegrationTests.cs`** | `POST /auth/invitations`, `POST /auth/invitations/accept`, Primary Operator flag enforcement | **Active** (Passing) |
| **`UnauthenticatedEndpointsTests.cs`** | Global HTTP 401 unauthenticated challenge verification, over an **eleven-route hand-maintained list**. Superseded in scope by `AuthorizationSurfaceIntegrationTests` (gh#511), which enumerates the real route table; kept as the narrow, explicit-list regression | **Active** (Passing) |
| **`FlattenTierAccountSetIntegrationTests.cs`** | **Which accounts each flatten tier acts on** (gh#512 ⇒ gh#187, R-13, ADR-0013, ADR-0015) — the credential-set boundary on **both** tiers, and their agreement. Guards: an account whose connection sits on a **sibling credential set** produces **no venue traffic at all** on the primary tier and on the watchdog (asserted via `PositionReadCount == 0`, since the credential check sits *above* `GetAccountsAsync` — "no close happened" cannot witness this, being equally true of a pass that read the venue and declined); and **both tiers act on the same account set** for one database + credential key, asserted behaviourally by comparing the distinct account keys in `ClosePositionCalls`, with the primary's non-empty set as the positive control. Exists because **both tiers' unit guards were vacuous** — their shared `Venue(...)` helper leaves `GetAccountsAsync` unstubbed, so the roster is FakeItEasy's empty dummy, every account is skipped as "not reported by the venue" before the credential comparison is reached, and deleting the production filter leaves both green (filed as gh#522). The foreign-key state is produced by a **database reassignment**, not a differently-keyed discovery: `POST /connections/{id}/accounts/discover` answers **409** for a connection on another key, so the API cannot create it — it arises only from rows discovered under one key whose connection later belongs to a sibling (a gh#210 rotation, or two processes on one database) | **Active** (Passing) |
| **`AuthorizationSurfaceIntegrationTests.cs`** | The **authorization surface** (gh#511 ⇒ gh#8, R-18, R-11/R-16) — every route the app maps, read from `EndpointDataSource` rather than a list, so a new route is covered the moment it is mapped. The hand list it replaces covered 11 of **45** authorized routes and included **no execution route, no kill-switch route and neither venue-truth read**; `AddAuthorization()` has no `FallbackPolicy`, so each route's gate is its own `.RequireAuthorization()` and an omission has nothing above it to catch it (`PUT /connections/{id}/credentials` and `DELETE /connections/{id}` shipped in gh#210 and were never added to the list). Guards: every mapped route 401s **anonymously** and **with a malformed token**; a structural check that the only routes reachable through the *absence* of a gate are the liveness and readiness probes, and the only ones **declaring** `[AllowAnonymous]` are login and accept-invite; the four anonymous routes really are reachable; and the token is genuinely validated — **expired**, **signed with another key**, **foreign issuer**, **foreign audience** — each against a **positive control** that mints a correct token the same way, so the negative cases cannot pass because the minting is wrong. Carries its own **anti-vacuity guard** (route count + explicit membership of all 8 execution, all 3 kill-switch and both venue-truth routes), proven necessary: with the enumeration emptied, both sweeps pass and only that guard goes red | **Active** (Passing) |

### The send path, the staged ladder and the production smoke

| Integration Test Suite | Covered Endpoints & Features | Current Status |
| :--- | :--- | :--- |
| **`OrderEndpointsIntegrationTests.cs`** | `POST /accounts/{id}/orders` — the **direct** send path: fail-closed risk absence, credential-key guard, gate refusal + audit trail, R-14 mode × environment (gh#130) | **Active** (Passing) |
| **`StagedOrderLadderIntegrationTests.cs`** | The **staged ladder** — `POST …/orders/arm`, `PUT /orders/{id}`, `POST /orders/{id}/take`, `DELETE /orders/{id}` — plus the gh#134 working-stop regression and the gh#96 DB mode guard (gh#157) | **Active** (Passing) |
| **`StopPlanPersistenceIntegrationTests.cs`** | Staged-stop plan persistence (`POST /accounts/{id}/orders` → `StopPlanRecord`); the four `CK_StopPlans_*` DB CHECK constraints proven by name on **both** safety-beyond-actual sides with a positive control; FK `ON DELETE CASCADE`; ATR not-yet-supported pin (gh#158) | **Active** (Passing) |
| **`SystemSmokeIntegrationTests.cs`** | `Category=Smoke` read-only probes against a deployed target (gh#131); the in-process host + its container are now **lazy**, so a deployed-target run starts no PostgreSQL container / needs no Docker (gh#152) | **Active** (Passing) |
| **`Api/AccountEntryConcurrencyIntegrationTests.cs`** | **Account-entry serialization** (gh#724 ⇒ gh#619; R-5 / R-11 / R-16, ADR-0007) — the concurrent proofs the unit tier structurally cannot run. gh#589 exists because `ComposeAsync` sized the R-5 gate off a **flat snapshot**, so two concurrent transmits each saw room and together doubled the risk; the fix (no-stacking + a per-account `pg_advisory_lock`) is a **database** primitive, so only real Postgres with genuinely parallel connections can witness it — an in-memory double serializes by construction and would pass over the defect | **Active** (Passing) |

### Storage tier — the event log and the database's own guards

| Integration Test Suite | Covered Endpoints & Features | Current Status |
| :--- | :--- | :--- |
| **`Data/EventBackboneIntegrationTests.cs`** | The append-only event log (ADR-0001) at the **storage tier** via `IEventLog` against the applied `AddEventBackbone` hypertable migration — envelope round-trip (`jsonb` payload, UTC instant), monotonic sequences under concurrent appends, the #156 replay two-rows-one-`Id` contract, id generation, `ReadAfter` ordering + paging, cursor upsert, blank type/source rejection; and a non-UTC `OccurredAt` rejection (gh#201). **Retention-gap signal (gh#228, flipping the gh#162 pin now gh#227 shipped):** `ReadAfterAsync` returns `EventPage.Gap` (requested cursor + oldest surviving sequence) when a cursor trails the dropped window, and stays silent for a fresh group (cursor 0), an empty head, a contiguous read, and once a consumer has resumed inside the window; the gap is durable across a restart. The drop is manufactured by deleting the oldest rows — the gap decision is a row-existence check, not a chunk read, so this is the same decision path a real chunk-drop leaves (gh#161) | **Active** (Passing) |
| **`Data/SuggestionDatabaseGuardIntegrationTests.cs`** | **The database's own guards on the suggestion journal** (gh#552 ⇒ epic gh#17; R-4, R-14, R-20) — the last line, below the API. Nine guards ship on `Suggestions` / `SuggestionDispositions` and, before this, **not one was asserted anywhere**: they existed only in the DbContext and a migration, where a careless `HasCheckConstraint` edit, a regenerated migration or a "cleanup" would remove them invisibly until bad data was already stored. All are `CHECK`s, a unique index, a self-referencing `RESTRICT` foreign key, a column default, or a PL/pgSQL constraint trigger, so **no in-memory provider enforces any of them** — only a container-backed run witnesses them, and every case writes **straight through the DbContext**, deliberately bypassing the endpoints, because what is under test is what the database refuses once something gets past them. Covers: undeclared `Mode`, unknown `State`, non-positive `Size` (0 and −1), `Confidence` outside 0–100 (**both** ends, since a one-sided range check is the classic half-guard), `ExpiresAt` at or before `CreatedAt`, the disposition's unknown `Kind`, and the **one-disposition-per-suggestion unique index** (the backstop that makes "append-only" true rather than intended). The sharp one is **`ct_suggestions_mode_matches_account`** — R-14's cross-table rule a CHECK cannot express — exercised on **INSERT and on both UPDATE arms**: moving the row's `Mode`, and moving the row to an account with a different mode, which an insert-only or Mode-only guard would each miss. Each assertion **names its constraint**, so a case cannot pass because a *different* guard fired. Prove-red: dropping all seven original guards in the database reddens **all twelve** of those guard cases while the fully-valid-suggestion **control stays green** — the control being what stops every case above passing if the table were simply rejecting all writes. **The supersede-chain guards (gh#550 ⇒ gh#620), broken out of the gh#611 review**: `SupersedesId`'s `RESTRICT` FK (`FK_Suggestions_Suggestions_SupersedesId`) refuses deleting a superseded row while a later version still points at it, and refuses insert of a dangling `SupersedesId`, each with its own anti-vacuity control (deleting/inserting an *unreferenced* row still succeeds); and `Version` defaults to `1` at the database, proven by bypassing the CLR initializer (which already sets `1` and would otherwise make the case pass with no DB default at all) — inserting with `Version` at its CLR sentinel (`0`) and reading back `1` | **Active** (Passing) |
| **`Api/SuggestionDbGuardIntegrationTests.cs`** | The **first four** database guards on the suggestion spine (gh#552 ⇒ gh#17; R-4 / R-14) against real Postgres and real migrations — the three checks `CK_Suggestions_Mode_NotUndeclared` / `_State_NotUnknown` / `_Size_Positive` and the cross-table constraint trigger `ct_suggestions_mode_matches_account`. **Sibling of `Data/SuggestionDatabaseGuardIntegrationTests.cs` above, not a duplicate of it**: this suite is the original gh#552 increment; the `Data/` one widened the same card to nine guards and added the gh#550/gh#620 supersede-chain FK. Both are kept — they name their constraints, so a guard removed from the model reddens whichever suite claims it | **Active** (Passing) |
| **`Data/CitedFactorDatabaseGuardIntegrationTests.cs`** | **The database's own guards on the multi-cited-factor set** (gh#958, QA for gh#729; R-4, ADR-0026), written independently of the dev PR and mirroring `Data/SuggestionDatabaseGuardIntegrationTests.cs`'s pattern for `SuggestionCitedFactors`. Covers the **`ON DELETE CASCADE`** FK (a factor never outlives its suggestion — asserted by re-querying the child table directly, not just that the delete didn't throw, since a missing cascade would still let the delete succeed and leave orphans); the **partial unique index** `UX_SuggestionCitedFactors_OnePrimary` (a second primary refused, paired with the anti-vacuity control that a second *supporting* row still succeeds, since the index is filtered to `IsPrimary` rows only); the **kind/columns pairing** `CK_SuggestionCitedFactors_KindColumns` (an Indicator row carrying level columns, and a Level row carrying indicator columns, each refused; a well-formed Level row accepted as the anti-vacuity control); and the scalar checks `CK_SuggestionCitedFactors_Timeframe_Positive` (both `0` and `-1`), `_LevelZoneOrdered` (equal and reversed top/bottom), and `_LevelKind_NotUnknown`. **`CK_SuggestionCitedFactors_Kind_NotUnknown` is deliberately NOT claimed here (gh#1053)**: its `"Kind" <> 0` is fully *subsumed* by `CK_SuggestionCitedFactors_KindColumns`, whose two disjuncts jointly require `Kind` ∈ {1,2}, so no row can violate the former without violating the latter and this suite's one `Kind = 0` case cannot independently distinguish them — dropping `Kind_NotUnknown` from the migration alone would redden nothing here. The constraint itself is fine (belt-and-braces in the schema, not a defect) and is not to be touched. Each case names its constraint. **Prove-red**: every guard was independently disabled in the migration (each CHECK, the FK changed to `Restrict`, the unique index dropped and separately made non-partial) and the corresponding case(s) confirmed red while the sibling anti-vacuity controls stayed green, then restored — including the `Kind = 0` case, which was found during prove-red to actually raise `CK_SuggestionCitedFactors_KindColumns` (Kind = 0 satisfies neither of that check's disjuncts) rather than the `Kind_NotUnknown` name the case first assumed, and the assertion was corrected to the constraint Postgres actually reports | **Active** (Passing) |
| **`Data/CitedFactorLevelSnapshotImmutabilityIntegrationTests.cs`** | **R-4's payoff for the snapshot-not-FK decision** (gh#958, QA for gh#729; ADR-0026): a cited `Level` factor is a **copy** of the `PriceLevel` zone at issuance, deliberately carrying no foreign key (`LevelId` is a soft reference). Drives the source `PriceLevel` through a merge (touch-count + band widen), a role flip (Resistance → Support), a retirement (`Active = false`), and finally a real **delete** — asserting the cited snapshot columns never move and the suggestion still reads through its normal `Include`, because there is no FK relationship left to break. **Prove-red**: a temporary real FK from `CitedFactor.LevelId` to `PriceLevels.Id` (`ON DELETE RESTRICT`), simulating a "fix" that reintroduces the coupling R-4 forbids, reddened the delete-step assertion exactly where the citation must survive — then removed | **Active** (Passing) |
| **`Data/RetentionGapConsumerIntegrationTests.cs`** | **Consumer-side** retention-gap handling (gh#228): the two safety-critical event-log consumers — the live `StopPromotionHost` and `ConditionalOrderHost` — must, when the log signals a gap, **log it high-severity (`Error`) and keep going**, neither swallowing the signal (silent loss) nor crash-looping. Runs the live hosts, manufactures a real hole under a caught-up consumer (delete the consumed window + burn a sequence), and asserts the emergency log fired and the cursor advanced past it (gh#228) | **Active** (Passing) |
| **`Data/InventoryIdentifierLiveSchemaIntegrationTests.cs`** | **§2's own "proven by name" claims, checked against the live schema** (gh#981, the gh#979 class) — the meta-guard `check-integration-test-inventory.sh` names as a limit it cannot cover: that script verifies a suite *exists*, never that a row's *content* is true, so a renamed database object leaves a row naming something that no longer exists with every arm of that check green. Harvests every `CK_`/`IX_`/`FK_`/`UX_`/`ct_` identifier §2 asserts "by name" via a **regex over §2's own markdown text** (never a hand-maintained second list — the harvest expands the doc's own shorthand, e.g. `` `CK_Suggestions_Mode_NotUndeclared` / `_State_NotUnknown` ``, against the full name's table prefix) and asserts each exists on the **live migrated schema** — `pg_constraint` for CHECK/FK, `pg_indexes` for indexes, `pg_trigger` for constraint triggers — the only authority true by construction for all four kinds, unlike the two rejected designs gh#981 measured: grepping the tree (migrations are append-only, so a renamed object's old name still appears and the grep flags nothing) and the ModelSnapshot (flags 4 of 17 candidate names, 3 of them false positives — EF's conventional FK/index names and a raw-SQL constraint trigger are never spelled there). **Anti-vacuity anchor**: the harvested set must be non-empty and contain one known member per kind, plus a regression guard on the harvest's own false-positive trap (a bare two-segment `` `CK_StopPlans` `` prose mention, not a real constraint name). **Prove-red**: gh#979's stale pre-rename name for the row below (gh#762 renamed it to `UX_SoftSignalFeedback_Importance`) reddened this suite's own Theory case before that row was fixed in this same PR | **Active** (Passing) |

### Tenancy, risk-profile and connection lifecycle

| Integration Test Suite | Covered Endpoints & Features | Current Status |
| :--- | :--- | :--- |
| **`MultiTenantIsolationIntegrationTests.cs`** | R-20 default-deny workspace isolation between two operators (ADR-0017): collection-scoping (`GET /connections` excludes the other operator), `GET /connections/{id}/accounts` → 404 for a non-owner, staged-order `take`/`cancel` → 404 (order stays `Staged`), and gate-decision isolation proven at a user-scoped `DbContext`; second operator via the invitation flow (gh#132) | **Active** (Passing) |
| **`RiskProfileLifecycleIntegrationTests.cs`** | Per-account risk declaration (`PUT /accounts/{id}/risk`) — persistence + one-per-account replace-on-redeclaration; and the **R-12 re-gate**: a profile tightened *after* arming refuses the next `take` (422 `RefusedByRisk`, order stays `Staged`, the refusal journaled as a blocked gate decision) (gh#143) | **Active** (Passing) |
| **`ConnectionLifecycleIntegrationTests.cs`** | Connection creation, account discovery, and the per-account stage override — the roster persists and reads back with **mode resolved from the firm's declared conventions**, not the venue's flag (Practice, not the stub's Live; unrecognised names stay `Undeclared`, gh#60); one-login-per-firm×platform `409` (ADR-0016); stage override → convention mode / `Unknown` refused; clear reverts to the resolved stage (gh#142). **gh#229** completes the suite for shipped gh#210: **credential rotation** (`PUT /connections/{id}/credentials`) repoints the key, the venue path picks up the new key on the next call, the stored value is the **verbatim env-entry name — never a secret** (ADR-0015), and R-20 gives `404` to a non-owner; **soft-delete** (`DELETE /connections/{id}`) deactivates rather than removes, **cascades to child accounts**, **preserves the journal** (orders survive — the highest-value guard, given the real `OnDelete(Cascade)` FKs), is idempotent, and R-20-scoped. **DEFECT gh#322** pinned: the send path does not enforce deactivation, so a retired connection still transmits orders (flips to a guard when fixed) | **Active** (Passing) |

### Staging tier — the pre-live venue gates

| Integration Test Suite | Covered Endpoints & Features | Current Status |
| :--- | :--- | :--- |
| **`TestHost/Staging/*` + `Observability/TelemetryStagingIntegrationTests.cs`, `Api/BracketSizingStagingIntegrationTests.cs`** | **Post-merge staging harness (gh#335)** — the shared bootstrap for the venue-execution gates (gh#269, gh#293) and the telemetry SLI suite (gh#234): `StagingConfig` binds the deployed API + a reserved **ProjectX practice account** + the live Prometheus/Loki/Tempo endpoints from **CI secrets** (never source); `[StagingVenueFact]` / `[StagingObservabilityFact]` / `[StagingGatewayFact]` **skip by construction** when unconfigured (so pre-merge CI stays green); `StagingApiClient` authenticates against the deployed API; `[StagingExecutionCollection]` serializes order-placing onto the one practice account (R-14, practice-only). Telemetry cases query the real backends | **Staging** (skips pre-merge) |
| **`Api/BracketPreservationStagingIntegrationTests.cs`** (gh#269 ⇒ gh#259) | **Bracket-preservation pre-live gate** — certifies the gh#259 reprice safety claim only the real gateway can witness: a **price-only in-place modify** of a working bracketed entry **leaves the attached protective bracket intact**. Places through the deployed app (its real gate), reprices in place, fills, then reads **venue truth directly from ProjectX** — the app exposes no resting-orders read and its `WorkingOrder` view drops the size — via `StagingProjectXGateway` (`GetOpenOrdersAsync` → raw `Order` with `Size`/`StopPrice`), asserting a native protective stop still rests covering the position. Prove-the-red: a gateway that drops the bracket on modify leaves the fill naked → no resting stop → red. Serialized on the reserved practice account; **prove-red'd on the first staging run** (only the gateway can witness it) | **Staging** (skips pre-merge) |
| **`Api/BracketSizingStagingIntegrationTests.cs`** (gh#293 ⇒ gh#292) | **Bracket-sizing hard pre-live gate** — certifies the gh#292 resize inference: the always-native safety bracket (no size field, instantiated on fill) sizes to the **realized fill**. **Resize UP** while working then fill → the on-fill protective stop covers the **full larger fill** (no naked excess); **resize DOWN** then fill → it covers the **smaller fill exactly** (no oversized residual that reverses into a naked position after close). Reads venue truth from the gateway (`StagingProjectXGateway`): position size + protective-stop `Size` both equal the resized quantity. Prove-the-red: a gateway that pins the bracket to the placement-time size leaves the stop at the old quantity → red. The partial-fill window is closed at the app (modify refuses a partially-filled order), so a 0-filled working order's realized fill is the whole new size. Serialized on the practice account; **prove-red'd on the first staging run** | **Staging** (skips pre-merge) |
| **`Api/BracketAfterPartialCloseStagingIntegrationTests.cs`** (gh#1012 ⇒ gh#928) | **Bracket-after-partial-close hard pre-live gate** — the sibling of gh#293: that one certifies bracket *sizing* across a working-order **resize**, this one certifies bracket *behaviour* across a position **partial close**. Fills a bracketed entry (position `N`, native protective stop sized `N`), issues `partialCloseContract` for `M < N` at the gateway, then reads venue truth: every resting leg must have **auto-reduced to `N-M`**. Long, **short**, and a **take-profit (OCO)** case, since a gateway could manage one direction or one leg and not the other. Baseline assertions (position **and** bracket at `N` *before* the close) are load-bearing — without them a post-close reading of `N-M` cannot distinguish an auto-reduce from a bracket that was under-sized all along. Prove-the-red: a bracket **left at `N`** fails the size assertion (on trigger it sells `N` against an `N-M` position and **flips into an opposing one** — a protective mechanism creating exposure); a bracket **cancelled** fails the not-null assertion (the remainder is left **naked**). Only an auto-reduce is green. The close is issued **direct to the gateway** because the copilot has no reduce path at all — gh#928 is unimplemented and blocked on this gate; the entry is still placed through the deployed app's real risk gate. **NOT YET RUN — no practice credentials in the authoring environment; the finding is still open** and nothing downstream may claim the gate cleared | **Staging** (skips pre-merge) — **unrun** |
| **`Api/CustomTagFillEchoStagingIntegrationTests.cs`** (gh#643 ⇒ gh#631) | **customTag-echo pre-live gate** — certifies the one venue assumption gh#631's fill-level veto rests on and that **no mocked test can reach**: that ProjectX echoes `customTag` on a **terminal (filled)** order record. The echo is proven for the placement ack and for *resting* orders, but the veto only ever reads a **filled, round-tripped** order; if it is not echoed there, every answer is `NoFillFound`, the veto silently never fires, and the gh#622 second-entry hazard is as open as before — **with the whole unit suite green**, because those necessarily fake the gateway. **Two gates, deliberately, because they fail for different reasons:** an *independent* venue-truth read through `StagingProjectXGateway` (`GetOrdersAsync` → raw `Order`, matched by a predicate written without reference to production) — red means **the venue does not echo the tag**; and a *production-seam* call to `IOrderExecutor.FindFilledOrderByTagAsync` asserting `Filled` / `VetoesRelease` / `FilledSize` — red **while the first is green** means the venue is fine and **our matching is wrong**. Both run the same **place → fill → flatten** round trip, then assert the fill is still findable **while the resting-orders and positions reads see nothing** — that contrast *is* the gate, being the state those two reads structurally cannot see and exactly the state the reconcile observes. A third case pins the cheap negative (a never-placed tag reads as no-match, not an error). Prove-the-red: **pending the first staging run** — this session had no staging credentials. If it comes back red, check the `SearchOrderRequest` `startTime`/`endTime` vs `startTimestamp`/`endTimestamp` question in the ProjectX submodule **first**: both gates share that path, so a schema mismatch fails them identically and is a client bug, not a disproof of the echo. Serialized on the reserved practice account | **Staging** (skips pre-merge) |
| **`Api/VenueRefusalDefinitiveStagingIntegrationTests.cs`** (gh#668 ⇒ gh#629) | **Definitive-refusal pre-live gate** — certifies the one venue assumption gh#629's throw-site classification rests on: that a **genuinely-invalid order comes back from the real gateway as a well-formed response with `success == false`**, so `ProjectXVenue.PlaceOrderAsync` reaches its `VenueRefusalKind.Definitive` arm. Every unit test fakes the client (they assert the *mapping*, **given** a `!success` response) and gh#663's container-tier suite arms an adversarial venue to throw the exception directly — neither ever asks ProjectX. **It fails in both directions:** if the gateway signals invalid orders any other way (HTTP 4xx, a transport throw, `success: true` with an error payload) the `Definitive` arm is **never reached in production**, every venue rejection strands as `Taking`/`Firing`, and the auto-resolve is **inert while every test is green** (the gh#643 shape); if the gateway can answer `!success` for an order it nevertheless placed, a maybe-live order is auto-released and the next take/send/fire **stacks a second live order** on it (gh#530 / gh#589). The probe is a ticket valid in **every** respect except its contract handle, derived from the real active contract with only the expiry segment replaced (`CON.F.US.EP.M25` → `…QA668`) — an unknown contract **cannot rest, fill, or be partially accepted**, unlike a tick/size violation that could be rounded or clamped and left resting. It transmits through `StagingProjectXGateway.PlaceOrderAsync` — the production adapter, **bypassing the app deliberately**, because the app's own gate refuses a malformed ticket before transmit and the venue would never see it. Then the assertion the exception's shape cannot make: **nothing rests** — working orders gain nothing, the net position is unchanged, and order history shows no working or executed record under the probe's `customTag` (a *rejected* record is fine). Non-vacuity is built in: a **valid control entry** placed through the app (200 ticks below the market, where it cannot fill) must be witnessed by the *same* `OpenOrdersAsync` call that later witnesses the probe's absence — a blind read reddens on the control. Prove-the-red: **pending the first staging run** — this session had no staging credentials; each run writes one `gh#668 OBSERVED:` line carrying the exception type, `Kind`, `ErrorCode` and the gateway's `errorMessage` (or an HTTP status, which would itself mean the `!success` route was not taken). A red is **reported against gh#629 as a new `work:code` issue**, never fixed from this suite. Serialized on the reserved practice account (R-14) | **Staging** (skips pre-merge) |

### Market data — bars, indicators and gap recovery

| Integration Test Suite | Covered Endpoints & Features | Current Status |
| :--- | :--- | :--- |
| **`MarketData/GapBackfillIntegrationTests.cs`** | **Event-log gap → backfill recovery** (gh#514 ⇒ gh#306, R-1) — `GapBackfillService.ReplayForStopPromotionAsync` driven **directly** against real Timescale. Driving it directly rather than through `StopPromotionHost` is the point: that host's catch-all turns an EF translation failure into *"resuming at the head without recovery"*, so a defect here would be a log line rather than a red test. Guards: the **relational projection** — `List<Guid>.Contains` → `= ANY`, a constructor projection into a **private nested record**, `Distinct()` over it, and `Symbol ?? Instrument` → `COALESCE` — all of which the unit tier evaluates client-side and so never translates; a hidden stop **promotes** when the window's adverse extreme crossed its band, and **does not** when it fell one tick short (a control pair differing by exactly one scalar, which is also the only thing that catches a **Low/High polarity swap** in the synthetic quote — the negative case alone cannot); coverage measured from the **finest stored resolution** with the real uncovered duration **metered**, against an hourly bar spanning the same window that must not be credited; a fully covered window **meters nothing**; and a replayed gap **promotes exactly once**. gh#557 added the **missed-trigger** half: a pending conditional the blind window crossed is **reported and never fired** — the deliberate non-recovery, because a conditional fires an *entry* and firing one on a cross that happened minutes ago would act on stale grounds. The positive detection carries the weight there (a rise-to judged against the window's **high**, a fall-to against its **low**, with an uncrossed control), because `DetectMissedTriggersAsync` takes no venue and performs no save — so "still Pending, zero orders" is structurally true even when the read finds nothing. The suite is the first to store two bar resolutions over one window — the existing seeding hardcodes a single one, so the `Min()` that picks the finest series had never had a second value to choose between | **Active** (Passing) |
| **`MarketData/GapBackfillHostRecoveryIntegrationTests.cs`** | **Gap recovery through the LIVE hosts** (gh#557 ⇒ gh#306, R-1) — the half its sibling above structurally cannot reach. That suite strips every hosted service *on purpose* (with `StopPromotionHost` alive a promotion count is true whether the backfill acted or not, and `VenueConnectionMonitorHost` flips every `Hidden` plan to `Orphaned` on its first pass, deleting the subject), so it proves what the **service** computes; this one proves what the **host** does around it: which window it hands the service, whether it runs it at all, and whether the consumer survives it throwing. Three cases: a pending conditional the blind window crossed is **named to the operator** — the host's log is the only surface this consumer reports through — with its direction, trigger price and contract, while an **uncrossed control** must not appear and all three stay `Pending` with nothing transmitted; **no committed cursor ⇒ recovery is skipped**, not run against an invented lower bound (bars that *would* promote are seeded precisely so "promoted nothing" is a statement about the skip); and a **recovery that throws** is contained — the consumer keeps consuming past the gap, the venue shows the attempt was really made, and the plan stays `Hidden` rather than claiming an exchange-held stop that does not exist. Two traps the suite is built around: every gap is quoted on a **different contract** than the seeded stops (the retention-gap recipe's own MES quote would otherwise promote them through the *ordinary* path), and stops are seeded **after** cursor catch-up so the monitor's first pass cannot orphan them. Windows are made real by **back-dating the committed cursor** rather than inventing one. Every state assertion waits for the cursor to clear the gap first — asserting off the log alone let a broken host pass by not having finished yet, which prove-red caught | **Active** (Passing) |
| **`MarketData/IndicatorProjectionIntegrationTests.cs`** | **The indicator projection and the ATR it feeds the stop band** (gh#513 ⇒ gh#310, gh#311, R-1, R-22) against real Timescale. The chain **bars → projection → `StoredIndicatorSource`** had never run at any tier: every StopPlan-touching suite seeds a *tick*-based band, and the only ATR in the integration tier was a literal handed to `ResolveDistance`, so the value had never come from stored bars. That gap is silent by nature — an ATR the store cannot produce resolves to `null`, the band is unresolvable, and an ATR-banded stop simply never promotes, indistinguishable from "price never came close". Guards: **every series the bar store holds is projected** (four series across two instruments × two resolutions, so the projection-then-`Distinct()` over a private nested record has something real to collapse and per-series separation is observed rather than assumed); **an unchanged second pass writes nothing** (idempotence judged where it can only be judged — the column is `numeric(18,8)`, so a value carrying more scale would round-trip to something the in-memory `row.Value != value` comparison could call different, rewriting all history every poll); the **composite key rejects a duplicate bucket** while still admitting a *different* indicator on the same bucket, so ATR and RSI can share a series (the key lives in a hand-written migration, invisible to the model-level in-memory provider); and the **execution path reads back what the projection wrote**, with an uncovered instrument returning `null` as the control. Reuses `OcoExitTestPostgresFactory` for host suppression | **Active** (Passing) |
| **`MarketData/KeyLevelProjectionIntegrationTests.cs`** | **The key-level projection and its reconcile into `PriceLevel`** (gh#946 ⇒ gh#597, gh#596, R-10 / R-22) against real Timescale, written independently of `KeyLevelProjectionServiceTests` per the QA adversarial-double rule. What only real Postgres proves: the four `CK_PriceLevels_*` checks actually reject a bad row (`ZoneOrdered`, `Bottom_Positive`, `Kind_NotUnknown`, `Timeframe_Positive`) — the InMemory unit tier enforces only the EF model's shape, never a DB constraint, so a detector defect producing one of these shapes would pass unit and 500 in production; **idempotence over `numeric(18,8)`** — `Significance = prominence / ATR` does not terminate, so a second pass over unchanged bars must write nothing and the exact quantised value (`4.16666667m`) must round-trip through the real column; **reconcile persisted** — a role reversal flips `Kind` **in place** on the same `Id`, an overlapping pivot raises `TouchCount` **in place** on the same `Id`, and a level that no longer forms is **retired** (`Active = false`, kept for the journal) and excluded from `IPriceLevelSource.GetActiveLevelsAsync`; **ATR reuse end-to-end** — two series with different real spike magnitudes are run through the **real** `IndicatorProjectionService` (gh#311, never a hand-fed literal), and the series with the larger real ATR must come out with the wider zone, proving each series' width is driven by its **own** reused ATR rather than a shared or stale value, with a sibling control proving a series with no ATR at its pivot bucket writes no zone at all (no partial write); and **per-series isolation** — a corrupt series (a negative ATR `KeyLevels.Detect` refuses) must not cost a sibling series its already-committed `SaveChangesAsync`, a durability claim only a real backend's commit semantics can prove. Reuses `OcoExitTestPostgresFactory` for host suppression | **Active** (Passing) |
| **`MarketData/BarStoreIntegrationTests.cs`** | **Clean-historical bar store & REST backfill** (R-1, R-9, R-20, ADR-0001) via `BarBackfillService.BackfillAsync` driven directly against real Timescale: the store is a **system of record**, not a cache — **no retention policy** (introspected against `timescaledb_information.jobs`; the event log's 24h policy is the positive control) and **untouched when the event log's data is dropped**; an overlapping re-poll **updates in place, never duplicates** (composite PK `{Venue,Instrument,Resolution,BucketStart}` rejects a raw duplicate by construction); a venue **revision wins in place**; a **still-forming bucket is never stored as final** (partial-bar drop — the highest-value guard); the **capability seam** refuses loudly when `HistoricalBars` is absent (R-17); a **failed poll heals next pass** and a **venue throw never kills the host**; and bars are **visible to every operator** (no tenant filter — market data is global, R-20). Extends the stub to feed bars + model the capability/throw seams; hosts suppressed via `OcoExitTestPostgresFactory` (gh#303) | **Active** (Passing) |

### Cross-asset context prices

| Integration Test Suite | Covered Endpoints & Features | Current Status |
| :--- | :--- | :--- |
| **`MarketData/ContextIngestionIntegrationTests.cs`** | The **producer contract** of the cross-asset context price path (gh#608, of gh#496 / gh#411; R-1, R-17, ADR-0001) — its own event type and a deterministic id, over the plain `PostgresApiFactory`. Written from the issue and the operator decision recorded on gh#496, not from the implementation. Context prices ride the same event log as quotes but must never be mistaken for them: a shared type would put a correlated instrument's tick through the stop-promotion and conditional-firing consumers | **Active** (Passing) |
| **`MarketData/ContextIngestionLiveHostIntegrationTests.cs`** | The **live-host half** of the same coverage (gh#705, of gh#608; gh#496, gh#495, gh#411 · R-1, R-17 · ADR-0001) — the cases that need `StopPromotionHost` and `ConditionalOrderHost` actually *running*, so they sit on the gh#557 live-host fixture shape (`ContextLiveHostPostgresFactory`) rather than the host-stripped factory its sibling uses | **Active** (Passing) |
| **`MarketData/ContextSourceOutageIntegrationTests.cs`** | The **second half of gh#705** — a **context outage with the real adapter in the loop** (case 3) and **capability refusal at the seam** (case 5). Traces gh#496, gh#495, gh#411 · R-1, R-17 · ADR-0001 · gh#557 (the live-host fixture shape) · gh#304 (the `ContextIngestion__Symbols__0..7` allowlist shape — the compose-allowlist trap the platform contract names) | **Active** (Passing) |

### The background engines — conditional firing and auto-flatten's three tiers

| Integration Test Suite | Covered Endpoints & Features | Current Status |
| :--- | :--- | :--- |
| **`ConditionalFiringIntegrationTests.cs`** | The **synthetic conditional-entry firing engine** (ADR-0007, pairing the #198 watcher): `POST /accounts/{id}/orders/conditional` arms a pending conditional (persisted `Pending`, **transmits nothing**); the firing watcher (`ConditionalFiringService` / `ConditionalOrderHost`) fires it to a **working native order** when a quote crosses the trigger, **re-running R-5 / R-16 / R-12 at fire time** — a conditional valid at arm but violating the gate at fire is **refused, not sent** (proven by tightening risk between arm and fire); cancel-if drift; expiry outranks a crossing trigger; fired-then-replayed idempotency; a live-background-host `market.quote` backbone consume; and the `CK_ConditionalOrders_CancelDrift_StaleSide` / `_Direction_NotUnknown` DB checks proven by name (gh#180) | **Active** (Passing) |
| **`AutoFlattenSchedulerIntegrationTests.cs`** | The **primary auto-flatten scheduler** (R-13, ADR-0013) via `AutoFlattenService.RunPassAsync` at a controlled instant: closes an open position at its per-market deadline (`flatten.executed`) and **never opens** exposure; warns before the deadline, journals `missed` past the firing window, `disabled`, and `unconfigured`; retries to the attempt cap then `escalated` on a surviving position — every decision journalled to the real event log. Drives the shared `TestHost` position stub (open positions + configurable close) with the always-on hosts suppressed (gh#186) | **Active** (Passing) |
| **`AutoFlattenWatchdogIntegrationTests.cs`** | The **redundant auto-flatten watchdog** (R-13, ADR-0013) via `AutoFlattenWatchdogService.RunPassAsync`: the independent second tier steps in only on the primary's failure — a position open past deadline **+ grace** — closing once per pass (`flatten.watchdog.saved`), **defers to the primary inside the grace window**, journals `flatten.watchdog.rejected` when the close leaves exposure **or the venue rejects it** (one attempt, retried next pass — never a silent give-up), and `flatten.watchdog.critical` past the firing window; never opens exposure (gh#188) | **Active** (Passing) |
| **`DeadMansSwitchCheckInIntegrationTests.cs`** | The **dead-man's-switch daily check-in** (R-13, ADR-0019, ADR-0015) via `FlattenCheckInService.RunPassAsync` at a chosen instant — the **tier-3, out-of-process** redundancy whose **silence** is the pager signal, and the first coverage it has had. Both failure directions are guarded: exposure past the deadline **withholds** the instrument's check-in (so the monitor pages) while other flat markets still report; a **flat** market is vouched for exactly once, recorded against the **market** day; exposure **aggregates across every account** on the venue (a flat account beside an open one still withholds); an account on a **sibling credential key** is skipped before any venue read (ADR-0015); a **rejected** report is deliberately **not recorded**, so the next pass retries it (proven by refusing once, then accepting); and outside the `IsDue` window the pass does **no venue work at all** — witnessed by a new `PositionReadCount` on the stub, since an empty result alone cannot tell "didn't look" from "looked and withheld". All six guards **prove-red'd** across two rounds (exposure-ignored, credential-filter off, rejection recorded anyway, both due-gates bypassed; then a second round proving the positive report-flat guard). The external monitor is a `RecordingDeadMansSwitch` — an outbound third-party seam like the venue, which records what was vouched and can refuse, never supplying the decision; hosts suppressed via `DeadMansSwitchTestPostgresFactory` (gh#408) | **Active** (Passing) |
| **`Api/ConditionalFiringPerRecordIntegrationTests.cs`** | **Per-record commit in the conditional-firing pass** (gh#578, of gh#532 — safety-critical; R-11 / R-16, ADR-0007, ADR-0013), plus the **transmit→journal window** the durable pre-transmit intent closes (gh#577). Written from the issues and the requirement, not from the implementation (QA contract §Role). The failure it exists for: a pass that commits once at the end loses every earlier conditional's journal when a later one throws — leaving orders live at the venue with no record behind them | **Active** (Passing) |
| **`Api/VenueRefusalOutcomeIntegrationTests.cs`** | The **typed venue-refusal outcome** (gh#663, of gh#629 — safety-critical; R-11 / R-12, ADR-0007's 2026-08-04 update, ADR-0013 §9) at **both** catch sites — the operator take (`POST /orders/{id}/take`) and the conditional fire (`ConditionalFiringService`). Written from the issue, not the implementation. Arms an adversarial venue to throw at the send, so the classification (`Definitive` vs a maybe-live transport fault) is exercised through the real composition; the *venue's own* half of that assumption — that a genuinely-invalid order really does come back `success == false` — is the staging gate `Api/VenueRefusalDefinitiveStagingIntegrationTests.cs`, which is the only tier that can ask ProjectX | **Active** (Passing) |
| **`SuggestionDriftIntegrationTests.cs`** + **`SuggestionDriftConsumerLifecycleIntegrationTests.cs`** + **`SuggestionDriftTeardownIntegrationTests.cs`** | The **suggestion-drift consumer** (gh#632 ⇒ gh#546, R-4 / R-12, ADR-0013) — the event log's third `market.quote` consumer, siblings of the #153/#198 quote watchers above. Authored from the spec, independent of the gh#546 implementation. `ISuggestionDrift.MarkDriftedStaleAsync`'s guarded `ExecuteUpdate` (the gh#530 shape — unverifiable at the unit tier, which drives a faithful in-memory double instead) is driven **directly** against real Postgres: a long's drifted ask / a short's drifted bid → `Stale` (both directions, both sides); within tolerance stays `Active`; the **exact boundary** (`\|price − entry\| == band`) stays `Active`, pinning the strict `>`; `StateChangedAt` lands in the **same write** as the state move. **Forward-only**: the predicate is `Active`-only — a `Stale` or `ExpiredVoid` row (and its stamp) is never touched, even under **genuine concurrent Postgres transactions** racing drift against the sibling expire sweep's `IN (Active, Stale)` guard, which always resolves to `ExpiredVoid` regardless of interleaving order (never backward). **Cross-owner**: one pass marks Active rows under different `UserId`s alike — the background discovery bypasses R-20 by construction (no owner predicate at all). The **symbol↔contract bridge** (`SuggestionDriftService.ProcessQuotesAsync`, direct-driven): a quote for the venue-resolved front-month contract marks the suggestion stored under its **neutral symbol**; an instrument with **no configured spec** or one the **venue cannot resolve** (an adversarial `MakeResolveContractThrow`, added by this suite) leaves it `Active` — "cannot measure ⇒ no transition" (R-4), both halves. Hosts suppressed via `OcoExitTestPostgresFactory` (the sanctioned reuse other suites already make of it) for the guarded-update/bridge suite; the **consumer lifecycle** (the gh#546 DoD's explicit ask) is the separate live-host suite over `SuggestionDriftTestPostgresFactory` (mirrors `RetentionConsumerTestPostgresFactory`'s log-capturing shape): a real backbone `market.quote` reaches the suggestion end to end, the `suggestion-drift` cursor advances and commits **per batch** across two sequential passes, and — in its **own** suite and fixture (`SuggestionDriftTeardownIntegrationTests` over `SuggestionDriftTeardownTestPostgresFactory`) — an independently-torn-down host exits **promptly and without an Error-level log from the host under test**, the observable trace the gh#169 `ObjectDisposedException` cascade would otherwise leave. That teardown case is deliberately isolated (gh#632 review): its fixture host runs **no** hosted services and `CreateDriftOnlyHost` adds back exactly one, so no second drift consumer races the same `suggestion-drift` cursor on the same database, and the Error assertion is **scoped by logger category** so an unrelated always-on consumer cannot fail it for the wrong reason. Executed against real Postgres — **17/17 green**; prove-red reasoning for the guarded-update geometry remains by inspection against `SuggestionLifecycle.HasDrifted`, documented in the suite's own remarks | **Active** (Passing) |

### The outbound seams — the durable pager and the agent-review route

| Integration Test Suite | Covered Endpoints & Features | Current Status |
| :--- | :--- | :--- |
| **`Observability/NotificationDurabilityIntegrationTests.cs`** | The **durable notification seam** (gh#442 ⇒ gh#400/gh#437, R-13, ADR-0019) — that a page **survives the process that raised it**. Runs on the shared `NotificationHarnessPostgresFactory`, which substitutes only the **wire** (the socket beneath `PushoverNotificationChannel`), so the outbox, relay, queue, dedup decorator and the real Pushover adapter are all **production objects** — and the `Pushover:IsConfigured == true` branch is under test for the first time. Guards: a page is **persisted before anything is delivered** (read through a fresh scope, so it proves a commit); one incident is **one row** *while it is still owed* (the gh#458 unique index on `DedupKey` **filtered to `DeliveredAt IS NULL`**, invisible to an in-memory provider); an incident that clears first is **withdrawn undelivered** and never pages; and a row **left by a dead process** is picked up by a later host. gh#481 added the **dedup key's lifetime**: a *delivered* incident releases its key and is recordable again; suppression still holds while the first occurrence is owed (asserted across two scopes, so the change-tracker's `Local` set cannot be what suppresses); a **resolved-and-recurring** incident pages twice; and an already-owed incident **does not abort the producer's save** — the gh#455 shape, driven through a real auto-flatten escalation, since `EnlistAsync` only *stages* and the violation would surface on the flatten's own `SaveChanges`, outside the try/catch that guards the enlist. Ships with a **fidelity guard** — three layers (a `sealed` `ConfigureTestServices`, a descriptor-count check around the suite hook, and a resolve-and-typecheck of the composed seam) plus named `NotificationHarnessFidelityTests` — because the gh#442 defect was a *copy-pasted* harness block that silently deleted the outbox from two hosts once gh#437 landed; the guard is **prove-red'd** by pasting that block into a third harness and watching it throw by name. The four `DEFECT gh#459` pins (the P0 this suite found) were flipped to the real guarantee when gh#468 landed. **One assertion is now pinned `gh#497 — resolved, and **not** by flipping this assertion`**: the second occurrence of a *delivered* incident is recorded in the outbox (2 rows — a delivered incident releases its key there, gh#458) but deliberately still collapses to **one page**. Two occurrences with **no resolve between them** are indistinguishable at this layer from one *continuing* incident, and the flatten re-emits roughly every 15s while exposure persists — releasing the key on delivery would page ~120 times in a 30-minute exposure, precisely the alert fatigue ADR-0019 §4 exists to prevent. gh#497's real defect was that the incident never **ended**: `AutoFlattenService` resolved only on its **own** successful close, so an exposure ended by the operator's hand (or a prop firm's forced flatten) left the key armed forever. **Fixed at the producer** (`f8aa3a1`) — a pass now re-arms whenever it observes the instrument flat **by any means**; `Outbox_ShouldPageAgain_WhenTheIncidentResolvedBetweenOccurrences` is the end-to-end shape of it (gh#442, gh#481, gh#497) | **Active** (Passing) |
| **`AgentReviewRouteIntegrationTests.cs`** | The **agent-review route** (R-4, ADR-0008, ADR-0007) via `TriggerEvaluationService.ScanAsync` at a chosen instant — the **first place an LLM enters the system**, and the first coverage of *"the gate lives below the model."* The headline guards assert that invariant **behaviourally**, which the structural unit test cannot: after a fire — **including one whose completion demands execution and a disabled kill switch** — the journal holds a `Suggestion` and **nothing else** (no `Order`, no `GateDecision`, no `StopPlan`), the venue records zero calls, and the proposal stays inert across later scans. **One review per arming edge** is proven durably (three scans of a still-true condition ⇒ exactly one LLM call), including under a **provider outage** (a fault consumes the edge — no retry storm, the unbounded-spend failure), and a trigger authored into an already-satisfied condition **seeds silently**. **Size and mode are the operator's**: a completion demanding 50 contracts still stages the trigger's size, and mode is read live off the account. Fail-closed through the **production composition**: unparseable output and an **absent direction** (which must never default to `Buy`) stage nothing, and a **default deployment with no `Llm:ApiKey` binds `NullTriggerReviewer`** — zero provider calls, but an honest advisory. R-20 scoping and the `CK_Triggers_AgentReview_RequiresAccountAndSize` / `CK_Triggers_Mechanical_NoAccount` checks are proven against **real Postgres** (asserted by constraint name). The only double is an **adversarial `ILlmProvider`** — it feeds raw completions, never the decision, so the real `LlmTriggerReviewer` stays under test; indicator readings are seeded as real `IndicatorValues` rows through the production source. Hosts suppressed via `AgentReviewTestPostgresFactory` (gh#429) | **Active** (Passing) |
| **`AiSpendIntegrationTests.cs`** | The **AI-spend governor + ledger** over that same agent-review route (gh#479 ⇒ gh#448 / gh#431 / gh#478, R-20, ADR-0008) against **real Postgres**, on a budget-configured `AiSpendTestPostgresFactory` — **one budget per factory**, so cases vary the *seeded spend*, never the config. Guards: an **empty spend window reads 0** and keeps the governor **active**, witnessed by a same-pass accrual still blocking a second fire (prove-red'd by **faulting the read** — `CallCount → 2`); the day's spend **summed across every owner** — operator + a stranger + the `SystemOwner` embed-sentinel via `IgnoreQueryFilters` — over budget **blocks** the fire (**zero** model calls) with the arm still advancing to `Fired` and a durable outbox advisory (prove-red'd by scoping the read per-owner); the window **rolls over on the Central trading day, not UTC midnight** — a 02:00Z row belongs to yesterday (prove-red'd by keying on UTC midnight); **one billed triage call ⇒ exactly one `AiUsage` row**, owned by the *firing* operator, surviving the seven `CK_AiUsage_*` checks, with `$0.00270000` round-tripping `numeric(18,8)`; and each `CK_AiUsage_*` **refusal by name** (a `Theory` on `PostgresException.ConstraintName`) with a well-formed positive control. The only double is the same **adversarial `ILlmProvider`** (gh#559's configurable `LlmUsage`, so the spend assertions are non-vacuous). **Finding (flagged for the Coding Agent):** the issue's literal prove-red — delete the `(decimal?)` cast so `SUM`-over-empty throws — does **not** reproduce on EF 10 + Npgsql, which return 0 for a non-nullable `SUM` over an empty set; the cast is **defensive, not load-bearing**, and `ReadWindowSpendAsync`'s comment claiming the non-nullable overload "would throw" is outdated. a **fail-open ledger** — a DB `BEFORE INSERT` barrier refusing every AiUsage row is swallowed by **both** the ledger's own catch and the fire's call-site catch, so the committed suggestion + firing survive (prove-red'd by removing both); and a **budget-aware escalation skip** (gh#478) — when triage fits but a triage→deep pair would not, the deep call is withheld with an advisory (prove-red'd by forcing `allowEscalate`). All seven cases landed | **Active** (Passing) |

### Overrides, connection loss and restart recovery

| Integration Test Suite | Covered Endpoints & Features | Current Status |
| :--- | :--- | :--- |
| **`KillSwitchIntegrationTests.cs`** | The **kill switch** (R-11, ADR-0007) end-to-end over HTTP (10 tests): engaging (**hold-to-confirm** — an unconfirmed request is refused `422` and does not engage) **disables outbound** so a subsequent direct send **and** a staged `take` are both refused (`RefusedByKillSwitch`, nothing transmitted, the ticket stays `Staged`, and the refusal **precedes sizing** — no `GateDecisionRecord`); **cancels working orders**; and per mode either **flattens all** open positions (close called, count reported) or **halts only** (leaves them on their native safety stops, closes nothing) — with an **omitted mode failing safe to FlattenAll**. The reducing path is **deliberately exempt**: auto-flatten still closes at the deadline while engaged (ADR-0007). Disengaging **re-enables** outbound; the engaged lock **rehydrates at host startup** from the durable row (a fresh host on the same DB comes up engaged, ADR-0013). Reuses the shared `TestHost` position stub + `FlattenTestPostgresFactory` (gh#190) | **Active** (Passing) |
| **`OrphanHandlingIntegrationTests.cs`** | **Connection-loss orphan handling** (R-11, R-19, ADR-0013) via `OrphanGuardService.OrphanAsync`/`RearmAsync` + `StopPromotionService.PromoteForQuoteAsync`: a drop moves **hidden** stops to `Orphaned` (high-severity `synthetic_risk` log) while **native** exchange-held stops and **pending conditionals** are left untouched; an orphaned stop is **never promotable** (Hidden-only, by construction) and the **native safety stop stays the floor** throughout; reconnect **re-arms** orphans to `Hidden` against venue truth and **auto-resumes nothing else** (native/retired/conditional); a restart **reconciles lingering orphans** on the first pass; ownership is **preserved** through the `IgnoreQueryFilters` sweep; and flaps are **idempotent**. Adversarial venue positions + always-on hosts suppressed via `OrphanTestPostgresFactory` (gh#192) | **Active** (Passing) |
| **`OrphanGuardEventLogIntegrationTests.cs`** | **Orphan-guard protection events on the event log** (gh#707, of gh#704; R-11, R-19, ADR-0013) — independent coverage of the `protection.orphaned` / `protection.restored` journalling the unit tier can only prove against a faked `IEventLog`. On real Postgres: an orphan pass appends a **readable** `protection.orphaned` row (log-assigned sequence, `Source == "orphan-guard"`) carrying the count **and** the R-19 `nativeSafetyStopRemainsFloor` flag, round-tripped through a real `ReadAfterAsync`; a re-arm that transitions something appends `protection.restored` — proven across **all three transitioning shapes** (re-arm, retire-on-closed-position, and a reconciled partial close), each asserted on the payload's `rearmed`/`retired`/`stillOrphaned` counts; an **all-unverifiable** pass (the venue unreachable) restores nothing and journals nothing, so the earlier alert stands; **terminal consistency** — after the pass, a consumer reacting to the event reads a **committed** `StopPlan`, read back from a brand-new scope so the assertion is what an independent consumer would itself see over Postgres, not what the acting scope remembers doing (this pair deliberately does **not** prove the append/commit *ordering* — that observation lives in `OrphanGuardEventOrderingIntegrationTests.cs` below, gh#715); and the **best-effort contract** is proven against a **real** Postgres append failure — a temporary `CHECK (false) NOT VALID` constraint added to the live `"Events"` table (not a mock), which fails every append for the test's duration — showing the orphaning / re-arm stays **committed** and the high-severity log still stands. Reuses `OrphanTestPostgresFactory` (gh#192) unmodified. Green on container-backed Postgres in the pre-merge CI job, and the guards were exercised against a deliberately reversed save/journal order — which reddened the two append-failure cases and, notably, **not** the two consistency cases, which is why the ordering claim above is scoped as it is | **Active** (Passing) |
| **`OrphanGuardEventOrderingIntegrationTests.cs`** | **The orphan-guard commit-then-journal ORDERING guard** (gh#715, of gh#707/gh#704; R-11, R-19, ADR-0013) — the observation its sibling suite above deliberately cannot make. `OrphanGuardOrderingPostgresFactory` wraps the composition root's real `IEventLog` (`TimescaleEventLog`, kept alive under its own concrete type — the gh#382 "wrap, don't replace" discipline `InstrumentationSafetyPostgresFactory` established, with its own displaced-registration fidelity guard here too) in `ObservingEventLog`: when `OrphanGuardService` appends `protection.orphaned` / `protection.restored`, the decorator reads the watched `StopPlan`'s **committed** staging from a **fresh DI scope**, *during* the pass, before delegating to the real append — the `ObservingGuard` shape (gh#629 / PR #667). Two guards: `Orphan_ShouldAppendTheOrphanedEvent_OnlyAfterTheStopPlanHasCommitted` and `Rearm_ShouldAppendTheRestoredEvent_OnlyAfterTheStopPlanHasCommitted` each assert the staging observed at the instant of append is already the **post-transition** value. Prove-red'd locally (Testcontainers blocked in the authoring sandbox — see the PR): reversing `OrphanGuardService`'s save/journal order in `OrphanAsync` reddened only the Orphan guard, and in `RearmAsync` reddened only the Rearm guard — neither touched the other, and the existing unit tier (`OrphanGuardServiceTests`, a faked `IEventLog`) stayed green throughout, since only a real, shared-`DbContext` append can expose this ordering at all. **Execution against real Postgres is pending CI** — the local prove-red ran against a temporary EF-InMemory harness reproducing the same production types (`OrphanGuardService`, `TimescaleEventLog`), not this suite's own `PostgresApiFactory` host, for the same Docker-registry-blocked reason PR #710 hit | **compiles clean; execution pending CI** |
| **`StateRehydrationIntegrationTests.cs`** | **Decision-state rehydration across a restart** (R-20, R-12, R-13, ADR-0013) — the Postgres container outlives the host, so a second host via `CreateHost` re-runs `StartupTasks` against the same DB (that *is* the restart). A staged order stays `Staged` (neither auto-sent nor cancelled), a pending conditional stays `Pending`, an expired suggestion is **never resurrected as takeable** (by construction — no suggestion→order path), the **kill-switch lock survives** the restart (regression on gh#189), **R-20 default-deny holds** through it (another operator's staged order → `404`), an orphaned stop is **never silently promoted** on a dead connection (Hidden-only), and an **inconsistent surface fails safe** — the kill switch engages `HaltOnly`, loud, not quietly repaired. Always-on hosts suppressed via `RehydrationTestPostgresFactory` (gh#223) | **Active** (Passing) |
| **`SuggestionLifecycleRestartIntegrationTests.cs`** | **Suggestion lifecycle across a restart** (gh#552 ⇒ epic gh#17; R-4/R-8/R-12/R-20, ADR-0013) — the restart half of the gh#552 suite, over the same `RehydrationTestPostgresFactory` restart (its container outlives the host, so `CreateHost` re-runs `StartupTasks` — migrate → the gh#545 **recovery-expire** pass → decision-state rehydrate — against the same DB). A suggestion whose validity window elapsed **during downtime** comes back **`ExpiredVoid`, not `Active`** (the recovery-expire voids it *before* the surface is counted; the active count is `0` and nothing is auto-taken); a still-valid one comes back **`Active` but inert** (no order created); **`Stale` and `ExpiredVoid` rows are never deleted** by rehydration (the journal keeps them, and a `Stale` row inside its window is never chased back to `Active`); and **cross-operator isolation holds through the restart** — owner A's suggestion **and its disposition** keep their owner, and a second operator gets a `404` on the by-id read (ADR-0013's named QA item). Prove-red on each, against real Postgres: disabling the `StartupTasks` recovery-expire reddens the lapsed case (comes back `Active`); an over-aggressive expire (ignoring `ExpiresAt`) reddens the survivor and the Stale-persists cases; `IgnoreQueryFilters` on the by-id read reddens the isolation case (`404 → 200`). The drift → `Stale` (and *stays* `Stale`) transition and the concurrent expire-vs-drift writer race are the sibling `SuggestionDrift*` suite's (above), cited not duplicated. Container Postgres — 4/4 green | **Active** (Passing) |
| **`SettlementReconcileIntegrationTests.cs`** | **EOD settlement reconcile** (R-13, R-19, R-12, R-20, ADR-0013) via `PositionReconciliationService.ReconcileAsync` driven at a chosen `now` over a user-scoped `DbContext`: the reconcile **yields to venue truth** (returns the adversarial stub's position verbatim — there is no local store), tags a position carried through the maintenance window on the **`Settlement`** basis and **never `Live`** (a re-mark is not a P&L move), returns to **`Live`** once the window passes, **derives each window from the instrument's session** (GC/CL settle before ES/NQ — a hard-coded window can't produce Settlement at 12:30 *and* 14:45 with a Live gap between) using the **DST-aware** `MarketClock` (one UTC instant → Live in July, Settlement in January), **declares `Unknown`** with no positions when the venue is unreachable, keeps auto-flatten's **verify-flat** intact when the window overlaps the deadline, and preserves **R-20** (another operator cannot reconcile the owner's account). Adds a `MakeVenueUnreachable` stub hook; hosts suppressed via `SettlementTestPostgresFactory` (gh#194) | **Active** (Passing) |

The **venue-truth resting-orders read** was proposed here under gh#381 and **shipped** under gh#534; its row lives with the other venue-truth reads, in [Observability, the venue-truth read and the send-as-is path](#observability-the-venue-truth-read-and-the-send-as-is-path). This line is a pointer, not a row — the suite is rostered once, there.


### The working-order family and the gate's last mile

| Integration Test Suite | Covered Endpoints & Features | Current Status |
| :--- | :--- | :--- |
| **`OcoCancelOnExitIntegrationTests.cs`** | **OCO-cancel-on-exit** (R-11, ADR-0007) via `OcoExitService.ProcessExitAsync` fed the account-event seam's flat `PositionEvent` (gh#219): a flat exit **retires** the hidden / native / orphaned stop plan (→ `Retired`) and **audits** it (`AuditAction.PositionExit`), **cancels the dangling native legs** at the venue while **sparing the operator's journaled entry**; the dangerous direction is guarded hardest — a **partial fill never retires protection**, a stale flat event **never retires while the venue still shows the position open** (re-open race), and **another operator's plan is untouched** (R-20 per-owner context); replays are a **no-op** and a venue cancel rejected as already-gone is **swallowed without a retry-storm**. Extends the stub with native working-legs + recorded/throwing cancels; hosts suppressed via `OcoExitTestPostgresFactory` (gh#184) | **Active** (Passing) |
| **`AccountEventIngestionIntegrationTests.cs`** | **Account-event ingestion** (R-17, R-11, ADR-0001) via `AccountEventIngestionService.ProcessAsync`: a fill writes a `Fill` row and advances its order to `PartiallyFilled` / (cumulatively) `Filled`; a **voided** fill is ignored; a **replayed** fill is deduped by the `{OrderId, VenueFillKey}` unique index (never double-counted); an order-state event moves a working order to `Cancelled` / `Rejected` (retiring a never-filled entry's stop plan + `AuditRecord`); the journaled `Fill` **carries its owner** (R-20 per-owner write, background plumbing); and a stray event for an unknown order / foreign account is ignored, never a phantom fill. Driven directly at each `AccountEvent`; hosts suppressed via `OcoExitTestPostgresFactory` (gh#281) | **Active** (Passing) |
| **`CancelWorkingOrderIntegrationTests.cs`** | **Cancel-working-order** (R-11, ADR-0007) via `DELETE /orders/{id}`: a working order is **cancelled at the venue** (recorded on the stub), marked `Cancelled`, its stop plan **retired**, and the cancellation **audited** (`AuditAction.OrderCancelled`); a **terminal** order is refused `409` (not a silent no-op); **R-20** holds (another operator → `404`, order untouched); and a **venue rejection** returns `409` **without forcing a terminal status** (venue truth reconciles it) — the plan stays as-is. **gh#297** folds in the **retire**, the **audit**, and a **record-integrity-on-benign-reject** case (coherent status↔staging + **no retry-storm**, exactly one venue cancel) as their own named guards. HTTP-driven with directly-seeded working orders; hosts suppressed via `OcoExitTestPostgresFactory` (gh#282, gh#297) | **Active** (Passing) |
| **`StopPromotionConcurrencyIntegrationTests.cs`** | **Position-aware stop promotion + self-cancel on a lost race** (R-11, R-12, ADR-0007) via `StopPromotionService.PromoteForQuoteAsync` driven directly against **real Postgres**: a due hidden stop is **not** promoted when the venue reports the position **flat or reversed** (fail-closed, plan stays Hidden, nothing transmitted); when a **concurrent retire commits mid-place** (on a second DI scope/connection — a genuine lost-update the InMemory provider cannot reproduce) the promotion **self-cancels the leg it just placed** and never clobbers `Retired` back to `Native`; if that self-cancel is itself rejected, a high-severity **`synthetic_risk`** log fires and the pass does **not** throw; and in a multi-stop pass one plan's lost race is **isolated** (the other still promotes — per-record reconcile). Adds a `PlaceOrderAsync` side-effect hook + placed-id recording + global cancel-reject to the stub; hosts suppressed via `OcoExitTestPostgresFactory` (gh#274) | **Active** (Passing) |
| **`ModifyWorkingOrderIntegrationTests.cs`** | **Modify-working-order** (R-11, R-12, R-16, R-20, ADR-0007) via `PATCH /orders/{id}/price` over HTTP — a reprice is a **re-decision, not a rubber stamp**: the repriced entry persists and the venue leg is modified **in place** (size held invariant, transmitted `null`) **only after** the risk gate re-passes at the unchanged size, witnessed by the adversarial stub's recorded `ModifyOrderAsync`; a modify that **violates the re-read risk profile is refused** (`422 RefusedByRisk`, order untouched, venue never touched — R-12); an entry that **crosses its stops is refused** on the effective geometry **before** the venue, so `CK_StopPlans` can never trip post-reprice (protection stays consistent); a reprice is **audited** (`AuditAction.OrderModified`, before→after entry); a **non-working** order is refused (`409`, no venue contact) and a **missing reference price** is rejected (`400`, R-16); and **R-20 holds** (another operator gets `404`, nothing modified). Adds a `ModifyOrderAsync` override + recorded/throwing modify hooks to the stub; hosts suppressed via `OcoExitTestPostgresFactory` (gh#283). **gh#387** extends the same file across the rest of the modify family: the **working-stop re-stage** (gh#267) is proven a **local** write — a hidden stop re-stages `Order.WorkingStopPrice` + the plan's `ActualStopPrice` with the **venue never contacted**, a crossing move is refused `422` before commit, a coincident-stop order **gains** a Hidden plan, an **Orphaned** plan is refused `409`, and a **widen under `SizingBasis.ActualStop`** re-gates the per-trade layer; the **combined entry + working-stop move** (gh#278) commits both atomically with the full chain re-validated **before** the venue and a venue rejection leaving **both** the order and the plan untouched; and the **resize** (gh#292) **transmits the new size** to the gateway, honours the **gate-approved** quantity on a downsize (`Resized`, echoed not silent), refuses `422` on a non-positive size, `400` on a same-size no-op, `409` on a non-`Working` order, and is refused (`RefusedByKillSwitch`) while the kill switch is engaged — seven core guards (kill-switch, size-transmission, working-only, both ordering guards, the ActualStop widen re-gate, and the combined-move gate re-validation of the new working stop) **prove-red'd** by disabling each in production and observing exactly those tests fail. An adversarial multi-agent review hardened it — two guards that passed regardless of their named defect were re-calibrated (a crossing geometry the risk gate caught independently → a geometry only the ordering backstop catches; a widen refusal driven by the constant safety-stop layer → the working-stop `PerTradeRisk` layer as the sole discriminator) and coverage added (genuine downsize, gate-block-on-resize, combined-move gate refusal, resize/re-stage audit, gate-decision persistence, Native/Retired plan refusals) — 31 tests total | **Active** (Passing) |
| **`TakeProfitBracketIntegrationTests.cs`** | **Take-profit bracket at entry** (gh#170, R-11, ADR-0007) — the optional third leg of the native exchange OCO, over `POST /accounts/{id}/orders`: a **wrong-side** target (below entry for a long, above for a short) is refused **before the gate** (`409 RefusedByInvalidTarget`, no order journaled and **no `GateDecisionRecord`** — distinguishing it from a risk block), never flipped; a **winning-side** target is accepted and **transmitted** as the captured `OrderRequest.ProfitTarget` (read from the adversarial stub, never a fabricated echo), riding the same placed order as the always-native **protective stop** on the opposite side of entry (the two-leg OCO); and the leg is **optional** (no target still places). The wrong-side refusal + the profit-leg transmission are **prove-red'd** (disable `WrongSideTarget`; null the transmitted target). Hosts suppressed via `OcoExitTestPostgresFactory` (gh#170) | **Active** (Passing) |
| **`ConsistencyTargetIntegrationTests.cs`** | **The prop-firm consistency target end to end** (gh#394 ⇒ gh#380, R-5, ADR-0007) — the rule that caps how much of an evaluation's profit may come from one day. Guards the **last mile** the unit suite cannot reach (a target persisted, validated and returned by the API and still never reaching the gate — gh#380's actual defect), **both postures** on the same breach (`Block` refuses with `RiskLayer.ConsistencyTarget` and approves nothing; `Advisory` trades through), the **migration default** (`SET … = DEFAULT` proves the column's own default is `Advisory`, so a schema change cannot start refusing orders), **Central-time day bucketing** (two trades straddling UTC midnight are one CME session; UTC bucketing reads 40% where Central reads 67%), and the **degenerate windows** (loss-making, empty, open-position-only). Two guards began as **pinned defects (gh#407)** — the gate built a `GateAdvisory` and nothing carried it across the API boundary, so the *default* posture produced no observable output at all — and are now that fix's **regression guards**, asserting the advisory is surfaced (breached when it is, raised-but-not-breached while the operator can still act) **and journalled**. All ten **proven red** by seven independent breaks, one guard per break | **Active** (Passing — 10 guards) |
| **`RealizedReaderModeFilterIntegrationTests.cs`** | **The R-14 mode filter on the two realized-P&L readers** (gh#756, of gh#746 — fix landed in PR #755, merged 2026-08-11; R-14, R-5, R-4, R-20, ADR-0007) — `DailyRealizedReader` / `ConsistencyWindowReader` must count only trades taken under the account's **current** mode, so a practice result never feeds a live governor, headroom or payout window. Every case is an account with trades on **both sides of a Practice → Live change**, driven entirely over HTTP so the suite compiles and runs identically before and after the fix (which changes both readers' signatures). Proves: the **send-path consistency gate** — a practice day can neither **disqualify** a compliant live payout (live-only 50% vs blended 67% against a 60% target) nor **rescue** a real live breach (live-only 90% vs blended 30% against 50%), both against a host booted as `Production` because a `Live` account is refused everywhere else by the R-14 environment gate; the **headroom read** — a practice loss must not eat live headroom, and (the unsafe direction) a practice **profit** must not net against a live loss and report a full governor on a day that is 900 down; **R-20** — the new `Account.Mode` read is default-deny scoped like the `Trade` read, isolated by the one shape where the profile read cannot answer first (a profile visible to A whose account is B's); and the **two absences** — an `undeclared` account counts nothing (**by construction**: `CK_Trades_Mode_NotUndeclared` refuses an undeclared trade outright, asserted), and the fix's defensive `mode is null` branch is **unreachable on a real FK** (`RiskProfiles → Accounts` cascades, so the profile cannot outlive its account; headroom still 404s). **Was expected RED on `develop` until #755 merged** (it did, 2026-08-11; gh#746 and gh#756 are closed, and these are now that fix's regression guards) — that red *was* the prove-the-red gh#756 asks for, reached by base rather than by local revert; the two structural guards (check constraint, FK) are green on both sides. **Witnessed on CI**: 6 failed / 549 passed / 28 skipped across the whole integration project — the six mode-filter cases and nothing else, the two send-path ones on exactly the opposite verdicts they were built to separate. That run also caught a defect in the suite itself (a Central-offset `DateTimeOffset` Npgsql refuses for `timestamptz`, which reddened the three closed-today cases on the *write* and made the check-constraint case pass on the **wrong exception**): the helper now normalises to UTC and that case asserts `CK_Trades_Mode_NotUndeclared` **by name** | **Active** (Passing — gh#746's regression guards since PR #755 merged 2026-08-11) |

### Reconcile and the fill backfill — recovering a stranded send

| Integration Test Suite | Covered Endpoints & Features | Current Status |
| :--- | :--- | :--- |
| **`Api/ReconcileStrandedTakeIntegrationTests.cs`** | `POST /orders/{id}/reconcile` — gh#736, the fifth case of gh#724 (of gh#619; R-11 / R-12 / R-16, ADR-0007, ADR-0013). Written from the issue, not the implementation. The state under recovery is the dangerous one: a take that stranded mid-send, **possibly live at the venue**, where guessing wrong in either direction either strands protection or stacks a second live order | **Active** (Passing) |
| **`Api/ReconcileAdoptStillOpenIntegrationTests.cs`** | The **adopt-still-open arm** of the same endpoint — gh#769, the container-tier proof of gh#723 (of gh#619, part 3; R-8 / R-9 / R-11 / R-16 / R-20, ADR-0007, ADR-0013). Written from the issue and ADR-0013, **not** from the implementation (QA contract §Role) | **Active** (Passing) |
| **`Api/ReconcileStrandSweepIntegrationTests.cs`** | The **runtime strand-detection sweep** (gh#923, of gh#722; R-11, R-13, R-20; ADR-0013's 2026-08-15 update, ADR-0007) against real PostgreSQL. The named failure mode: an order left `Taking` or a conditional left `Firing` mid-send — possibly live at the venue with no journal behind it — **forms while the process is up and sits there unseen**, because the only two detectors were the operator's own eye and the restart rehydrator. The sweep is deliberately **propose-and-confirm** — it detects and alerts, and never acts (the operator ratified that autonomy boundary rather than letting it auto-resolve) | **Active** (Passing) |
| **`Api/BackfillEntryFillPartialCollisionIntegrationTests.cs`** | The **per-leg fill-key collision guard** inside `BackfillEntryFillsAsync` (`Api/Orders/OrderEndpoints.cs`) — gh#793, spun off the #791 review of gh#770 (R-5 / R-8 / R-9, ADR-0001, ADR-0013). Written from the issue body, not the implementation (QA contract §Role). The `{OrderId, VenueFillKey}` uniqueness that dedups a replayed fill is also what a backfill can collide with, and a collision staged into a caller's `DbContext` surfaces on **their** save — so the guard has to pre-check rather than rely on the constraint | **Active** (Passing) |
| **`Api/AdoptedRoundTripTradeIntegrationTests.cs`** | The **end-to-end #770 chain** — gh#960, the QA card for gh#770 (R-5 / R-8 / R-9, ADR-0001, ADR-0013). Written from the issue, not either implementation (QA contract §Role). Where the two suites above prove their own pieces in isolation, this one drives all of them together against real Postgres: a Filled take stranded with no venue key and its entry fills dropped, adopted through the **still-open** arm of `POST /orders/{id}/reconcile` (gh#723), then driven flat through the real `AccountEventIngestionService` / `TradeJournalService` pipeline — asserting exactly one `Trade` composes with the correct signed `RealizedPnL`, keyed on the adopt's own backfilled entry `Fill`. A second case redelivers that same entry fill through the account-event stream and asserts the `{OrderId, VenueFillKey}` unique index turns it into an idempotent no-op — no second `Fill`, no second `Trade` | **Active** (Passing) |

### The trade journal — FIFO pairing, write ordering and the flat-before-fill window

The `Trade` rows these suites guard are what the R-4 and R-5 governors read, so a lost or double-counted round trip is not a reporting defect — it mis-states consumed headroom to the gate.

| Integration Test Suite | Covered Endpoints & Features | Current Status |
| :--- | :--- | :--- |
| **`Api/TradeFifoPairingIntegrationTests.cs`** | gh#759's **FIFO per-leg trade pairing** and the composite natural key `(ClosingFillId, OpeningFillId)` it moved to (gh#799; ADR-0022; R-8, R-9, and the R-4 / R-5 governors the rows feed) against real Postgres. Authored from gh#799's acceptance list and gh#759's statement of the hazard rather than from the writer — the independence is the point of the tier. The hazard: a FIFO-pairing idempotency key **re-derives** on a late out-of-order fill, so exact-key dedup double-counts | **Active** (Passing) |
| **`Api/TradeJournalConcurrencyIntegrationTests.cs`** | The **two ordering hazards in the production `Trade` writer** (gh#751, of gh#731; R-8 / R-9) — found by the gh#734 adversarial review and fixed at `206dde6`, so these are that fix's regression guards. Real Postgres, because both hazards are interleavings an in-memory provider serializes away | **Active** (Passing) |
| **`Api/TradeJournalWriteFaultIntegrationTests.cs`** | The **write-fault narrowing** gh#747 puts on `TradeJournalService.ProcessFlatAsync` (gh#779; R-11; ADR-0013) — authored from the spec, not the implementation, which was still unmerged (PR #781) when the suite was written | **Active** (Passing) |
| **`Api/FlatBeforeFillAdverseOrderIntegrationTests.cs`** | The **flat-before-fill window** (gh#911, the QA card for gh#748; real Postgres): a `PositionEvent`(flat) delivered to the account-event stream **before** its closing `FillEvent` must still journal exactly one `Trade` once the fill lands, through a **defer-then-retry** path — not be lost. Authored from gh#911's and gh#748's own text, not gh#748's implementation. Losing it means the round trip never reaches the journal, and the R-4 / R-5 governors **under-count a loss** | **Active** (Passing) |
| **`Data/TradeOpeningFillKeyMigrationIntegrationTests.cs`** | The **migration half** of gh#759's composite trade key (gh#799 bullets 4 and 5; ADR-0022): what `AddTradeOpeningFillKey` does to a `Trades` table that **already holds rows**, and what its `Down` does once the split it enables actually exists. Like the gh#486 backfill suite, it must stand *before* the migration to witness it, so it seeds through **raw SQL naming the pre-change columns** — seeding current-model entities into a pre-change schema is exactly the `42703 column … does not exist` failure this suite exists to catch | **Active** (Passing) |

### The journal outcome writer — R-9 resolution and R-15 removal (gh#909, gh#832)

QA pair for gh#909 (the writer + removal/read surface) and gh#832 (the `Outcome` entity + R-15 flags): gh#940 and gh#908 batched into one PR because both sit on the same `Outcome` tree and would otherwise collide. Written from the two issues' own text, never from `OutcomeJournalService` / `OutcomeEndpoints` / the DbContext model.

| Integration Test Suite | Covered Endpoints & Features | Current Status |
| :--- | :--- | :--- |
| **`Api/OutcomeWriterIntegrationTests.cs`** | **`OutcomeJournalService.ComposeClosedTradeOutcomesAsync`** (gh#940, of gh#909; R-9) against real Postgres — the mechanism only a real unique index can witness. The **idempotency race**: composing the same closed trade twice must never mint two `Outcome` rows for it, proven **deterministically** rather than via `Task.WhenAll` — the suite's own prove-red pass caught the racing version staying green with `IX_Outcomes_TradeId` dropped entirely, because a container-local round trip resolves the pair sequentially through the anti-join alone, with or without the index behind it. The case instead lets the first compose commit normally, then attempts a second insert of the identical row shape a racing pass would build, and asserts it is rejected by name via `IX_Outcomes_TradeId`. **Refuse-don't-guess**: a closed trade with a null `RealizedPnL`, and an open trade, both compose nothing. **Sign mapping over real `numeric`**: positive / negative / exactly-zero `RealizedPnL` → `Win` / `Loss` / `NoFillScratch`, read back through Postgres rather than held only in .NET memory. **Cross-owner sweep**: the background writer has no ambient request user, so each written outcome is stamped from its OWN trade's owner, proven across two operators in one pass. Uses `OutcomeTestPostgresFactory` (every hosted service stripped, including the writer's own five-minute poll), so the only thing ever composing outcomes in these tests is the call each case makes | **Active** (Passing) |
| **`Api/OutcomeRemovalSurfaceIntegrationTests.cs`** | The **`/outcomes` R-20 isolation, report-toggle and hard-delete-audit surface** end to end over real Postgres (gh#940, of gh#909; R-9 / R-15 / R-20). Operator B (created through the real invitation flow, as `MultiTenantIsolationIntegrationTests` does) gets a **404** from every mutating route — soft-delete, restore, both independent toggles, hard-delete — against operator A's outcome, leaving it untouched and auditing nothing; A's list never enumerates B's rows. The **report toggle**: `GET /outcomes` excludes a soft-deleted row by default, `?includeDeleted=true` returns both figures for the same set. **Hard delete + its audit fact**: the row is gone, and the `AuditRecord` it writes (`action = OutcomeHardDeleted`, `placement = None`, `source = null`) reaching the table at all proves it satisfied `CK_AuditRecords_Source_MatchesAction` on real Postgres — an in-memory `IAuditLog` applies no check constraint at all, so the unit tier cannot see this. **R-15 flag independence over HTTP**: soft-delete moves all three flags together; the two toggles move one at a time, each surviving the other's earlier write. Outcomes have no create endpoint, so every fixture row is seeded straight through the `TradingCopilotDbContext`, stamped with the real operator id looked up from `Users` | **Active** (Passing) |
| **`Api/UnfilledSuggestionOutcomeIntegrationTests.cs`** | **`OutcomeJournalService.ComposeUnfilledSuggestionOutcomesAsync`** (gh#956, of gh#939, itself of gh#909; R-9) against real Postgres — the untaken-suggestion mirror of `OutcomeWriterIntegrationTests`' closed-trade sweep. **Composition**: a never-taken `ExpiredVoid` suggestion → `Expired`; a `Passed` disposition → `NoFillScratch`; a **pass wins over the clock's expiry** when a suggestion carries both (proven by construction — flipping the basis pick to favor expiry reddens exactly this case and no other). **Never composed here**: a `Taken`/`Modified` disposition (built through the real `SuggestionDisposition.ForTake` factory, not a hand-rolled row, so the take-snapshot CHECKs stay honest), and a suggestion an **`Order` was armed from** even with no disposition row at all (the gh#939 review's race-proof exclusion). **Idempotency**: the same deterministic TOCTOU-gap proof as the trade sweep, against `IX_Outcomes_SuggestionId` (filtered to `TradeId IS NULL`) — and a **positive** case shows two trade-derived legs sharing one `SuggestionId` (gh#759) never trip that filtered index. **Cross-owner stamping** mirrors the trade sweep. **Cascade FK via account removal**, composed end to end from the top (`Account → Suggestion/Trade → Outcome`) rather than the direct-delete hop `OutcomeDbGuardIntegrationTests` proves — this suite's own prove-red pass caught the trade-derived case vacuous on its first draft: a fixture that also linked a suggestion let the (separately-Cascade) `Suggestion → Outcome` FK mask a broken `Trade → Outcome` FK entirely, so the case now seeds a trade with no suggestion lineage. Uses `OutcomeTestPostgresFactory` | **Active** (Passing) |
| **`Api/OutcomeSuppressionIntegrationTests.cs`** | The **`OutcomeSuppression`** recomposition-suppression tombstone (gh#965, of gh#955; R-9 / R-15 / R-20) — the DB-enforced half only real Postgres can witness. **`CK_OutcomeSuppressions_OneParent`** refuses a two-null and a both-set row, with the one-key anti-vacuity accept case. **Idempotent suppression**: the two unique filtered indexes (`IX_OutcomeSuppressions_TradeId` / `..._SuggestionId`) each reject a second tombstone for the same source. **The delete sticks against the LIVE sweep** — the anti-join an EF-InMemory double cannot exercise: hard-delete a trade-derived and an untaken outcome through the real `DELETE /outcomes/{id}`, then run the real `OutcomeJournalService` sweep and assert it does not recompose either (proven by removing each sweep's `suppressed` anti-join in turn and watching exactly its case redden). **Cascade FK via account removal** for both a trade-keyed and a suggestion-keyed tombstone. **R-20**: the tombstone is owner-stamped from the deleted outcome, and a suppression keyed on one owner's trade never silences the cross-owner sweep for an unrelated owner's still-live one. **Audit-failure survival**: a dedicated host with its own throwaway container swaps in an always-throwing `IAuditLog` to prove the remove + tombstone commit **before** the audit runs and survive its failure — removing the endpoint's `catch` reddens this case with the exact 500 the swallow exists to prevent. Reuses `OutcomeTestPostgresFactory`; the audit-failure case alone stands up its own factory so the throwing double never perturbs the rest of the suite | **Active** (Passing) |
| **`Data/OutcomeDbGuardIntegrationTests.cs`** | The **database's own guards on `Outcome`** (gh#908, of gh#832; R-9 / R-15 / R-20) — `CK_Outcomes_Resolution_NotUnknown` and `CK_Outcomes_ParentPresent`, each named on refusal, with the positive-control anti-vacuity case. The **FK removal behaviors trace the model's shipped configuration, not gh#908's original text**: gh#908 was filed expecting a deleted `Suggestion` to `SetNull` its outcome's `SuggestionId` and leave the row; the gh#832 review (gh#939, operator decision 2026-08-16) changed the FK to `Cascade` instead, so this suite pins THAT — deleting a `Trade` cascades its outcome away, and deleting a `Suggestion` cascades away **every** outcome carrying that `SuggestionId`, including a trade-derived one that still has its `TradeId` (a documented lineage-loss trade-off the DbContext's own comment accepts, since no production path deletes a suggestion directly today). Also guards the **R-20** per-operator query filter over `Outcomes`, and that the three R-15 flags round-trip through EF's private setters (`SoftDelete()` moves all three; the two independent toggles move one at a time) | **Active** (Passing) |

### Observability, the venue-truth read and the send-as-is path

| Integration Test Suite | Covered Endpoints & Features | Current Status |
| :--- | :--- | :--- |
| **`Observability/TelemetryIntegrationTests.cs`** | **Trace continuity across the event log's async gap** (ADR-0002, engineering §7) with the **live** consumers running against real Postgres: a producer's W3C `traceparent` rides the envelope, **survives the `varchar(64)` column byte-identically**, and a genuine `stop-promotion.consume` / `conditional-order.consume` span **links back to the producer's trace** — so `websocket → append → consume` is one trace, not two halves; an **absent** traceparent starts a fresh trace and still consumes; a **malformed** one is ignored and the consumer **keeps consuming afterwards** (a reporting fault must never stall a safety consumer). Captured via a BCL `ActivityListener` on the app's own source — no assertion is satisfied by the test's own input. Adds `TelemetryTestPostgresFactory` (live hosts). **Exemplars are NOT covered — filed as gh#338** (QA contract rule 3) (gh#329) | **Active** (Passing) |
| **`Api/WorkingOrderReadIntegrationTests.cs`** | **The venue-truth resting-orders read** — `GET /accounts/{id}/orders` (gh#534 ⇒ gh#381, R-13, R-20, ADR-0013): the operator's answer to *is protection actually standing at the venue?* Everything it claims is computed in the endpoint projection — `isProtective` exists nowhere in the domain types — so it can only be asserted over HTTP, and the unit tier does not stand behind it: its endpoint tests cover the 404 and then construct a `WorkingOrder` locally and assert `StopPrice is not null` on it, a **tautology that never calls the handler**. Guards: each leg carries **its own size** (two legs at different sizes, neither of them 1 — gh#381 exists because a protective leg sized under the position it guards left the remainder unprotected and invisible); a **limit-only take-profit is not protective**, because it caps the gain and protects nothing about the loss; **declared-unknown is tellable apart from an empty book at the wire** — `orders` is `[]` in *both*, so `markBasis` is the only discriminator, and both legs live in **one** test with a raw-body inequality on top because either half alone would pass against a system that reports every failure as an empty book; and another operator gets **404, not an empty 200**, which would both leak that the id is real and read as an absence of protection. Uses the per-account `MakeAccountUnreadable` (gh#533) rather than `MakeVenueUnreachable`, which throws from the *roster* read and would also break discovery. **No case asserts `markBasis == "Live"`** — the endpoint reads `UtcNow` and the built-in schedules make `Settlement` legitimate for ~3h/day; the assertion is `NotBe("Unknown")` | **Active** (Passing) |
| **`Api/HiddenWorkingStopReadIntegrationTests.cs`** | **The hidden working-stop join on the resting-orders read** (gh#934 ⇒ gh#865, gh#656, gh#267; R-11, ADR-0007) — the four PLATFORM-held fields (`workingStopPrice`, `stopStaging`, `safetyStopPrice`, `entryPrice`) `GET /accounts/{id}/orders` now carries alongside the venue-truth leg, joined from the local `StopPlanRecord` to the journaled `Order.Id`. Written from the gh#934 issue, independently of `WorkingOrderReconciliationEndpoints.ReadAsync` / `MapRestingOrder`; `Api/WorkingOrderReadIntegrationTests.cs` (gh#656) already proves the venue-truth half, so this suite is the independent proof of the platform-held join it does not touch. Guards: a **Hidden** plan surfaces matched to the venue leg **by the journaled Order.Id**, with the venue-truth fields on the same row asserted unaffected (a pure additive join); the four fields are **present or absent as a whole** — a journaled order with no plan carries a non-null `orderId` but null platform fields, and an unjournaled venue-spawned leg carries all five as null; **Native** and **Orphaned** plans still surface their `stopStaging` (only `Hidden` gates the UI's move control, so every kind must be readable); a **terminal** (Filled) order's plan is **never surfaced**, even when the venue still reports the leg resting (a stale post-fill report) — proven by dropping the join's `Status == Working` scope, which reddens exactly that case; and **R-20 at the plan level** — two operators whose app accounts both resolve to the SAME fixed venue account key (the roster hands every discovery the same four keys) each get only their own plan, proven by a break that drops both the account scope and the per-user filter from the journaled-order lookup, which leaks the other operator's `Order.Id` and reddens exactly that case. All four breaks were run inside a Linux `dotnet/sdk:10.0` container (Windows local runs hit intermittent Smart App Control 0x800711C7) with Testcontainers via the mounted host Docker socket | **Active** (Passing) |
| **`Observability/ProtectionCensusIntegrationTests.cs`** | **The protection census** (gh#533 ⇒ gh#370, R-20, ADR-0008) — the gauges behind the **P1 `UnprotectedPosition`** rule, which evaluates `trading_positions_unprotected > 0` and says *the venue reports open exposure with no protective stop behind it*. Until this suite **nothing read those gauges back at any tier**: `ExecutionMetricsTests` has no case for `SetPositionProtection`, so transposing the two `Interlocked.Exchange` targets left every test green while the P1 fired on the wrong series. Guards: an unprotected position is published **on the literal instrument name the rule resolves to** (not a constant compared with itself); a **protected** position is open-but-not-unprotected — and that control is what actually catches a transposition, since the headline case has open == unprotected == 1 and a swap is invisible in it; and **a pass that cannot read one of several accounts publishes nothing at all**, leaving the last complete census standing, because a total that silently omits an unreadable account errs toward *fewer* unprotected positions — quietening the P1 by exactly the thing it should report. Extends the venue stub with a per-account `MakeAccountUnreadable` (`MakeVenueUnreachable` is **inert** here: it is checked only inside `GetAccountsAsync`, which the census never calls). Reuses `ExecutionSliTestPostgresFactory`, whose **concrete**-sink swap keeps the shipped `FailureTolerantExecutionMetrics` decorator in place (gh#382); the gauges are **observable**, so every read is preceded by an explicit `PumpGauges()` | **Active** (Passing) |
| **`Observability/ExecutionSliIntegrationTests.cs`** | **The execution SLIs report the truth** (ADR-0002, engineering §7, PRD §7) against real Postgres — not that the instruments exist, but that what they say is true: the gate counter’s **total reconciles with the `GateDecisionRecord` rows** (asserted on the summed increments, so a double-count cannot hide behind a right-looking measurement count); a deadline that passes with **nothing to flatten still emits** (`nothing-to-do` — silence would be indistinguishable from a flatten that never fired); the **watchdog is never merged with the primary**; the orphan gauge **rises on a drop and returns to zero on re-arm** (asserted against the rows first, so a failure names the right culprit); and **cardinality is bounded** — tag keys are a closed set and no value is an id, swept across many accounts and orders. The kill-switch **restart** case sits in its own class (`ExecutionSliRestartIntegrationTests`) with its own fixture, because a second host publishing the same instruments is real cross-talk. Adds `ExecutionSliTestPostgresFactory` (private meter + `SliCapture`) (gh#330) | **Active** (Passing) |
| **`Observability/InstrumentationSafetyIntegrationTests.cs`** | **Instrumentation must never break trading** (ADR-0002, engineering §9) — faults injected at the seams the system depends on, with the whole suite under a **configured-but-unreachable** OTLP endpoint: a dead collector does **not** fail, delay or un-journal a send; and a **throwing `IExecutionMetrics`** is probed at three call sites — the gate decision, the **order ack** (the dangerous one: a live order with no `Order` row and no stop plan), and the **operator’s emergency halt**. Those three began as **pinned defects (gh#343)** and are now that fix's **regression guards** (flipped in gh#382): each asserts the trade *completes* and is journaled, and the halt both returns `200` **and** really engaged. The fault is injected **inside** the shipped `FailureTolerantExecutionMetrics` rather than in place of it — a fourth case asserts exactly that, because between gh#343 landing and gh#382 the harness replaced the registration outright and all three pins passed **vacuously** over a host the deployment did not have. Adds `InstrumentationSafetyPostgresFactory` + `FaultInjectingExecutionMetrics` (gh#331) | **Active** (Passing — 4 guards; red proven by making the decorator rethrow) |
| **`Api/OpenApiDocumentIntegrationTests.cs`** | The **drift-guard over the generated OpenAPI document** (gh#612 ⇒ gh#604, R-10) — the test that makes *"the spec can never silently drop an endpoint"* true rather than merely intended. Asserted against the document the running host generates, so a route that stops being described is caught where it happens rather than by a consumer discovering the gap later | **Active** (Passing) |
| **`Api/SendAsIsEndpointsIntegrationTests.cs`** | The **send-as-is fast path** (gh#181) + the **default entry action** preference (gh#218) over real Postgres: proves the fast path is a shortcut past the **manual review only**, never the gate — R-5 sizing, R-16 caps, the R-14 mode × environment gate, the always-native protective stop, and the kill switch all bind exactly as on `arm → take`. Adversarial venue stub (reports `Live` for every account). QA for gh#182 | **Active** (Passing) |

### The §9 audit trail — the two safety write sites and its migration

| Integration Test Suite | Covered Endpoints & Features | Current Status |
| :--- | :--- | :--- |
| **`Api/AuditTrailIntegrationTests.cs`** | The **immutable §9 audit trail** gh#765 wired at the two safety-critical write sites — every kill-switch engage/disengage and every auto-flatten — driven **end to end through the real host** against real Postgres (gh#789 ⇒ gh#765; engineering §9, R-11, R-13, ADR-0007, ADR-0013). The unit tier proves each site stamps the right fields against a fake `IAuditLog`; what only the real composition and a real database witness is that the rows are **durable**, that the kill-switch history **survives the re-engage** that overwrites the single mutable `KillSwitchState` row, that a background flatten stamps the **account's** owner with no ambient operator, and that the whole outcome set (`Flat` / `Escalated` / `Missed`) reaches the table intact | **Active** (Passing) |
| **`Data/AuditRecordConstraintIntegrationTests.cs`** | The **`AuditRecord` check constraints and the R-20 owner filter** (gh#789 ⇒ gh#765; engineering §9, ADR-0007) against real Postgres — a fake `IAuditLog` applies **no** check constraint and enforces **no** query filter, so the two things gh#765 added *below the model* had nothing standing behind them. Guards `CK_AuditRecords_Source_NotUnknown` (a persisted `AuditSource` is never the refusable zero, while a genuinely absent source — the stop-plan actions — stays null); `CK_AuditRecords_Source_MatchesAction` (a safety action **must** carry a source and a stop-plan action **must** leave it null — the "record a safety action with an unknowable trigger" defeat an in-memory tier cannot catch); and the **R-20** per-operator filter over `AuditRecords` | **Active** (Passing) |
| **`Data/AuditSourceMigrationIntegrationTests.cs`** | The **migration half** of gh#765 (gh#789 acceptance): what `AddAuditSourceForKillAndFlatten` does to an `AuditRecords` table that already holds an operator's history — the connection-loss and order-lifecycle rows that predate the kill-switch and auto-flatten write sites. The migration is **additive** (a nullable `Source` column plus the two checks), so it must apply over those rows **without loss** and leave their source null; a check added without that allowance would refuse the operator's own history | **Active** (Passing) |

### News → relevance → salience

| Integration Test Suite | Covered Endpoints & Features | Current Status |
| :--- | :--- | :--- |
| **`MarketData/NewsIngestionIntegrationTests.cs`** | **News-ingestion engine** (gh#358, D2·1, R-2/R-9, ADR-0014) — the deduped multi-source **store of record** over real Postgres with two adversarial `INewsSource` feeds; assertions on the persisted `NewsRecord` rows (canonical-URL dedup, both provenances kept), never on engine internals, and the suite never calls production `NewsDedupKey` to derive an expected value. The R-2 **fuzzy fallback** (title similarity + time window + ticker overlap, `NewsFuzzyDedup`) is engine-side and **unit-tested** (gh#764); its container-tier / live-feed proof is QA's (gh#360 / gh#464) and is **not yet in this suite** — this line no longer claims it. QA for gh#360 | **Active** (Passing) |
| **`MarketData/LiveNewsProviderIntegrationTests.cs`** | **The live-provider proof of the ingestion engine above** (gh#1122, the "no staging required" half of gh#464; parent gh#383; R-2/R-17) — the real Finnhub + Tiingo REST APIs reached through **production's own conditional registration** (the suite hands `Program.cs` the provider keys via config rather than re-registering `INewsSource` itself, so the construction path under test is the shipped one), over a throwaway Postgres container (no deployed app). `LiveNewsProviderFactAttribute`-gated on both `Finnhub__ApiKey` / `Tiingo__ApiKey` (probed under both the app's and the repository secrets' casing — `TestHost/LiveProvider/LiveProviderConfig.cs`); skips honestly pre-merge, runs for real only via `live-provider-gates.yml`'s `workflow_dispatch`, the only place those keys are forwarded. **What the first live run found, filed rather than fixed (QA contract §3):** Finnhub's real feed lands and is asserted on its mapped shape (URL/title/timestamp); distinct live URLs each keep their own row (no over-collapse); a Tiingo outage (real 401/403, not a stubbed throw) does not sink Finnhub's news; credentials resolve from config only, proven by a **keyless** host registering zero `INewsSource` (this one case is deliberately ungated — no live credential needed — so it also runs pre-merge); and free-tier data quality is reported via `FreeTierFeeds_ShouldReportTheirDataQuality`'s diagnostic dump. **`SameStory_ShouldCollapseToOneRecord_AcrossLiveProviders` is `Skip`-blocked, not shipped as a false pass**: Tiingo's plan excludes the News API (gh#1125 — a real 403, valid token), so R-2's cross-source collapse cannot be witnessed on any environment until that entitlement gap closes; `Tiingo_ShouldStillBeRefusedByItsPlan_PinningTheGapUntilGh1125` pins the observed refusal so the gap stays visible in CI and reddens the moment it is bought, which is the cue to unblock the collapse case. Two further defects filed: gh#1123 (the shipped 60-minute lookback admits zero of Finnhub's real articles) and gh#1124 (Finnhub's general category tags no tickers, starving gh#359 relevance). QA for gh#1122 | **Active** (Passing/skipped per the findings above; the cross-source case is a declared block on gh#1125) |
| **`Relevance/RelevanceRoutingIntegrationTests.cs`** | **News-relevance routing** (gh#361, D2·2, R-2/R-6, ADR-0014) — the deterministic ticker + text → matched-instruments/topics mapping against the deployment's global relevance config (gh#359), including the config-edit → re-resolution loop (gh#418) and the `CK_NewsTopics_Scope_NotUnknown` / `IX_NewsTopics_Name` DB guards proven **by name** against real Postgres. QA for gh#361 | **Active** (Passing) |
| **`Relevance/SemanticTopicMatchIntegrationTests.cs`** | The **end-to-end semantic-topic-match half of gh#877** (of gh#763, paired with the merged gh#854): `NewsRelevanceService.ResolveAsync` materializes a topic onto `MatchedTopics` by cosine proximity alone — a topic embedded near a news item whose keywords never appear in its text — composing with the pre-existing keyword path in the same pass (neither crowds out the other), and a semantically-matched **instrument-scoped** topic still attaches its `Instrument` to `MatchedInstruments`. Vectors are seeded directly as basis directions (identical vs. orthogonal), so the expected cosine similarity is known by construction rather than computed from a real embed pass. The topic-embedding write/read half lives in `Ai/NewsEmbeddingTopicMatchIntegrationTests.cs` above | **Active** (Passing) |
| **`Signals/NewsSalienceIntegrationTests.cs`** | **Importance-starring salience — Part A** (gh#362, gh#27, ADR-0014) over `/api/news` + `/api/news/feedback`: a star raises the salience of **similar future** news, a mute is its down-weight inverse, un-starring reverts, and a **salience floor** keeps a muted item visible (no filter bubble); the `CK_SoftSignalFeedback_Kind_NotUnknown` / `UX_SoftSignalFeedback_Importance` DB guards proven by name (renamed by gh#762 from its pre-rename name; gh#979 caught this row still naming that one). **Extended by gh#971** for the gh#762 **direction** axis (👍/👎): the `UX_SoftSignalFeedback_Direction` filtered unique index proven by name (the duplicate-refusal mirror of the importance guard); the two axes' independence (a Star **and** a ThumbsUp on the same item are both storable); re-rating/clearing one axis through the write endpoint leaves the other untouched (row counts + values asserted on real Postgres); and — the property this coverage exists to protect — **direction is salience-inert**, proven by diffing the full ranked `/api/news` feed with and without direction ratings in play. QA for gh#362 + gh#971 | **Active** (Passing) |
| **`Signals/SalienceIsNotARiskControlIntegrationTests.cs`** | **Salience is never a risk control — Part B** (gh#362, gh#27, ADR-0014) — the **behavioural** half of a two-tier guard (the unit test asserts it structurally). Drives the real order-submit → gate path with the adversarial venue stub and asserts that a maximally-starred or maximally-muted operator profile leaves the gate's decision **byte-identical** to the un-rated baseline — closing the leak a reflection check cannot see (a gate call-site scaling `RequestedQuantity` by a salience multiplier). **Extended by gh#971** with the identical Theories for the gh#762 **direction** axis (maximally-thumbs-up / maximally-thumbs-down) — direction is structurally unreachable from enforcement too (ADR-0007), proven via the same kind-agnostic helper, so a leak feeding ANY `SoftSignalFeedback` into the gate regardless of kind would redden all four Theories together, not just two. QA for gh#362 + gh#971 | **Active** (Passing) |

**gh#464's live-feed proof landed in two halves, and this row is the first.** `FINNHUB__APIKEY` / `TIINGO__APIKEY`
cleared as repository secrets on 2026-09-05, which unblocked the half needing only real credentials and a store —
gh#1122, the row above, covers exactly that, and its first live run is what surfaced gh#1123/#1124/#1125. gh#464
itself stays open for the deployment-only residue: Railway's `soothing-illumination` project still has only a
`production` environment (one `Postgres` service, offline) — no `dev`/`staging` deploy for a news poller to run
against as part of the deployed app, which is a platform-provisioning gap this suite cannot close.

### The news-embedding tree — semantic retrieval and its GC

Every suite here is **relational-only by construction**: `TradingCopilotDbContext` `Ignore()`s `EmbeddingRecord` unless `Database.IsNpgsql()` (gh#109, ADR-0001), so the pgvector reads are *reachable but untestable* at the unit tier and exist only against real Postgres + pgvector. The read-only suites share `EmbeddingReadTestPostgresFactory`, which strips every always-on `IHostedService` so the real GC host's wall-clock loop cannot race an explicit call; the suites that also need to drive a real embed pass share `EmbeddingProviderDoubleTestPostgresFactory` instead, which does the same host stripping and additionally doubles `IEmbeddingProvider` with `AdversarialEmbeddingProvider` (Cohere cannot exist pre-merge — no key, no egress).

| Integration Test Suite | Covered Endpoints & Features | Current Status |
| :--- | :--- | :--- |
| **`Ai/NewsEmbeddingPgvectorReadIntegrationTests.cs`** | The **pgvector similarity read** (gh#855, of gh#763, epic gh#14 [D2]) that `NewsEmbeddingServiceTests` proves is reachable but untestable in memory — the `CosineDistance` nearest-N query, the HNSW cosine index built by the `AddEmbeddingStore` migration, and the composite-key identity behind `OwnerId` | **Active** (Passing) |
| **`Ai/NewsEmbeddingRecallIntegrationTests.cs`** | The **recall hazard** the gh#852 adversarial review flagged in `PgVectorNewsSimilarity` (gh#861): `.Where(OwnerKind == SoftSignal)` filters the HNSW **approximate** scan, and pgvector applies that filter *after* the scan rather than as part of it (the access method has no vocabulary for a non-vector predicate). On the polymorphic `Embeddings` table the query can **under-return** — fewer than `n` SoftSignal neighbours when `n` or more exist — because the candidate window fills with other owner kinds first. **Its noise crowd names kinds with no partial vector index of their own** (gh#1110): gh#1065 gave `Suggestion` `IX_Embeddings_Vector_Cosine_Suggestion`, so it was swapped for `Topic`. **That is cost and accuracy, not a hole in the guard** — the red path here is served by the *table-wide* `IX_Embeddings_Vector_Cosine`, which holds every row of every kind, so an indexed noise row fills the candidate window exactly as an unindexed one does; measured in gh#1112 by substituting an indexed kind into the sibling suite's crowd, where the read still starved to 0 of 5. What the swap buys is a bulk seed that stops paying incremental HNSW maintenance for 3,750 rows it never queries, and a `_noiseKinds` list that still means what its remarks say | **Active** (Passing) |
| **`Ai/NewsEmbeddingModelScopedReadIntegrationTests.cs`** | The by-owner / nearest reads **filtering by the current model** (gh#888, paired with gh#881, of gh#763), plus the **model-transition self-heal** a re-embed under a new model is supposed to provide — the analogue of #855 / #877. A read that ignored the model would silently mix vectors from two embedding spaces, where distance means nothing | **Active** (Passing) |
| **`Ai/NewsEmbeddingTopicMatchIntegrationTests.cs`** | The **topic-embedding half of semantic topic match** (gh#877, of gh#763, paired with the merged gh#854): `NewsEmbeddingService.EmbedPendingAsync`'s topic pass — composed content (`name + "\n\n" + join(' ', keywords)`), `OwnerKind = Topic`/`Model`/`Dimensions`/`ContentHash` populated, idempotent-skip with no second paid call, re-embed on a keyword or name change — and `GetTopicVectorsAsync` / `GetVectorsAsync`'s cross-kind isolation over the shared `ReadVectorsAsync` helper. The end-to-end semantic-match-onto-`MatchedTopics` half lives in `Relevance/SemanticTopicMatchIntegrationTests.cs` below | **Active** (Passing) |
| **`Ai/EmbeddingOrphanSweepIntegrationTests.cs`** | The **orphaned-owner embedding GC** (gh#902, paired with QA card gh#914): a renamed `NewsTopic` or a deleted `NewsRecord` leaves an `EmbeddingRecord` behind with no producer, and `EmbeddingOrphanGcHost.SweepAsync`'s set-based anti-join `DELETE` is what reclaims it — a relational-only delete, exercisable only against real Postgres. **Aligned by gh#1065:** `Suggestion` gained a producer and moved onto the allow-list, so it is no longer an example of a producer-less kind and left the one case that used it as one (`Rule` / `MarketSnapshot` still carry that claim). The two **new** producer-backed branches — `Suggestion` vs `Suggestions.Id`, `JournalEntry` vs `Trades.Id`, in both directions — are **gh#1096**'s | **Active** (Passing) |
| **`Ai/EmbeddingStaleModelBackstopIntegrationTests.cs`** | The **crash-leaked stale-model backstop** (gh#920, paired with dev card gh#915, of gh#902): `IEmbeddingOrphanStore.DeleteStaleModelDuplicatesAsync`'s self-`EXISTS` `DELETE` — a `Model != current` row is swept only when the SAME owner also has a current-model row (the sibling proves legitimacy); an owner's sole vector (whatever its model) is never touched; no over-reach across several owners and both owner kinds in one pass; and the backstop does not run with no current model configured | **Active** (Passing) |
| **`Ai/CrossKindEmbeddingSweepIntegrationTests.cs`** | The embedding GC over the **two owner kinds gh#1065 added** (gh#1096, the paired QA card for gh#1065 / build PR #1095): `EmbeddingOrphanStore.DeleteOrphansAsync`'s `Suggestion` arm (anti-joined against `Suggestions.Id`) and `JournalEntry` arm (against `Trades.Id`), plus the gh#915 stale-model backstop over those same kinds. Unreachable below this tier — the `Embeddings` store's `Vector` column has no in-memory-provider mapping at all (gh#109, ADR-0001), so neither `ExecuteDeleteAsync` anti-join can be executed even once. **The failure mode it exists for**: both arms compare `suggestion.Id.ToString() == embedding.OwnerId`, a `uuid`→`text` translation with no other precedent in this codebase; render it as anything but the lowercase-hyphenated form `Guid.ToString()` wrote and the first pass deletes **the entire kind** while *nothing faults*, so the host's per-kind `catch` never fires, the embed pass silently re-embeds and re-bills every cycle, and grounding degrades back to news-only with no signal anywhere. Every case therefore carries its **live-owner converse**, and each orphan is produced by **deleting a row that existed** rather than by inventing a key that never did. Covers: an orphaned `Suggestion` / `JournalEntry` embedding reclaimed while a live owner's row survives; a **non-canonical Guid rendering** (uppercase, and the hyphen-less `N` form) of a live owner's id treated as an orphan — the anti-vacuity control proving the anti-join is a real text comparison and not a predicate that keeps everything; `IgnoreQueryFilters` proven **load-bearing** on both branches, with owners under two *different* non-empty operators surviving and an in-case control showing that a read of the same producer tables — scoped to the identity a background DI scope actually **resolves**, rather than to an assumed `Guid.Empty` — returns **zero** rows; and the stale-model backstop deleting exactly one crash-leaked duplicate per new kind while an owner's *only* vector is never touched. Every pass runs through a capturing logger asserted empty, because `SweepAsync` swallows a per-kind fault and an arm that **threw** otherwise reports the same zero a correct no-op does — which every "nothing was deleted" case here would pass over. Builds the `Firm` → `Connection` → `Account` chain both producers are FK'd to (with the account mode R-14's `ct_suggestions_mode_matches_account` demands) — the fixture cost gh#1096 was carded to carry, and the reason the sibling sweep suite's gh#1065 alignment could only *drop* its `Suggestion` case. Drives `EmbeddingOrphanGcHost.SweepAsync`, never the store alone, since that is the one place `EmbeddingOrphanSweep.SweepableKinds` is consulted. **Prove-red**: an unconditional delete (anti-join dropped) reddens all six cases; removing both kinds from `SweepableKinds` reddens exactly the three reclaim cases; removing `IgnoreQueryFilters` reddens all six (the whole kind is deleted on the first pass); dropping the stale-model self-`EXISTS` reddens only the only-vector case; a no-op backstop reddens only the duplicate case. **Finding, recorded not fixed**: the *inner* `.IgnoreQueryFilters()` on each producer sub-query is **inert** — removing it alone left all six green, because the root call is query-wide — so it documents intent rather than enforcing it; the root call is the load-bearing one | **Active** (Passing) |
| **`Ai/CrossKindEmbeddingRecallIntegrationTests.cs`** | That **each newly retrievable kind's partial HNSW index is present and actually chosen** (gh#1096 ⇒ gh#1065) — `IX_Embeddings_Vector_Cosine_Suggestion` and `IX_Embeddings_Vector_Cosine_JournalEntry`, added by the `AddContextVectorIndexes` migration. gh#1065's unit tier pins that the shipping predicate carries a **constant** owner kind (`PgVectorEmbeddingRecall.PredicateFor` compiled and asserted to hold a `ConstantExpression`, since a captured variable becomes a SQL *parameter* no partial index predicate can cover) — necessary but not sufficient: a correct constant against a **missing** index behaves identically, and the symptom of either is a *silently empty* result forever. Reproduces gh#861's starvation shape per kind at 15,000 rows — a crowd of 7,500 rows of the three owner kinds that have **no** partial index of their own, every one constructed strictly closer to the query than every target row, plus the other target kind at the same distance band — and asserts `IEmbeddingRecall.NearestAsync` still returns a full *n*, all of the asked-for kind (a count-only check would pass a "fix" that widened the search instead of scoping it). One seed poses the question to both indexes; keeping the crowd on unindexed kinds is also what keeps the bulk load affordable. Only the *table-wide* `IX_Embeddings_Vector_Cosine` is dropped and rebuilt around the seed (pgvector's bulk-load guidance) — the two partial indexes under test are never recreated by the fixture, so what serves the reads is the DDL the migration shipped. Before the seed it also reads `pg_indexes` and asserts the **closed set** of owner kinds any partial index on the table selects is exactly `{SoftSignal, Suggestion, JournalEntry}` — not a subset check, deliberately. The **load-bearing half is the targets**: without a partial index of its own, a target kind's read has nothing for this suite to prove chosen, and it would report a starvation it was never positioned to prevent. The noise half is **seed cost and accuracy, not protection** — an earlier version of this row and remark claimed an indexed noise kind stops crowding, which gh#1112 disproved by running it (an indexed kind substituted into the crowd with the target index dropped still starved the read to 0 of 5, because the crowding happens in the table-wide graph that holds every kind); what it buys is a bulk seed that never pays HNSW maintenance for 7,500 rows it will not query, and a `_noiseKinds` list that cannot quietly stop meaning what the remarks say — the drift that hit the sibling suite as gh#1110. It also refuses any partial index that does not partition by a simple `"OwnerKind" = <int>` equality, since a set predicate would cover several kinds behind a shape the parse cannot read. **Prove-red**: the *behavioural* evidence came first, before that guard existed — removing the `Suggestion` index from the migration returned **0 of 5** on that kind, and removing the `JournalEntry` one 0 of 5 on its own, each with the other kind unaffected; with the guard in place the same break now reddens **earlier**, at the closed set, which is a sharper diagnosis of the same defect, so that behavioural evidence stays on record as *why the index matters* rather than superseded. The guard itself reddens three ways: an indexed kind (`SoftSignal`) smuggled into the crowd, a dropped target index, and a set-predicate index (`WHERE "OwnerKind" = ANY (ARRAY[3, 5])`) over two noise kinds. Both kinds are recalled **before** either is asserted and their counts asserted as one map (gh#1112), so a run reports every kind rather than abandoning at the first — an unevaluated kind is not a passing one | **Active** (Passing) |
| **`Ai/CohereRerankLiveContractIntegrationTests.cs`** | The Cohere rerank provider's **live contract and keyless degrade** (gh#976, of gh#975, ADR-0008) — the unit tier already covers the fault-degrade / metering-value matrix against a fake transport (`CohereRerankProviderTests`). This proves what only real DI composition, a real socket, and a real meter can: production's own `AiRegistration` switch resolves the keyless `UnavailableReranker` with no key configured and it never reaches the network (proven by a wrapping handler that throws on any send); a genuine HTTP round trip to a local Kestrel stub (`StubCohereRerankServer`) parses into a valid permutation ordered by descending relevance; and a real (stubbed) fault increments the real `ai.rerank.calls` instrument on the shared `MarqSpec.TradingCopilot.Ai` meter via a `MeterListener`, not a fake recorder. A fourth case (`CohereLiveFactAttribute`-gated on `COHERE_API_KEY`) exercises the genuine `/v2/rerank` endpoint and **skips** honestly in this environment, which carries no live credential | **Active** (Passing; 1 case skips without `COHERE_API_KEY`) |

### The suggestion spine — the take, the throttle and the headroom read

| Integration Test Suite | Covered Endpoints & Features | Current Status |
| :--- | :--- | :--- |
| **`Api/SuggestionTakeIntegrationTests.cs`** | The **suggestion take, part A** (gh#614 ⇒ gh#548; R-4 / R-11b / R-12 / R-5 / R-16 / R-20, ADR-0007 / ADR-0013) — `POST /suggestions/{id}/take`, the first path that turns a suggestion into an order, and therefore the first place the model's proposal meets the gate that must still bind. Authored from the spec (the gh#548 issue body, PRD R-11b/R-12 and the ADRs), **not** from the coding implementation (QA contract §Role) | **Active** (Passing) |
| **`Suggestions/SuggestionThrottleIntegrationTests.cs`** | The **R-4 suggestion-throttle wiring** (gh#745 ⇒ gh#551 / gh#588 / gh#587, R-4 / R-5, ADR-0008) against **real Postgres**. The wiring shipped in #744 with full unit coverage on the EF **in-memory** provider; the two classes of behaviour invisible to that provider are what this suite exists for — authored independently of the implementation, from the spec | **Active** (Passing) |
| **`Suggestions/ConfluenceAssemblyIntegrationTests.cs`** | The **confluence assembly's scan → issuance path** (gh#967 ⇒ gh#730, ADR-0026, R-4) against **real Postgres**. The pure `ConfluenceAlignment` rule is unit-covered off the EF in-memory provider; this suite proves what only real Postgres can: a real multi-factor `Suggestion` composes (one primary + a corroborating indicator + an in-band level, satisfying `CK_SuggestionCitedFactors_KindColumns`); the primary stays the fired signal even against a smaller-timeframe level; the degenerate N=1 case excludes a retired level; an unresolvable instrument spec degrades to N=1 without aborting the pass — proving the co-located mechanical route still fires; the cited level snapshot stays frozen (no FK to `PriceLevels`) across a merge / flip / retire / delete of its source; and the venue-agnostic level read cites every venue while the resulting suggestion and factor set stay R-20 owner-scoped. Authored independently from the gh#967 issue spec, feeding raw `IndicatorValueRecord` / `PriceLevel` rows only (never a production-computed factor set) | **Active** (Passing) |
| **`Api/DailyHeadroomReadIntegrationTests.cs`** | The **daily-headroom read surface** — `GET /accounts/{id}/risk/headroom` + `DailyRealizedReader` — specified by gh#587 / gh#638 (R-5): the projection that lets the proactive, suggestion-time governor (gh#551) and any future headroom display (U3, gh#25) read today's consumed headroom **without composing an order**. Its R-14 mode filter is guarded separately by `RealizedReaderModeFilterIntegrationTests.cs` above, so a practice day cannot feed a live governor | **Active** (Passing) |

### Metric names, alert rules, alerting and the trigger-confirmation gate

| Integration Test Suite | Covered Endpoints & Features | Current Status |
| :--- | :--- | :--- |
| **`Observability/PrometheusMetricNamesTests.cs`** | The **pure instrument → Prometheus name matching** the staging telemetry suite (gh#332) depends on — runs in the **pre-merge** tier because the staging assertions cannot, and a matching rule that silently never matched would make the staging suite pass **vacuously** or fail on the first staging run for an unrelated reason. QA for gh#332 | **Active** (Passing) |
| **`Observability/AlertRuleSeriesReconciliationTests.cs`** | **Every alert-rule and dashboard series/label reconciled against what the app actually emits** (gh#536 ⇒ gh#245/#482/#370, R-20, ADR-0008) — the complement to `PrometheusMetricNamesTests` (which pins name *derivation*) and the promtool assertions (gh#537, which pin *threshold logic*): this pins that the series and labels the rules and dashboards **reference** are ones the meters actually **publish**, so a typo'd `outcome=` / `tier=` matcher or a wrong suffix — a rule that **silently never fires**, the highest-probability observability defect — is caught pre-merge. Source of truth is a live **`MeterListener`** over real `ExecutionMetrics` / `LlmMetrics` / `GovernorMetrics` / `EmbeddingMetrics` instances, never reflection over name constants (a constant compared with itself proves nothing); the Prometheus series are derived from the OTLP → `prometheusremotewrite` translation (brace-annotation units `{…}` dropped, `ms → milliseconds`, `_total` on **monotonic** sums only — an `UpDownCounter` exports bare — `_bucket` / `_sum` / `_count` on histograms), and **that translation table is itself pinned by a case** so it cannot drift ahead of the instruments that would expose it. Harvests `expr:` **only** (never descriptions / comments), handling `>-` folded scalars and dashboard `targets[].expr`; the dashboards are **enumerated from the directory, never listed by hand**, so a newly added one is reconciled the moment it lands. **Anti-vacuity anchor first** (non-empty rule + instrument sets, explicit membership including a **dashboard-only** series) — without it every case passes by matching nothing. **Database-independent** — no `PostgresApiFactory`, so it runs pre-merge without a container. Reconciled **clean** against current `develop`; prove-red'd by renaming an instrument (cases 0/1/2 go red). QA for gh#536 | **Active** (Passing) |
| **`Alerting/AlertingIntegrationTests.cs`** | The **alerting increment** (gh#246, gh#243, ADR-0019, R-13) through `AutoFlattenService.RunPassAsync` at a controlled instant, against real Postgres with the notification channel behind a recording double so **no test can page the operator**. Two claims that pull in opposite directions, both load-bearing: **it fires when it must** (a failed flatten has to reach a phone) and **it stays silent when it must** (a clean session pages nothing — a pager that cries on healthy days gets muted, and a muted pager manufactures confidence). Migrated onto the transport-only host in gh#480, so it now runs production's real `outbox → queue → dedup → Pushover` chain. The dead-man's-switch cases needing an **external** monitor are staging-tier, not here | **Active** (Passing) |
| **`Api/TriggerConfirmationGateIntegrationTests.cs`** | The **confirm-before-live gate** (gh#486 ⇒ gh#470, R-4 / R-7, ADR-0008) — what the unit tier structurally *cannot* reach: the gate lives in the scan's **queries** (owner discovery + the per-owner load), and a query predicate is only real against a real database; likewise the `CHECK`, the index, and the migration. **Every negative is paired with a positive** — "nothing fired" is trivially satisfied by accident (a misspelled symbol, an unseeded indicator, a stripped notifier), so each inertness claim has a twin changing **only the confirmation** and observing a real fire: a `TriggerFirings` row, an outbox row, and a page on the wire. Driven through the real `ScanAsync(now, …)` with hosted services stripped | **Active** (Passing) |
| **`Data/TriggerConfirmationBackfillIntegrationTests.cs`** | The **migration half** of the same gate (gh#486 ⇒ gh#470) — and it **owns its own container**, because every other suite reaches a database already migrated to head, which is exactly the state in which a backfill is invisible. To witness one you must stand *before* it: migrate to the **preceding** revision, write a row as the old world would have, then let `AddTriggerConfirmation` run. Asserts the migration does two **opposite** things at once — existing rows restored to `Confirmed` by an explicit `UPDATE` (a schema upgrade must never silently disarm alerts an operator relies on — the gh#458 failure class), while new rows still fall to the load-bearing `defaultValue: 0` (`Unconfirmed`) | **Active** (Passing) |
| **`Data/SuggestionCitedFactorsBackfillIntegrationTests.cs`** | The **migration half of the multi-cited-factor model** (gh#958, QA for gh#729; ADR-0026), mirroring `Data/TriggerConfirmationBackfillIntegrationTests.cs`'s own-container shape: migrate to the revision preceding `AddSuggestionCitedFactors`, write suggestions with the old `CitedIndicator`/`CitedPeriod`/`CitedResolutionMinutes` columns (raw SQL — the current `Suggestion` entity cannot even express them), then let the migration run. Asserts every pre-existing suggestion ends with **exactly one** primary `Kind = 1` factor carrying the old citation, **and** that the old columns are gone (`information_schema.columns`) — a backfill that left them in place would silently double-author the citation. Includes the **pre-gh#542 zero case**: a `CitedResolutionMinutes = 0` row, which the migration's `GREATEST(..., 1)` fail-safe must land at `TimeframeMinutes = 1` rather than abort the whole migration on `CK_SuggestionCitedFactors_Timeframe_Positive`. An anti-vacuity control confirms a fresh suggestion issued **after** the migration still persists normally. **Prove-red**: the `GREATEST` fail-safe removed reddened the case with a migration-aborting `DbUpdateException` on the zero row (not merely a wrong value); the three `DropColumn` calls removed reddened the "old columns gone" assertion specifically, both restored afterward | **Active** (Passing) |

### The co-pilot chat — the grounded turn and its read-only tool layer

| Integration Test Suite | Covered Endpoints & Features | Current Status |
| :--- | :--- | :--- |
| **`Api/Chat/ChatToolLayerIntegrationTests.cs`** | The **read-only chat tool layer** (gh#930 ⇒ gh#925, R-6, ADR-0025, extending gh#916's grounded turn) through `POST /conversations/{id}/turns` against real Postgres. This is the first surface where **the model chooses an action and the app executes it**, so the suite is written at the boundary. Five claims: a `tool_use` really **grounds** the turn (the scripted model can only produce journal text by echoing what the loop fed back, so an ungrounded turn cannot satisfy it), the answer persists and is pushed **per-owner**; the co-pilot **never reaches an order / write path**; the loop is **bounded**; **every** model call is ledgered; and `query_journal` is **R-20 owner-scoped**. The doubled seam is the outbound `ILlmProvider` only (`ScriptedChatLlmProvider`) — the tool registry, `ChatTurnService` and its round cap, the endpoint, the tenant filter, the ledger and the governor are the shipped composition, and `IChatRealtimeNotifier` is **wrapped, not replaced**, so production's `Clients.User` routing stays on the path. Guard construction: the offered / unoffered cases are **one theory over one mechanism** — the fed-back `LlmToolResult` — with `query_journal` as the control row, so the "never dispatched" verdict is proven to be a refusal rather than the only thing the assertion can say; a broken round cap trips the provider's runaway latch and goes **red** instead of hanging; the ledger case scripts **distinct per-call token counts**, so an aggregated or duplicated row fails where a count alone would pass. The order tripwire is a **recorder that would have moved** — the adversarial venue's place / modify / cancel / close counters plus a zero-`Order`/zero-`Suggestion` row check — deliberately not the method-name check §*The guard discipline* cites from PR #140. QA for gh#930 | **Active** (Passing) |
| **`Api/Chat/NewsGroundingIntegrationTests.cs`** | **Always-on news grounding, end to end** (gh#996 ⇒ gh#995, R-6, ADR-0027) through `POST /conversations/{id}/turns` against real Postgres + pgvector. Written independently from the ADR and the tracking issue. The scripted model can only answer with what it was actually handed (`request.Messages[^1].Content`), so a turn whose grounding never ran cannot satisfy the positive case, and the negative case (no news seeded) asserts the message is **byte-identical** to the raw prompt — ADR-0027 clause 2's own "empty grounding is a no-op" claim, not merely "no crash". The **safety-critical** injection case seeds a `NewsRecord` carrying an instruction-shaped sentinel and proves it reaches the model **only** as user-role content — the system prompt (asserted on every recorded call) never carries it, and the turn's shape (one streaming call, no tool dispatch) is unaffected. Two fail-open degrades: no embedding provider available (short-circuits before any ledgered spend), and a **genuine pgvector read fault** injected at the database (renaming the `Embeddings.Embedding` column out from under the live query, mirroring `AiSpendIntegrationTests`' insert-barrier trigger, never by doubling `INewsEmbeddingSimilarity`) — proving the endpoint's own fail-open `catch` around retrieval actually runs on an unexpected fault, not merely on the pipeline's internal degrade-to-empty. The two doubled seams are the outbound `ILlmProvider` and the outbound `IEmbeddingProvider` (`AdversarialEmbeddingProvider`, the same double `EmbeddingProviderDoubleTestPostgresFactory` uses) — `PgVectorNewsSimilarity`'s real cosine-distance read and the real keyless `UnavailableReranker` stay production code. QA for gh#996 | **Active** (Passing) |
| **`Api/Chat/NewsGroundingSpendIntegrationTests.cs`** | **Always-on grounding's interaction with the AI-spend governor** (gh#996 ⇒ gh#995 clauses 4/5, ADR-0027, ADR-0008) on a budget-configured host (`NewsGroundingSpendTestPostgresFactory`, gh#479's "one budget per factory" shape — cases vary the seeded spend, never the config). Three claims: grounding's **own** embed + rerank spend is ledgered and stamped to the conversation's owner, as two rows distinct from the turn's own chat completion (rerank rides `Chat` with `Tier == null`, the chat completion `Chat`/`Deep` — distinguished by tier, not merely counted); grounding is the **first cost shed** once spend crosses the 80% pre-alert threshold — the chat call itself still runs, but retrieval never fires and books no `Embed` row even though matching news exists; and a **governor-blocked** turn (429, spend at the hard cap) writes **no** `AiUsage` rows at all and persists nothing — retrieval never reached, because the gate returns before the operator's own turn is even saved. QA for gh#996 | **Active** (Passing) |
| **`Api/Chat/CrossKindGroundingIntegrationTests.cs`** | **Cross-kind** always-on grounding, end to end (gh#1096 ⇒ gh#1065, R-6, R-20, ADR-0027) through `POST /conversations/{id}/turns` against real Postgres + pgvector — `Api/Chat/NewsGroundingIntegrationTests.cs`'s shape widened from the news slice gh#995 shipped to the owner-scoped kinds. Only the two outbound third-party seams are doubled (the scripted model and the Cohere embed); `PgVectorEmbeddingRecall`'s cosine read, `ContextRetrievalService`'s cross-kind merge and R-20 hydrate, the keyless `UnavailableReranker`, `ContextEmbeddingContent`'s renderers and `ChatTurnService`'s envelope are all production code under test, and the model's only source of text is the exact content of the last message the loop sent it. Covers: a turn grounded on the operator's own `Suggestion` **and** closed `Trade`, asserted on the kind-labelled envelope lines and each renderer's exact output, with the fixed system prompt carrying none of it; **another operator's rows never reaching the model** — R-20 is enforced at the *hydrate*, since the `Embeddings` table is not `IUserOwned` and the recall is deployment-wide — with the turn left byte-identical to an un-grounded one, **plus an anti-vacuity control in the same case**: a second turn after re-owning the *very same rows* to the operator, where the identical sentinels now do arrive, so the first turn's silence can only be the tenant filter and not a broken read or a mis-seeded vector — both turns are taken **and reduced to named facts asserted as one map** (gh#1112), because taking them both is not enough on its own: sequential assertions are still fail-fast, and with the control's three last a first-turn failure left the control's evidence unobserved — which is not the same as passing; and the **safety** case — an instruction-shaped sentinel in a model-authored `Suggestion.Rationale` reaching the model **only** as user-role content, with the turn's shape unchanged (one streaming call, two persisted messages), because a rationale is the model's own earlier output and elevating it would let one turn's prose become the next turn's instructions. **Prove-red**: reducing `RetrievalKinds.All` to News reddens all three cases; bypassing the hydrate's tenant filter reddens exactly the isolation case, on the turn-1 facts of the map above, with the control turn's facts reported in the same failure rather than skipped; moving the grounding block into the system prompt reddens all three, the safety case on the assertion that matters | **Active** (Passing) |
| **`Api/Chat/SearchNewsToolIntegrationTests.cs`** | The **`search_news` tool's pgvector recall** (gh#988, of gh#987, R-6), driven through the real `IChatTool` resolved from DI rather than `INewsRetrievalService`/`INewsEmbeddingSimilarity` directly — the layer between `NewsEmbeddingRecallIntegrationTests` (gh#861, the bare similarity seam) and `NewsGroundingIntegrationTests` (gh#996, the chat-turn endpoint). Five claims: nearest-first recall honors the requested `limit`; a Suggestion-owned embedding identical to the query vector never surfaces (seeded WITH a matching `NewsRecord`, so a dropped `OwnerKind` filter is actually visible at hydration rather than silently absorbed by "nothing to hydrate"); the R-20 global read is unaffected by another operator's `SoftSignalFeedback` on the same item; an unavailable embedding provider degrades to `{"results":[]}` with **no** `Embed` ledger row (the distinguishing side effect of the early short-circuit, since this double's own null-vector fallback would otherwise mask a dropped guard); and a genuine pgvector fault (the `Embeddings.Embedding` column renamed out from under the live query) fails **closed** through the tool's own `catch` to a compact error string — the fail-**closed** counterpart to gh#996's fail-**open** endpoint catch. The one doubled seam is `AdversarialEmbeddingProvider`; `PgVectorNewsSimilarity` and the real keyless `UnavailableReranker` stay production code. QA for gh#988 | **Active** (Passing) |
| **`Api/Chat/SearchNewsRerankConsumptionIntegrationTests.cs`** | **`search_news`'s rerank-order consumption** (gh#988, of gh#987) — its own host swaps in `AdversarialReranker` (deterministically reverses its input) in place of the keyless default, proving the tool's returned order is genuinely the reranker's own rather than a silently-substituted recall order, with no candidate dropped in the round trip. Kept in its own class / container because it needs a different `IReranker` composition than the sibling suite's keyless-passthrough default. QA for gh#988 | **Active** (Passing) |

---

## 3. High-Value Test Opportunities & Risk Analysis

```mermaid
flowchart TD
    subgraph Staged Send Path Integration
        A[POST /orders/arm] -->|Stage Proposal| B[(Database Order Row)]
        B -->|WorkingStopPrice Persisted| C[PUT /orders/{id}]
        C -->|Re-gate Edit| D[POST /orders/{id}/take]
        D -->|Fresh Venue & Risk Re-check| E[Transmitted / Working]
        D -->|Risk Violation| F[Staged / HTTP 422 Refused]
    end
    subgraph Multi-Tenant Isolation
        G[Operator A Context] -->|Query Operator B Order| H[HTTP 404 Not Found]
        I[Operator A Context] -->|Take Operator B Order| J[HTTP 404 Not Found]
    end
```

### High-Risk Execution Paths — the original audit's three targets (**all since delivered**)
*Kept as the record of what this audit was opened to close. Each is now covered; see §2 for the suite.*

1. ~~**Working Stop Persistence & Re-validation (`gh#134`)**~~ — **delivered**: `StagedOrderLadderIntegrationTests`
   carries the gh#134 regression; limit/market orders armed with tight working stops do not size against the
   worst-case safety stop at `take`, and `WorkingStopPrice` persistence is proven against real Postgres.
2. ~~**Database Trigger Guard Verification**~~ — **delivered**: the `enforce_mode_matches_account` constraint
   trigger (`Order.mode` must equal `Account.mode`) is exercised in the order suites (gh#130, gh#157) — see §1.
3. ~~**Audit Trail Integrity (`GateDecisionRecord`)**~~ — **delivered**: that every sized attempt, placed or
   blocked, persists a `GateDecisionRecord` scoped to `UserId` / `AccountId` / optional `OrderId` is asserted
   across the order, kill-switch and consistency-target suites.

---

## 4. What the suites found — live status is the tracker

**Per-issue status is no longer mirrored here.** It was: a 56-row table of issue → suite → target file → ✅/🟡
that restated the tracker and went stale the moment an issue moved. The root [`AGENTS.md`](../AGENTS.md) rules
that out — task specs and status belong in the issue, never as a file under `documentation/`, because a parallel
copy duplicates the tracker and drifts from it. Retired in gh#616.

**For live status, ask the tracker** — it is the source of truth (gh#144):

```bash
gh issue list --state all --search "QA in:title" --json number,title,state
```

§2 above remains the inventory of *what exists and where it lives*; that is repo knowledge and stays here.

### What the retired table held that a status query cannot recover

A suite's value is the defect it caught, and that is recorded nowhere else. Preserved:

| Suite (its issue) | Found |
| :--- | :--- |
| Notification durability (gh#442) | **P0 gh#459** — the outbox relay resolved its downstream back onto the seam it drains, so it drained into itself and silently discarded every notification |
| Notification durability (gh#481) | **P1 gh#497** |
| Flatten-tier account set (gh#512) | **gh#522** — both auto-flatten tiers shared one credential set |
| Instrumentation safety (gh#331) | **gh#343** |
| Consistency target (gh#394) | **gh#407** |

### Known coverage gaps — also not recoverable from a status query

- **gh#514** — pending-conditional detection and two live-host cases were **not delivered** with the suite; they
  need a separate host-recovery approach (`GapBackfillHostRecoveryIntegrationTests.cs`).
- **gh#512** — one case was **not delivered**; it needs its own decision first (see that PR).

---

## 5. Route-map realignment log (gh#160)

The open QA specs were drafted from the audit's *intended* surface, not the *built* one; gh#160 corrected each
against the live route map on `develop` so every remaining test names an endpoint that exists:

- **#142** — `PUT /connections/{id}/credentials` and `DELETE /connections/{id}` do **not** exist; they are missing
  *features*, moved to [#210](https://github.com/adammarquette/trading-copilot/issues/210). The suite now covers
  connection creation, account discovery, and stage resolution/override — all routes that exist. Re-estimated 3 → 2.
- **#132** — `GET /connections/{id}` (by-id) and a gate-decision audit endpoint do **not** exist. Test 1 becomes
  collection-scoping (`GET /connections` must exclude the other operator's connection); Test 4 is parked at the
  `TradingCopilotDbContext` level until a gate-decision read surface exists. Stays **P0**.
- **#143** — declaration is `PUT /accounts/{id}/risk`, not `POST`; its `take` test now **reuses the gh#157
  staged-ladder harness** rather than standing up a second arm/take fixture.

Earlier drift this refresh also cleared: the never-created `OrderExecutionEndpointsIntegrationTests.cs` /
`ProductionSmokeTests.cs` target names (the shipped files are `OrderEndpointsIntegrationTests.cs` /
`SystemSmokeIntegrationTests.cs`); `#134` mislisted as a parent *task* (it is a **PR**); and a stale
"Ready for Development" header while several suites had already shipped and closed.
