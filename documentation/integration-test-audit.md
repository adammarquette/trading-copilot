# Integration Test Suite Audit & Ticket Backlog Report

**Author**: Primary QA / SDET  
**Date**: July 23, 2026 · **Realigned to the route map**: 2026-07-24 (gh#160)  
**Target Repository**: `trading-copilot` (`src/MarqSpec.TradingCopilot.IntegrationTests`)  
**Status**: Living inventory — suites land incrementally; see §4 for current per-issue status · reconciled 2026-07-30 (gh#490: added 5 shipped suites the inventory had missed — send-as-is, news ingestion/relevance/salience, the pre-merge Prometheus-name guard)  

---

## 1. Executive Summary & Audit Context

As the primary QA/SDET for **Trading Copilot**, a comprehensive audit was conducted across the API endpoints, service layer, domain guards, multi-tenancy model (ADR-0017 / R-20), and database persistence layer (`TradingCopilotDbContext` & DB constraint triggers).

The objective of this audit is to identify high-value integration testing gaps, quantify risk coverage, and define actionable tickets to raise the quality ceiling of the platform.

### Key Audit Findings (as first written; §2 and §4 below are the live record)
1. **Existing Integration Coverage**: Firm Onboarding, Risk Profile Declaration, User Invitation / Primary-Operator Auth Guard, the unauthenticated challenge, order execution (direct + staged ladder), the staged-stop plan, the event backbone, the synthetic conditional-entry firing engine, and the production smoke suite — see §2 for the full inventory.
2. **Gaps — delivered (✅) and remaining (⬜):**
   - ✅ **Staged Send Path & Order Execution (`arm` → `edit` → `take` → `cancel`)** — direct send (gh#130), the staged ladder (gh#157), and the stop-plan DB guards (gh#158) all landed against Testcontainers Postgres.
   - ✅ **R-14 Database Mode & Environment Enforcement** — the `Order.mode == Account.mode` trigger and mode × environment refusals are exercised in the order suites (gh#130, gh#157).
   - ✅ **Multi-Tenant / Multi-Operator Data Isolation (R-20 / ADR-0017)** — **gh#132**: collection-scoping, cross-tenant `404`s on accounts/orders, and gate-decision isolation proven at the `DbContext` (§2).
   - ✅ **Connection Lifecycle & Account Stage** — creation, discovery, and stage resolution/override covered (**gh#142**). Credential *rotation* and connection *soft-delete* remain **missing features**, not missing tests → **gh#210** for the Coding Agent.
   - ✅ **Production Read-Only Smoke Test Suite** — gh#131; the lazy-fixture fix so a deployed-target run starts no container is gh#152.
   - ✅ **Event Backbone Storage** — the append-only Timescale log at the storage tier (gh#161).
   - ✅ **Synthetic Conditional-Entry Firing Engine (ADR-0007 / R-11)** — arm (persist `Pending`, transmit nothing) → a quote crossing the trigger fires it to a **working native order** through the **authoritative fire-time re-gate** (R-5 / R-16 / R-12: a conditional valid at arm but violating the gate at fire is refused, not sent); cancel-if drift, expiry-outranks-fire, fired-then-replayed idempotency, a live-host `market.quote` backbone consume, and the `CK_ConditionalOrders_*` DB guards — **gh#180**, pairing the #198 watcher.

---

## 2. Current Integration Test Inventory

Below is the baseline inventory of current integration tests under [`src/MarqSpec.TradingCopilot.IntegrationTests`](../src/MarqSpec.TradingCopilot.IntegrationTests) (`Api/` for HTTP-driven suites, `Data/` for storage-tier ones):

| Integration Test Suite | Covered Endpoints & Features | Current Status |
| :--- | :--- | :--- |
| **`FirmOnboardingEndpointsIntegrationTests.cs`** | `POST /firms`, `GET /firms`, `PUT /firms/{id}/conventions`, connection creation & account discovery | **Active** (Passing) |
| **`RiskEndpointsIntegrationTests.cs`** | `PUT /accounts/{id}/risk`, `GET /accounts/{id}/risk` | **Active** (Passing) |
| **`UserInvitationEndpointsIntegrationTests.cs`** | `POST /auth/invitations`, `POST /auth/invitations/accept`, Primary Operator flag enforcement | **Active** (Passing) |
| **`UnauthenticatedEndpointsTests.cs`** | Global HTTP 401 unauthenticated challenge verification, over an **eleven-route hand-maintained list**. Superseded in scope by `AuthorizationSurfaceIntegrationTests` (gh#511), which enumerates the real route table; kept as the narrow, explicit-list regression | **Active** (Passing) |
| **`FlattenTierAccountSetIntegrationTests.cs`** | **Which accounts each flatten tier acts on** (gh#512 ⇒ gh#187, R-13, ADR-0013, ADR-0015) — the credential-set boundary on **both** tiers, and their agreement. Guards: an account whose connection sits on a **sibling credential set** produces **no venue traffic at all** on the primary tier and on the watchdog (asserted via `PositionReadCount == 0`, since the credential check sits *above* `GetAccountsAsync` — "no close happened" cannot witness this, being equally true of a pass that read the venue and declined); and **both tiers act on the same account set** for one database + credential key, asserted behaviourally by comparing the distinct account keys in `ClosePositionCalls`, with the primary's non-empty set as the positive control. Exists because **both tiers' unit guards were vacuous** — their shared `Venue(...)` helper leaves `GetAccountsAsync` unstubbed, so the roster is FakeItEasy's empty dummy, every account is skipped as "not reported by the venue" before the credential comparison is reached, and deleting the production filter leaves both green (filed as gh#522). The foreign-key state is produced by a **database reassignment**, not a differently-keyed discovery: `POST /connections/{id}/accounts/discover` answers **409** for a connection on another key, so the API cannot create it — it arises only from rows discovered under one key whose connection later belongs to a sibling (a gh#210 rotation, or two processes on one database) | **Active** (Passing) |
| **`AuthorizationSurfaceIntegrationTests.cs`** | The **authorization surface** (gh#511 ⇒ gh#8, R-18, R-11/R-16) — every route the app maps, read from `EndpointDataSource` rather than a list, so a new route is covered the moment it is mapped. The hand list it replaces covered 11 of **45** authorized routes and included **no execution route, no kill-switch route and neither venue-truth read**; `AddAuthorization()` has no `FallbackPolicy`, so each route's gate is its own `.RequireAuthorization()` and an omission has nothing above it to catch it (`PUT /connections/{id}/credentials` and `DELETE /connections/{id}` shipped in gh#210 and were never added to the list). Guards: every mapped route 401s **anonymously** and **with a malformed token**; a structural check that the only routes reachable through the *absence* of a gate are the liveness and readiness probes, and the only ones **declaring** `[AllowAnonymous]` are login and accept-invite; the four anonymous routes really are reachable; and the token is genuinely validated — **expired**, **signed with another key**, **foreign issuer**, **foreign audience** — each against a **positive control** that mints a correct token the same way, so the negative cases cannot pass because the minting is wrong. Carries its own **anti-vacuity guard** (route count + explicit membership of all 8 execution, all 3 kill-switch and both venue-truth routes), proven necessary: with the enumeration emptied, both sweeps pass and only that guard goes red | **Active** (Passing) |
| **`OrderEndpointsIntegrationTests.cs`** | `POST /accounts/{id}/orders` — the **direct** send path: fail-closed risk absence, credential-key guard, gate refusal + audit trail, R-14 mode × environment (gh#130) | **Active** (Passing) |
| **`StagedOrderLadderIntegrationTests.cs`** | The **staged ladder** — `POST …/orders/arm`, `PUT /orders/{id}`, `POST /orders/{id}/take`, `DELETE /orders/{id}` — plus the gh#134 working-stop regression and the gh#96 DB mode guard (gh#157) | **Active** (Passing) |
| **`StopPlanPersistenceIntegrationTests.cs`** | Staged-stop plan persistence (`POST /accounts/{id}/orders` → `StopPlanRecord`); the four `CK_StopPlans_*` DB CHECK constraints proven by name on **both** safety-beyond-actual sides with a positive control; FK `ON DELETE CASCADE`; ATR not-yet-supported pin (gh#158) | **Active** (Passing) |
| **`SystemSmokeIntegrationTests.cs`** | `Category=Smoke` read-only probes against a deployed target (gh#131); the in-process host + its container are now **lazy**, so a deployed-target run starts no PostgreSQL container / needs no Docker (gh#152) | **Active** (Passing) |
| **`Data/EventBackboneIntegrationTests.cs`** | The append-only event log (ADR-0001) at the **storage tier** via `IEventLog` against the applied `AddEventBackbone` hypertable migration — envelope round-trip (`jsonb` payload, UTC instant), monotonic sequences under concurrent appends, the #156 replay two-rows-one-`Id` contract, id generation, `ReadAfter` ordering + paging, cursor upsert, blank type/source rejection; and a non-UTC `OccurredAt` rejection (gh#201). **Retention-gap signal (gh#228, flipping the gh#162 pin now gh#227 shipped):** `ReadAfterAsync` returns `EventPage.Gap` (requested cursor + oldest surviving sequence) when a cursor trails the dropped window, and stays silent for a fresh group (cursor 0), an empty head, a contiguous read, and once a consumer has resumed inside the window; the gap is durable across a restart. The drop is manufactured by deleting the oldest rows — the gap decision is a row-existence check, not a chunk read, so this is the same decision path a real chunk-drop leaves (gh#161) | **Active** (Passing) |
| **`Data/RetentionGapConsumerIntegrationTests.cs`** | **Consumer-side** retention-gap handling (gh#228): the two safety-critical event-log consumers — the live `StopPromotionHost` and `ConditionalOrderHost` — must, when the log signals a gap, **log it high-severity (`Error`) and keep going**, neither swallowing the signal (silent loss) nor crash-looping. Runs the live hosts, manufactures a real hole under a caught-up consumer (delete the consumed window + burn a sequence), and asserts the emergency log fired and the cursor advanced past it (gh#228) | **Active** (Passing) |
| **`MultiTenantIsolationIntegrationTests.cs`** | R-20 default-deny workspace isolation between two operators (ADR-0017): collection-scoping (`GET /connections` excludes the other operator), `GET /connections/{id}/accounts` → 404 for a non-owner, staged-order `take`/`cancel` → 404 (order stays `Staged`), and gate-decision isolation proven at a user-scoped `DbContext`; second operator via the invitation flow (gh#132) | **Active** (Passing) |
| **`RiskProfileLifecycleIntegrationTests.cs`** | Per-account risk declaration (`PUT /accounts/{id}/risk`) — persistence + one-per-account replace-on-redeclaration; and the **R-12 re-gate**: a profile tightened *after* arming refuses the next `take` (422 `RefusedByRisk`, order stays `Staged`, the refusal journaled as a blocked gate decision) (gh#143) | **Active** (Passing) |
| **`ConnectionLifecycleIntegrationTests.cs`** | Connection creation, account discovery, and the per-account stage override — the roster persists and reads back with **mode resolved from the firm's declared conventions**, not the venue's flag (Practice, not the stub's Live; unrecognised names stay `Undeclared`, gh#60); one-login-per-firm×platform `409` (ADR-0016); stage override → convention mode / `Unknown` refused; clear reverts to the resolved stage (gh#142). **gh#229** completes the suite for shipped gh#210: **credential rotation** (`PUT /connections/{id}/credentials`) repoints the key, the venue path picks up the new key on the next call, the stored value is the **verbatim env-entry name — never a secret** (ADR-0015), and R-20 gives `404` to a non-owner; **soft-delete** (`DELETE /connections/{id}`) deactivates rather than removes, **cascades to child accounts**, **preserves the journal** (orders survive — the highest-value guard, given the real `OnDelete(Cascade)` FKs), is idempotent, and R-20-scoped. **DEFECT gh#322** pinned: the send path does not enforce deactivation, so a retired connection still transmits orders (flips to a guard when fixed) | **Active** (Passing) |
| **`TestHost/Staging/*` + `Observability/TelemetryStagingIntegrationTests.cs`, `Api/BracketSizingStagingIntegrationTests.cs`** | **Post-merge staging harness (gh#335)** — the shared bootstrap for the venue-execution gates (gh#269, gh#293) and the telemetry SLI suite (gh#234): `StagingConfig` binds the deployed API + a reserved **ProjectX practice account** + the live Prometheus/Loki/Tempo endpoints from **CI secrets** (never source); `[StagingVenueFact]` / `[StagingObservabilityFact]` / `[StagingGatewayFact]` **skip by construction** when unconfigured (so pre-merge CI stays green); `StagingApiClient` authenticates against the deployed API; `[StagingExecutionCollection]` serializes order-placing onto the one practice account (R-14, practice-only). Telemetry cases query the real backends | **Staging** (skips pre-merge) |
| **`Api/BracketPreservationStagingIntegrationTests.cs`** (gh#269 ⇒ gh#259) | **Bracket-preservation pre-live gate** — certifies the gh#259 reprice safety claim only the real gateway can witness: a **price-only in-place modify** of a working bracketed entry **leaves the attached protective bracket intact**. Places through the deployed app (its real gate), reprices in place, fills, then reads **venue truth directly from ProjectX** — the app exposes no resting-orders read and its `WorkingOrder` view drops the size — via `StagingProjectXGateway` (`GetOpenOrdersAsync` → raw `Order` with `Size`/`StopPrice`), asserting a native protective stop still rests covering the position. Prove-the-red: a gateway that drops the bracket on modify leaves the fill naked → no resting stop → red. Serialized on the reserved practice account; **prove-red'd on the first staging run** (only the gateway can witness it) | **Staging** (skips pre-merge) |
| **`Api/BracketSizingStagingIntegrationTests.cs`** (gh#293 ⇒ gh#292) | **Bracket-sizing hard pre-live gate** — certifies the gh#292 resize inference: the always-native safety bracket (no size field, instantiated on fill) sizes to the **realized fill**. **Resize UP** while working then fill → the on-fill protective stop covers the **full larger fill** (no naked excess); **resize DOWN** then fill → it covers the **smaller fill exactly** (no oversized residual that reverses into a naked position after close). Reads venue truth from the gateway (`StagingProjectXGateway`): position size + protective-stop `Size` both equal the resized quantity. Prove-the-red: a gateway that pins the bracket to the placement-time size leaves the stop at the old quantity → red. The partial-fill window is closed at the app (modify refuses a partially-filled order), so a 0-filled working order's realized fill is the whole new size. Serialized on the practice account; **prove-red'd on the first staging run** | **Staging** (skips pre-merge) |
| **`MarketData/GapBackfillIntegrationTests.cs`** | **Event-log gap → backfill recovery** (gh#514 ⇒ gh#306, R-1) — `GapBackfillService.ReplayForStopPromotionAsync` driven **directly** against real Timescale. Driving it directly rather than through `StopPromotionHost` is the point: that host's catch-all turns an EF translation failure into *"resuming at the head without recovery"*, so a defect here would be a log line rather than a red test. Guards: the **relational projection** — `List<Guid>.Contains` → `= ANY`, a constructor projection into a **private nested record**, `Distinct()` over it, and `Symbol ?? Instrument` → `COALESCE` — all of which the unit tier evaluates client-side and so never translates; a hidden stop **promotes** when the window's adverse extreme crossed its band, and **does not** when it fell one tick short (a control pair differing by exactly one scalar, which is also the only thing that catches a **Low/High polarity swap** in the synthetic quote — the negative case alone cannot); coverage measured from the **finest stored resolution** with the real uncovered duration **metered**, against an hourly bar spanning the same window that must not be credited; a fully covered window **meters nothing**; and a replayed gap **promotes exactly once**. The suite is the first to store two bar resolutions over one window — the existing seeding hardcodes a single one, so the `Min()` that picks the finest series had never had a second value to choose between | **Active** (Passing) |
| **`MarketData/BarStoreIntegrationTests.cs`** | **Clean-historical bar store & REST backfill** (R-1, R-9, R-20, ADR-0001) via `BarBackfillService.BackfillAsync` driven directly against real Timescale: the store is a **system of record**, not a cache — **no retention policy** (introspected against `timescaledb_information.jobs`; the event log's 24h policy is the positive control) and **untouched when the event log's data is dropped**; an overlapping re-poll **updates in place, never duplicates** (composite PK `{Venue,Instrument,Resolution,BucketStart}` rejects a raw duplicate by construction); a venue **revision wins in place**; a **still-forming bucket is never stored as final** (partial-bar drop — the highest-value guard); the **capability seam** refuses loudly when `HistoricalBars` is absent (R-17); a **failed poll heals next pass** and a **venue throw never kills the host**; and bars are **visible to every operator** (no tenant filter — market data is global, R-20). Extends the stub to feed bars + model the capability/throw seams; hosts suppressed via `OcoExitTestPostgresFactory` (gh#303) | **Active** (Passing) |
| **`ConditionalFiringIntegrationTests.cs`** | The **synthetic conditional-entry firing engine** (ADR-0007, pairing the #198 watcher): `POST /accounts/{id}/orders/conditional` arms a pending conditional (persisted `Pending`, **transmits nothing**); the firing watcher (`ConditionalFiringService` / `ConditionalOrderHost`) fires it to a **working native order** when a quote crosses the trigger, **re-running R-5 / R-16 / R-12 at fire time** — a conditional valid at arm but violating the gate at fire is **refused, not sent** (proven by tightening risk between arm and fire); cancel-if drift; expiry outranks a crossing trigger; fired-then-replayed idempotency; a live-background-host `market.quote` backbone consume; and the `CK_ConditionalOrders_CancelDrift_StaleSide` / `_Direction_NotUnknown` DB checks proven by name (gh#180) | **Active** (Passing) |
| **`AutoFlattenSchedulerIntegrationTests.cs`** | The **primary auto-flatten scheduler** (R-13, ADR-0013) via `AutoFlattenService.RunPassAsync` at a controlled instant: closes an open position at its per-market deadline (`flatten.executed`) and **never opens** exposure; warns before the deadline, journals `missed` past the firing window, `disabled`, and `unconfigured`; retries to the attempt cap then `escalated` on a surviving position — every decision journalled to the real event log. Drives the shared `TestHost` position stub (open positions + configurable close) with the always-on hosts suppressed (gh#186) | **Active** (Passing) |
| **`AutoFlattenWatchdogIntegrationTests.cs`** | The **redundant auto-flatten watchdog** (R-13, ADR-0013) via `AutoFlattenWatchdogService.RunPassAsync`: the independent second tier steps in only on the primary's failure — a position open past deadline **+ grace** — closing once per pass (`flatten.watchdog.saved`), **defers to the primary inside the grace window**, journals `flatten.watchdog.rejected` when the close leaves exposure **or the venue rejects it** (one attempt, retried next pass — never a silent give-up), and `flatten.watchdog.critical` past the firing window; never opens exposure (gh#188) | **Active** (Passing) |
| **`DeadMansSwitchCheckInIntegrationTests.cs`** | The **dead-man's-switch daily check-in** (R-13, ADR-0019, ADR-0015) via `FlattenCheckInService.RunPassAsync` at a chosen instant — the **tier-3, out-of-process** redundancy whose **silence** is the pager signal, and the first coverage it has had. Both failure directions are guarded: exposure past the deadline **withholds** the instrument's check-in (so the monitor pages) while other flat markets still report; a **flat** market is vouched for exactly once, recorded against the **market** day; exposure **aggregates across every account** on the venue (a flat account beside an open one still withholds); an account on a **sibling credential key** is skipped before any venue read (ADR-0015); a **rejected** report is deliberately **not recorded**, so the next pass retries it (proven by refusing once, then accepting); and outside the `IsDue` window the pass does **no venue work at all** — witnessed by a new `PositionReadCount` on the stub, since an empty result alone cannot tell "didn't look" from "looked and withheld". All six guards **prove-red'd** across two rounds (exposure-ignored, credential-filter off, rejection recorded anyway, both due-gates bypassed; then a second round proving the positive report-flat guard). The external monitor is a `RecordingDeadMansSwitch` — an outbound third-party seam like the venue, which records what was vouched and can refuse, never supplying the decision; hosts suppressed via `DeadMansSwitchTestPostgresFactory` (gh#408) | **Active** (Passing) |
| **`Observability/NotificationDurabilityIntegrationTests.cs`** | The **durable notification seam** (gh#442 ⇒ gh#400/gh#437, R-13, ADR-0019) — that a page **survives the process that raised it**. Runs on the shared `NotificationHarnessPostgresFactory`, which substitutes only the **wire** (the socket beneath `PushoverNotificationChannel`), so the outbox, relay, queue, dedup decorator and the real Pushover adapter are all **production objects** — and the `Pushover:IsConfigured == true` branch is under test for the first time. Guards: a page is **persisted before anything is delivered** (read through a fresh scope, so it proves a commit); one incident is **one row** *while it is still owed* (the gh#458 unique index on `DedupKey` **filtered to `DeliveredAt IS NULL`**, invisible to an in-memory provider); an incident that clears first is **withdrawn undelivered** and never pages; and a row **left by a dead process** is picked up by a later host. gh#481 added the **dedup key's lifetime**: a *delivered* incident releases its key and is recordable again; suppression still holds while the first occurrence is owed (asserted across two scopes, so the change-tracker's `Local` set cannot be what suppresses); a **resolved-and-recurring** incident pages twice; and an already-owed incident **does not abort the producer's save** — the gh#455 shape, driven through a real auto-flatten escalation, since `EnlistAsync` only *stages* and the violation would surface on the flatten's own `SaveChanges`, outside the try/catch that guards the enlist. Ships with a **fidelity guard** — three layers (a `sealed` `ConfigureTestServices`, a descriptor-count check around the suite hook, and a resolve-and-typecheck of the composed seam) plus named `NotificationHarnessFidelityTests` — because the gh#442 defect was a *copy-pasted* harness block that silently deleted the outbox from two hosts once gh#437 landed; the guard is **prove-red'd** by pasting that block into a third harness and watching it throw by name. The four `DEFECT gh#459` pins (the P0 this suite found) were flipped to the real guarantee when gh#468 landed. **One assertion is now pinned `gh#497 — resolved, and **not** by flipping this assertion`**: the second occurrence of a *delivered* incident is recorded in the outbox (2 rows — a delivered incident releases its key there, gh#458) but deliberately still collapses to **one page**. Two occurrences with **no resolve between them** are indistinguishable at this layer from one *continuing* incident, and the flatten re-emits roughly every 15s while exposure persists — releasing the key on delivery would page ~120 times in a 30-minute exposure, precisely the alert fatigue ADR-0019 §4 exists to prevent. gh#497's real defect was that the incident never **ended**: `AutoFlattenService` resolved only on its **own** successful close, so an exposure ended by the operator's hand (or a prop firm's forced flatten) left the key armed forever. **Fixed at the producer** (`f8aa3a1`) — a pass now re-arms whenever it observes the instrument flat **by any means**; `Outbox_ShouldPageAgain_WhenTheIncidentResolvedBetweenOccurrences` is the end-to-end shape of it (gh#442, gh#481, gh#497) | **Active** (Passing) |
| **`AgentReviewRouteIntegrationTests.cs`** | The **agent-review route** (R-4, ADR-0008, ADR-0007) via `TriggerEvaluationService.ScanAsync` at a chosen instant — the **first place an LLM enters the system**, and the first coverage of *"the gate lives below the model."* The headline guards assert that invariant **behaviourally**, which the structural unit test cannot: after a fire — **including one whose completion demands execution and a disabled kill switch** — the journal holds a `Suggestion` and **nothing else** (no `Order`, no `GateDecision`, no `StopPlan`), the venue records zero calls, and the proposal stays inert across later scans. **One review per arming edge** is proven durably (three scans of a still-true condition ⇒ exactly one LLM call), including under a **provider outage** (a fault consumes the edge — no retry storm, the unbounded-spend failure), and a trigger authored into an already-satisfied condition **seeds silently**. **Size and mode are the operator's**: a completion demanding 50 contracts still stages the trigger's size, and mode is read live off the account. Fail-closed through the **production composition**: unparseable output and an **absent direction** (which must never default to `Buy`) stage nothing, and a **default deployment with no `Llm:ApiKey` binds `NullTriggerReviewer`** — zero provider calls, but an honest advisory. R-20 scoping and the `CK_Triggers_AgentReview_RequiresAccountAndSize` / `CK_Triggers_Mechanical_NoAccount` checks are proven against **real Postgres** (asserted by constraint name). The only double is an **adversarial `ILlmProvider`** — it feeds raw completions, never the decision, so the real `LlmTriggerReviewer` stays under test; indicator readings are seeded as real `IndicatorValues` rows through the production source. Hosts suppressed via `AgentReviewTestPostgresFactory` (gh#429) | **Active** (Passing) |
| **`KillSwitchIntegrationTests.cs`** | The **kill switch** (R-11, ADR-0007) end-to-end over HTTP (10 tests): engaging (**hold-to-confirm** — an unconfirmed request is refused `422` and does not engage) **disables outbound** so a subsequent direct send **and** a staged `take` are both refused (`RefusedByKillSwitch`, nothing transmitted, the ticket stays `Staged`, and the refusal **precedes sizing** — no `GateDecisionRecord`); **cancels working orders**; and per mode either **flattens all** open positions (close called, count reported) or **halts only** (leaves them on their native safety stops, closes nothing) — with an **omitted mode failing safe to FlattenAll**. The reducing path is **deliberately exempt**: auto-flatten still closes at the deadline while engaged (ADR-0007). Disengaging **re-enables** outbound; the engaged lock **rehydrates at host startup** from the durable row (a fresh host on the same DB comes up engaged, ADR-0013). Reuses the shared `TestHost` position stub + `FlattenTestPostgresFactory` (gh#190) | **Active** (Passing) |
| **`OrphanHandlingIntegrationTests.cs`** | **Connection-loss orphan handling** (R-11, R-19, ADR-0013) via `OrphanGuardService.OrphanAsync`/`RearmAsync` + `StopPromotionService.PromoteForQuoteAsync`: a drop moves **hidden** stops to `Orphaned` (high-severity `synthetic_risk` log) while **native** exchange-held stops and **pending conditionals** are left untouched; an orphaned stop is **never promotable** (Hidden-only, by construction) and the **native safety stop stays the floor** throughout; reconnect **re-arms** orphans to `Hidden` against venue truth and **auto-resumes nothing else** (native/retired/conditional); a restart **reconciles lingering orphans** on the first pass; ownership is **preserved** through the `IgnoreQueryFilters` sweep; and flaps are **idempotent**. Adversarial venue positions + always-on hosts suppressed via `OrphanTestPostgresFactory` (gh#192) | **Active** (Passing) |
| **`StateRehydrationIntegrationTests.cs`** | **Decision-state rehydration across a restart** (R-20, R-12, R-13, ADR-0013) — the Postgres container outlives the host, so a second host via `CreateHost` re-runs `StartupTasks` against the same DB (that *is* the restart). A staged order stays `Staged` (neither auto-sent nor cancelled), a pending conditional stays `Pending`, an expired suggestion is **never resurrected as takeable** (by construction — no suggestion→order path), the **kill-switch lock survives** the restart (regression on gh#189), **R-20 default-deny holds** through it (another operator's staged order → `404`), an orphaned stop is **never silently promoted** on a dead connection (Hidden-only), and an **inconsistent surface fails safe** — the kill switch engages `HaltOnly`, loud, not quietly repaired. Always-on hosts suppressed via `RehydrationTestPostgresFactory` (gh#223) | **Active** (Passing) |
| **`SettlementReconcileIntegrationTests.cs`** | **EOD settlement reconcile** (R-13, R-19, R-12, R-20, ADR-0013) via `PositionReconciliationService.ReconcileAsync` driven at a chosen `now` over a user-scoped `DbContext`: the reconcile **yields to venue truth** (returns the adversarial stub's position verbatim — there is no local store), tags a position carried through the maintenance window on the **`Settlement`** basis and **never `Live`** (a re-mark is not a P&L move), returns to **`Live`** once the window passes, **derives each window from the instrument's session** (GC/CL settle before ES/NQ — a hard-coded window can't produce Settlement at 12:30 *and* 14:45 with a Live gap between) using the **DST-aware** `MarketClock` (one UTC instant → Live in July, Settlement in January), **declares `Unknown`** with no positions when the venue is unreachable, keeps auto-flatten's **verify-flat** intact when the window overlaps the deadline, and preserves **R-20** (another operator cannot reconcile the owner's account). Adds a `MakeVenueUnreachable` stub hook; hosts suppressed via `SettlementTestPostgresFactory` (gh#194) | **Active** (Passing) |
| **`WorkingOrderReadIntegrationTests.cs`** *(to be written — QA)* | **Venue-truth resting-orders read** (R-1, R-13, R-17, ADR-0013, ADR-0015) via `GET /accounts/{id}/orders`: returns the working orders standing at the venue **including the attached protective bracket and its size**; an **unreachable venue is `Unknown` with no orders** — never an empty book, the distinction the read exists for; a connection this process holds **no credentials for** is `Unknown` and the venue is **not asked**; **R-20** — another operator gets `404`, not an empty list. Read-only: no writes, gate untouched (gh#381) | **Proposed** (endpoint shipped; suite not yet written) |
| **`OcoCancelOnExitIntegrationTests.cs`** | **OCO-cancel-on-exit** (R-11, ADR-0007) via `OcoExitService.ProcessExitAsync` fed the account-event seam's flat `PositionEvent` (gh#219): a flat exit **retires** the hidden / native / orphaned stop plan (→ `Retired`) and **audits** it (`AuditAction.PositionExit`), **cancels the dangling native legs** at the venue while **sparing the operator's journaled entry**; the dangerous direction is guarded hardest — a **partial fill never retires protection**, a stale flat event **never retires while the venue still shows the position open** (re-open race), and **another operator's plan is untouched** (R-20 per-owner context); replays are a **no-op** and a venue cancel rejected as already-gone is **swallowed without a retry-storm**. Extends the stub with native working-legs + recorded/throwing cancels; hosts suppressed via `OcoExitTestPostgresFactory` (gh#184) | **Active** (Passing) |
| **`AccountEventIngestionIntegrationTests.cs`** | **Account-event ingestion** (R-17, R-11, ADR-0001) via `AccountEventIngestionService.ProcessAsync`: a fill writes a `Fill` row and advances its order to `PartiallyFilled` / (cumulatively) `Filled`; a **voided** fill is ignored; a **replayed** fill is deduped by the `{OrderId, VenueFillKey}` unique index (never double-counted); an order-state event moves a working order to `Cancelled` / `Rejected` (retiring a never-filled entry's stop plan + `AuditRecord`); the journaled `Fill` **carries its owner** (R-20 per-owner write, background plumbing); and a stray event for an unknown order / foreign account is ignored, never a phantom fill. Driven directly at each `AccountEvent`; hosts suppressed via `OcoExitTestPostgresFactory` (gh#281) | **Active** (Passing) |
| **`CancelWorkingOrderIntegrationTests.cs`** | **Cancel-working-order** (R-11, ADR-0007) via `DELETE /orders/{id}`: a working order is **cancelled at the venue** (recorded on the stub), marked `Cancelled`, its stop plan **retired**, and the cancellation **audited** (`AuditAction.OrderCancelled`); a **terminal** order is refused `409` (not a silent no-op); **R-20** holds (another operator → `404`, order untouched); and a **venue rejection** returns `409` **without forcing a terminal status** (venue truth reconciles it) — the plan stays as-is. **gh#297** folds in the **retire**, the **audit**, and a **record-integrity-on-benign-reject** case (coherent status↔staging + **no retry-storm**, exactly one venue cancel) as their own named guards. HTTP-driven with directly-seeded working orders; hosts suppressed via `OcoExitTestPostgresFactory` (gh#282, gh#297) | **Active** (Passing) |
| **`StopPromotionConcurrencyIntegrationTests.cs`** | **Position-aware stop promotion + self-cancel on a lost race** (R-11, R-12, ADR-0007) via `StopPromotionService.PromoteForQuoteAsync` driven directly against **real Postgres**: a due hidden stop is **not** promoted when the venue reports the position **flat or reversed** (fail-closed, plan stays Hidden, nothing transmitted); when a **concurrent retire commits mid-place** (on a second DI scope/connection — a genuine lost-update the InMemory provider cannot reproduce) the promotion **self-cancels the leg it just placed** and never clobbers `Retired` back to `Native`; if that self-cancel is itself rejected, a high-severity **`synthetic_risk`** log fires and the pass does **not** throw; and in a multi-stop pass one plan's lost race is **isolated** (the other still promotes — per-record reconcile). Adds a `PlaceOrderAsync` side-effect hook + placed-id recording + global cancel-reject to the stub; hosts suppressed via `OcoExitTestPostgresFactory` (gh#274) | **Active** (Passing) |
| **`ModifyWorkingOrderIntegrationTests.cs`** | **Modify-working-order** (R-11, R-12, R-16, R-20, ADR-0007) via `PATCH /orders/{id}/price` over HTTP — a reprice is a **re-decision, not a rubber stamp**: the repriced entry persists and the venue leg is modified **in place** (size held invariant, transmitted `null`) **only after** the risk gate re-passes at the unchanged size, witnessed by the adversarial stub's recorded `ModifyOrderAsync`; a modify that **violates the re-read risk profile is refused** (`422 RefusedByRisk`, order untouched, venue never touched — R-12); an entry that **crosses its stops is refused** on the effective geometry **before** the venue, so `CK_StopPlans` can never trip post-reprice (protection stays consistent); a reprice is **audited** (`AuditAction.OrderModified`, before→after entry); a **non-working** order is refused (`409`, no venue contact) and a **missing reference price** is rejected (`400`, R-16); and **R-20 holds** (another operator gets `404`, nothing modified). Adds a `ModifyOrderAsync` override + recorded/throwing modify hooks to the stub; hosts suppressed via `OcoExitTestPostgresFactory` (gh#283). **gh#387** extends the same file across the rest of the modify family: the **working-stop re-stage** (gh#267) is proven a **local** write — a hidden stop re-stages `Order.WorkingStopPrice` + the plan's `ActualStopPrice` with the **venue never contacted**, a crossing move is refused `422` before commit, a coincident-stop order **gains** a Hidden plan, an **Orphaned** plan is refused `409`, and a **widen under `SizingBasis.ActualStop`** re-gates the per-trade layer; the **combined entry + working-stop move** (gh#278) commits both atomically with the full chain re-validated **before** the venue and a venue rejection leaving **both** the order and the plan untouched; and the **resize** (gh#292) **transmits the new size** to the gateway, honours the **gate-approved** quantity on a downsize (`Resized`, echoed not silent), refuses `422` on a non-positive size, `400` on a same-size no-op, `409` on a non-`Working` order, and is refused (`RefusedByKillSwitch`) while the kill switch is engaged — seven core guards (kill-switch, size-transmission, working-only, both ordering guards, the ActualStop widen re-gate, and the combined-move gate re-validation of the new working stop) **prove-red'd** by disabling each in production and observing exactly those tests fail. An adversarial multi-agent review hardened it — two guards that passed regardless of their named defect were re-calibrated (a crossing geometry the risk gate caught independently → a geometry only the ordering backstop catches; a widen refusal driven by the constant safety-stop layer → the working-stop `PerTradeRisk` layer as the sole discriminator) and coverage added (genuine downsize, gate-block-on-resize, combined-move gate refusal, resize/re-stage audit, gate-decision persistence, Native/Retired plan refusals) — 31 tests total | **Active** (Passing) |
| **`TakeProfitBracketIntegrationTests.cs`** | **Take-profit bracket at entry** (gh#170, R-11, ADR-0007) — the optional third leg of the native exchange OCO, over `POST /accounts/{id}/orders`: a **wrong-side** target (below entry for a long, above for a short) is refused **before the gate** (`409 RefusedByInvalidTarget`, no order journaled and **no `GateDecisionRecord`** — distinguishing it from a risk block), never flipped; a **winning-side** target is accepted and **transmitted** as the captured `OrderRequest.ProfitTarget` (read from the adversarial stub, never a fabricated echo), riding the same placed order as the always-native **protective stop** on the opposite side of entry (the two-leg OCO); and the leg is **optional** (no target still places). The wrong-side refusal + the profit-leg transmission are **prove-red'd** (disable `WrongSideTarget`; null the transmitted target). Hosts suppressed via `OcoExitTestPostgresFactory` (gh#170) | **Active** (Passing) |
| **`ConsistencyTargetIntegrationTests.cs`** | **The prop-firm consistency target end to end** (gh#394 ⇒ gh#380, R-5, ADR-0007) — the rule that caps how much of an evaluation's profit may come from one day. Guards the **last mile** the unit suite cannot reach (a target persisted, validated and returned by the API and still never reaching the gate — gh#380's actual defect), **both postures** on the same breach (`Block` refuses with `RiskLayer.ConsistencyTarget` and approves nothing; `Advisory` trades through), the **migration default** (`SET … = DEFAULT` proves the column's own default is `Advisory`, so a schema change cannot start refusing orders), **Central-time day bucketing** (two trades straddling UTC midnight are one CME session; UTC bucketing reads 40% where Central reads 67%), and the **degenerate windows** (loss-making, empty, open-position-only). Two guards began as **pinned defects (gh#407)** — the gate built a `GateAdvisory` and nothing carried it across the API boundary, so the *default* posture produced no observable output at all — and are now that fix's **regression guards**, asserting the advisory is surfaced (breached when it is, raised-but-not-breached while the operator can still act) **and journalled**. All ten **proven red** by seven independent breaks, one guard per break | **Active** (Passing — 10 guards) |
| **`Observability/TelemetryIntegrationTests.cs`** | **Trace continuity across the event log's async gap** (ADR-0002, engineering §7) with the **live** consumers running against real Postgres: a producer's W3C `traceparent` rides the envelope, **survives the `varchar(64)` column byte-identically**, and a genuine `stop-promotion.consume` / `conditional-order.consume` span **links back to the producer's trace** — so `websocket → append → consume` is one trace, not two halves; an **absent** traceparent starts a fresh trace and still consumes; a **malformed** one is ignored and the consumer **keeps consuming afterwards** (a reporting fault must never stall a safety consumer). Captured via a BCL `ActivityListener` on the app's own source — no assertion is satisfied by the test's own input. Adds `TelemetryTestPostgresFactory` (live hosts). **Exemplars are NOT covered — filed as gh#338** (QA contract rule 3) (gh#329) | **Active** (Passing) |
| **`Observability/ExecutionSliIntegrationTests.cs`** | **The execution SLIs report the truth** (ADR-0002, engineering §7, PRD §7) against real Postgres — not that the instruments exist, but that what they say is true: the gate counter’s **total reconciles with the `GateDecisionRecord` rows** (asserted on the summed increments, so a double-count cannot hide behind a right-looking measurement count); a deadline that passes with **nothing to flatten still emits** (`nothing-to-do` — silence would be indistinguishable from a flatten that never fired); the **watchdog is never merged with the primary**; the orphan gauge **rises on a drop and returns to zero on re-arm** (asserted against the rows first, so a failure names the right culprit); and **cardinality is bounded** — tag keys are a closed set and no value is an id, swept across many accounts and orders. The kill-switch **restart** case sits in its own class (`ExecutionSliRestartIntegrationTests`) with its own fixture, because a second host publishing the same instruments is real cross-talk. Adds `ExecutionSliTestPostgresFactory` (private meter + `SliCapture`) (gh#330) | **Active** (Passing) |
| **`Observability/InstrumentationSafetyIntegrationTests.cs`** | **Instrumentation must never break trading** (ADR-0002, engineering §9) — faults injected at the seams the system depends on, with the whole suite under a **configured-but-unreachable** OTLP endpoint: a dead collector does **not** fail, delay or un-journal a send; and a **throwing `IExecutionMetrics`** is probed at three call sites — the gate decision, the **order ack** (the dangerous one: a live order with no `Order` row and no stop plan), and the **operator’s emergency halt**. Those three began as **pinned defects (gh#343)** and are now that fix's **regression guards** (flipped in gh#382): each asserts the trade *completes* and is journaled, and the halt both returns `200` **and** really engaged. The fault is injected **inside** the shipped `FailureTolerantExecutionMetrics` rather than in place of it — a fourth case asserts exactly that, because between gh#343 landing and gh#382 the harness replaced the registration outright and all three pins passed **vacuously** over a host the deployment did not have. Adds `InstrumentationSafetyPostgresFactory` + `FaultInjectingExecutionMetrics` (gh#331) | **Active** (Passing — 4 guards; red proven by making the decorator rethrow) |
| **`Api/SendAsIsEndpointsIntegrationTests.cs`** | The **send-as-is fast path** (gh#181) + the **default entry action** preference (gh#218) over real Postgres: proves the fast path is a shortcut past the **manual review only**, never the gate — R-5 sizing, R-16 caps, the R-14 mode × environment gate, the always-native protective stop, and the kill switch all bind exactly as on `arm → take`. Adversarial venue stub (reports `Live` for every account). QA for gh#182 | **Active** (Passing) |
| **`MarketData/NewsIngestionIntegrationTests.cs`** | **News-ingestion engine** (gh#358, D2·1, R-2/R-9, ADR-0014) — the deduped multi-source **store of record** over real Postgres with two adversarial `INewsSource` feeds; assertions on the persisted `NewsRecord` rows (canonical URL → fuzzy dedup, both provenances kept), never on engine internals, and the suite never calls production `NewsDedupKey` to derive an expected value. QA for gh#360 | **Active** (Passing) |
| **`Relevance/RelevanceRoutingIntegrationTests.cs`** | **News-relevance routing** (gh#361, D2·2, R-2/R-6, ADR-0014) — the deterministic ticker + text → matched-instruments/topics mapping against the deployment's global relevance config (gh#359), including the config-edit → re-resolution loop (gh#418) and the `CK_NewsTopics_Scope_NotUnknown` / `IX_NewsTopics_Name` DB guards proven **by name** against real Postgres. QA for gh#361 | **Active** (Passing) |
| **`Signals/NewsSalienceIntegrationTests.cs`** | **Importance-starring salience — Part A** (gh#362, gh#27, ADR-0014) over `/api/news` + `/api/news/feedback`: a star raises the salience of **similar future** news, a mute is its down-weight inverse, un-starring reverts, and a **salience floor** keeps a muted item visible (no filter bubble); the `CK_SoftSignalFeedback_Kind_NotUnknown` / `IX_SoftSignalFeedbacks_UserId_NewsDedupKey` DB guards proven by name. QA for gh#362 | **Active** (Passing) |
| **`Signals/SalienceIsNotARiskControlIntegrationTests.cs`** | **Salience is never a risk control — Part B** (gh#362, gh#27, ADR-0014) — the **behavioural** half of a two-tier guard (the unit test asserts it structurally). Drives the real order-submit → gate path with the adversarial venue stub and asserts that a maximally-starred or maximally-muted operator profile leaves the gate's decision **byte-identical** to the un-rated baseline — closing the leak a reflection check cannot see (a gate call-site scaling `RequestedQuantity` by a salience multiplier). QA for gh#362 | **Active** (Passing) |
| **`Observability/PrometheusMetricNamesTests.cs`** | The **pure instrument → Prometheus name matching** the staging telemetry suite (gh#332) depends on — runs in the **pre-merge** tier because the staging assertions cannot, and a matching rule that silently never matched would make the staging suite pass **vacuously** or fail on the first staging run for an unrelated reason. QA for gh#332 | **Active** (Passing) |
| **`Alerting/AlertingIntegrationTests.cs`** | The **alerting increment** (gh#246, gh#243, ADR-0019, R-13) through `AutoFlattenService.RunPassAsync` at a controlled instant, against real Postgres with the notification channel behind a recording double so **no test can page the operator**. Two claims that pull in opposite directions, both load-bearing: **it fires when it must** (a failed flatten has to reach a phone) and **it stays silent when it must** (a clean session pages nothing — a pager that cries on healthy days gets muted, and a muted pager manufactures confidence). Migrated onto the transport-only host in gh#480, so it now runs production's real `outbox → queue → dedup → Pushover` chain. The dead-man's-switch cases needing an **external** monitor are staging-tier, not here | **Active** (Passing) |
| **`Api/TriggerConfirmationGateIntegrationTests.cs`** | The **confirm-before-live gate** (gh#486 ⇒ gh#470, R-4 / R-7, ADR-0008) — what the unit tier structurally *cannot* reach: the gate lives in the scan's **queries** (owner discovery + the per-owner load), and a query predicate is only real against a real database; likewise the `CHECK`, the index, and the migration. **Every negative is paired with a positive** — "nothing fired" is trivially satisfied by accident (a misspelled symbol, an unseeded indicator, a stripped notifier), so each inertness claim has a twin changing **only the confirmation** and observing a real fire: a `TriggerFirings` row, an outbox row, and a page on the wire. Driven through the real `ScanAsync(now, …)` with hosted services stripped | **Active** (Passing) |
| **`Data/TriggerConfirmationBackfillIntegrationTests.cs`** | The **migration half** of the same gate (gh#486 ⇒ gh#470) — and it **owns its own container**, because every other suite reaches a database already migrated to head, which is exactly the state in which a backfill is invisible. To witness one you must stand *before* it: migrate to the **preceding** revision, write a row as the old world would have, then let `AddTriggerConfirmation` run. Asserts the migration does two **opposite** things at once — existing rows restored to `Confirmed` by an explicit `UPDATE` (a schema upgrade must never silently disarm alerts an operator relies on — the gh#458 failure class), while new rows still fall to the load-bearing `defaultValue: 0` (`Unconfirmed`) | **Active** (Passing) |

---

## 3. High-Value Test Opportunities & Risk Analysis

```mermaid
flowchart TD
    subgraph Staged Send Path Integration
        A[POST /orders/arm] -->|Stage Proposal| B[(Database Order Row)]
        B -->|WorkingStopPrice Persisted| C[PUT /orders/{id}]
        C -->|Re-gate Edit| D[POST /orders/{id}/take]
        D -->|Fresh Venue & Risk Re-check| E[Transmitted / Working]
        D -->|Risk Violation| F[Staged / HTTP 422 Refused]
    end
    subgraph Multi-Tenant Isolation
        G[Operator A Context] -->|Query Operator B Order| H[HTTP 404 Not Found]
        I[Operator A Context] -->|Take Operator B Order| J[HTTP 404 Not Found]
    end
```

### High-Risk Execution Paths — the original audit's three targets (**all since delivered**)
*Kept as the record of what this audit was opened to close. Each is now covered; see §2 for the suite.*

1. ~~**Working Stop Persistence & Re-validation (`gh#134`)**~~ — **delivered**: `StagedOrderLadderIntegrationTests`
   carries the gh#134 regression; limit/market orders armed with tight working stops do not size against the
   worst-case safety stop at `take`, and `WorkingStopPrice` persistence is proven against real Postgres.
2. ~~**Database Trigger Guard Verification**~~ — **delivered**: the `enforce_mode_matches_account` constraint
   trigger (`Order.mode` must equal `Account.mode`) is exercised in the order suites (gh#130, gh#157) — see §1.
3. ~~**Audit Trail Integrity (`GateDecisionRecord`)**~~ — **delivered**: that every sized attempt, placed or
   blocked, persists a `GateDecisionRecord` scoped to `UserId` / `AccountId` / optional `OrderId` is asserted
   across the order, kill-switch and consistency-target suites.

---

## 4. Registered GitHub issues — status link table

Specs live in their GitHub issues (the tracker is the source of truth, gh#144); this section is a **link table
with status**, not a copy of the bodies. Rows are appended as suites land; the newest entries are at the bottom. **"(this PR)"** in a Status cell means *the PR that added that row* — it is the row's own delivery, not a reference to any current branch.

| Issue | Suite / concern | Target file | Status |
| :--- | :--- | :--- | :--- |
| [#130](https://github.com/adammarquette/trading-copilot/issues/130) | Direct send path & order execution | `OrderEndpointsIntegrationTests.cs` | ✅ Delivered · closed |
| [#157](https://github.com/adammarquette/trading-copilot/issues/157) | Staged ladder (arm → edit → take → cancel) | `StagedOrderLadderIntegrationTests.cs` | ✅ Delivered (gh#197) · closed |
| [#158](https://github.com/adammarquette/trading-copilot/issues/158) | StopPlan persistence & safety-beyond-actual DB guard | `StopPlanPersistenceIntegrationTests.cs` | ✅ Delivered (gh#196) · closed |
| [#161](https://github.com/adammarquette/trading-copilot/issues/161) | Event backbone storage & replay-dedupe | `Data/EventBackboneIntegrationTests.cs` | ✅ Delivered (gh#204) · closed |
| [#228](https://github.com/adammarquette/trading-copilot/issues/228) | Retention-gap signal (flips the gh#162 pin; consumers log high-severity + continue) | `Data/EventBackboneIntegrationTests.cs` (extended) + `Data/RetentionGapConsumerIntegrationTests.cs` (+ `TestHost/RetentionConsumerTestPostgresFactory.cs`) | ✅ Delivered (this PR) |
| [#131](https://github.com/adammarquette/trading-copilot/issues/131) | Production-safe read-only smoke suite | `SystemSmokeIntegrationTests.cs` | ✅ Delivered · closed |
| [#152](https://github.com/adammarquette/trading-copilot/issues/152) | Smoke suite starts no container on a deployed target | `SystemSmokeIntegrationTests.cs` (+ `TestHost/LazySmokeHostFixture.cs`) | ✅ Delivered (gh#205) · closed |
| [#180](https://github.com/adammarquette/trading-copilot/issues/180) | Synthetic conditional-entry firing engine (pairs the #198 watcher) | `ConditionalFiringIntegrationTests.cs` | ✅ Delivered (gh#216) |
| [#132](https://github.com/adammarquette/trading-copilot/issues/132) | Multi-tenant workspace & resource isolation | `MultiTenantIsolationIntegrationTests.cs` | ✅ Delivered (gh#213) — realigned to the route map (gh#160) |
| [#142](https://github.com/adammarquette/trading-copilot/issues/142) | Connection & account-stage suite | `ConnectionLifecycleIntegrationTests.cs` | ✅ Delivered (gh#217) — realigned (gh#160); missing endpoints → [#210](https://github.com/adammarquette/trading-copilot/issues/210) |
| [#229](https://github.com/adammarquette/trading-copilot/issues/229) | Credential rotation + connection soft-delete (completes the gh#142 suite) | `ConnectionLifecycleIntegrationTests.cs` (+10 cases) | ✅ Delivered (this PR) — QA for gh#210; found [#322](https://github.com/adammarquette/trading-copilot/issues/322) |
| [#335](https://github.com/adammarquette/trading-copilot/issues/335) | Staging harness bootstrap (config, gating, serialization) | `TestHost/Staging/*` + gated draft suites | ✅ Delivered (this PR) — serves [#234](https://github.com/adammarquette/trading-copilot/issues/234) / [#269](https://github.com/adammarquette/trading-copilot/issues/269) / [#293](https://github.com/adammarquette/trading-copilot/issues/293), which are authored + run on staging |
| [#514](https://github.com/adammarquette/trading-copilot/issues/514) | Event-log gap → backfill recovery on real Postgres (broken out of gh#306) | `MarketData/GapBackfillIntegrationTests.cs` (6 cases) + `TestHost/GapBackfillTestPostgresFactory.cs` | ✅ Delivered (this PR) — **the feared defect did not materialise**: the projection-then-`Distinct()` over a private nested record, the `COALESCE` arm and the `Bars` hypertable range read **all translate correctly** under Npgsql, so R-1's recovery guarantee is intact and is now guarded. Prove-red: dropping the `Distinct()` reddens only the projection case; swapping the synthetic quote's Low/High reddens the promote case while the **negative case stays green** (which is why the pair exists); measuring coverage from all bars instead of the finest reddens only the resolution case. **Three of the issue's eight cases were not delivered** — the pending-conditional detection and the two live-host cases need a second fixture with hosted services *running*, the opposite of this one; see the PR | **Active** (Passing) |
| [#303](https://github.com/adammarquette/trading-copilot/issues/303) | Clean-historical bar store & REST backfill (durability, idempotence, partial-bar, capability, global scope) | `MarketData/BarStoreIntegrationTests.cs` (10 cases) | ✅ Delivered (this PR) — QA for gh#302 |
| [#143](https://github.com/adammarquette/trading-copilot/issues/143) | Risk profile trailing drawdown & floor tracking | `RiskProfileLifecycleIntegrationTests.cs` | ✅ Delivered (gh#215) — realigned per gh#160 (`PUT` verb, reuses the gh#157 harness) |
| [#186](https://github.com/adammarquette/trading-copilot/issues/186) | Auto-flatten scheduler host | `AutoFlattenSchedulerIntegrationTests.cs` (+ shared `TestHost` position stub & `FlattenTestPostgresFactory`) | ✅ Delivered (gh#235) |
| [#188](https://github.com/adammarquette/trading-copilot/issues/188) | Redundant watchdog + rejected-order | `AutoFlattenWatchdogIntegrationTests.cs` | ✅ Delivered (gh#238) |
| [#190](https://github.com/adammarquette/trading-copilot/issues/190) | Kill switch — outbound halt, cancel working, flatten/halt, startup rehydration | `KillSwitchIntegrationTests.cs` (reuses the gh#186 position stub) | ✅ Delivered (gh#239) |
| [#192](https://github.com/adammarquette/trading-copilot/issues/192) | Connection-loss orphan handling (drop → orphan, reconnect → re-arm, restart reconcile) | `OrphanHandlingIntegrationTests.cs` (+ `TestHost/OrphanTestPostgresFactory.cs`) | ✅ Delivered (gh#241) |
| [#223](https://github.com/adammarquette/trading-copilot/issues/223) | Decision-state rehydration & R-20-through-restart | `StateRehydrationIntegrationTests.cs` (+ `TestHost/RehydrationTestPostgresFactory.cs`) | ✅ Delivered (gh#251) |
| [#194](https://github.com/adammarquette/trading-copilot/issues/194) | EOD settlement reconcile (venue-as-truth, settlement re-mark, window awareness, declare-unknown) | `SettlementReconcileIntegrationTests.cs` (+ `TestHost/SettlementTestPostgresFactory.cs`) | ✅ Delivered (gh#257) |
| [#184](https://github.com/adammarquette/trading-copilot/issues/184) | OCO-cancel-on-exit (retire protection on flat exit; never on a partial) | `OcoCancelOnExitIntegrationTests.cs` (+ `TestHost/OcoExitTestPostgresFactory.cs`) | ✅ Delivered (gh#258) |
| [#281](https://github.com/adammarquette/trading-copilot/issues/281) | Account-event ingestion (fill → journal, PartiallyFilled/Filled, terminal states) | `AccountEventIngestionIntegrationTests.cs` | ✅ Delivered (gh#285) — QA for gh#219 |
| [#282](https://github.com/adammarquette/trading-copilot/issues/282) | Cancel-working-order (venue cancel, retire plan, audit; 409 non-working; 404 R-20) | `CancelWorkingOrderIntegrationTests.cs` | ✅ Delivered (gh#288) — QA for gh#250 |
| [#297](https://github.com/adammarquette/trading-copilot/issues/297) | Granular retire / audit / no-retry-storm record-integrity guards folded into the cancel suite (from the closed dup gh#291) | `CancelWorkingOrderIntegrationTests.cs` | ✅ Delivered (this PR) — augments gh#282 |
| [#274](https://github.com/adammarquette/trading-copilot/issues/274) | Position-aware stop promotion + self-cancel on a concurrent-retire lost race (real-Postgres) | `StopPromotionConcurrencyIntegrationTests.cs` | ✅ Delivered (this PR) — QA for gh#263 |
| [#283](https://github.com/adammarquette/trading-copilot/issues/283) | Modify-working-order (entry reprice, re-gate, audit, crossing/not-working/R-20 refusals) | `ModifyWorkingOrderIntegrationTests.cs` | ✅ Delivered (this PR) — QA for gh#259 |
| [#387](https://github.com/adammarquette/trading-copilot/issues/387) | Modify family beyond entry reprice — working-stop re-stage (gh#267), combined move (gh#278), resize re-gate (gh#292) | `ModifyWorkingOrderIntegrationTests.cs` (+24 cases, 31 total) | ✅ Delivered (this PR) — QA for gh#267/#278/#292; 7 guards prove-red'd; adversarial-review-hardened (2 vacuous guards re-calibrated) |
| [#396](https://github.com/adammarquette/trading-copilot/issues/396) | Take-profit bracket at entry — wrong-side refused before the gate, winning-side transmitted as the OCO profit leg | `Api/TakeProfitBracketIntegrationTests.cs` (5 cases) | ✅ Delivered (this PR) — QA for gh#170; wrong-side + transmit guards prove-red'd |
| [#408](https://github.com/adammarquette/trading-copilot/issues/408) | Dead-man's-switch check-in — withhold when exposed, report when flat, cross-account aggregation, per-credential-key, retry on reject, no work when not due | `Api/DeadMansSwitchCheckInIntegrationTests.cs` (6 cases) + `TestHost/{DeadMansSwitchTestPostgresFactory,RecordingDeadMansSwitch}.cs` | ✅ Delivered (this PR) — QA for gh#244; **all 6 guards prove-red'd**; first coverage of the tier-3 flatten redundancy |
| [#429](https://github.com/adammarquette/trading-copilot/issues/429) | Agent-review route — gate below the model, one review per arming edge, size/mode are the operator's, DB guards | `Api/AgentReviewRouteIntegrationTests.cs` (14 cases) + `TestHost/{AgentReviewTestPostgresFactory,AdversarialLlmProvider}.cs` | ✅ Delivered (this PR) — QA for gh#402; prove-red'd, and **demonstrated the gap**: with the route writing to the execution path the structural unit test stays green while these guards go red |
| [#442](https://github.com/adammarquette/trading-copilot/issues/442) | The notification harness mirrored a chain production no longer ships — transport-only host, fidelity guard, durability coverage | `Observability/NotificationDurabilityIntegrationTests.cs` (6 cases + 2 fidelity) + `TestHost/{NotificationHarnessPostgresFactory,RecordingPushoverTransport}.cs` | ✅ Delivered (this PR) — **found P0 gh#459**; guard prove-red'd; alerting-suite migration sequenced behind gh#459 |
| [#480](https://github.com/adammarquette/trading-copilot/issues/480) | Migrate the alerting harness onto the transport-only host — completes gh#442, unblocked by gh#468 | `TestHost/AlertingTestPostgresFactory.cs` + `Alerting/AlertingIntegrationTests.cs` | ✅ Delivered (this PR) — the last host off the rebuilt chain, so **all three** are now covered by the fidelity guard (prove-red'd by pasting the old block back). The alerting suite now runs on production's `outbox → queue → dedup → Pushover` with `IsConfigured == true`; the dedup-key assertions moved to where the key now lives (the outbox row) and to POST identity on the wire; and the "one page per incident" case now records **which** layer collapses the repeat — verified to be the dedup decorator, not the outbox, since a delivered row releases its key under the gh#458 filtered index |
| [#512](https://github.com/adammarquette/trading-copilot/issues/512) | Both flatten tiers act on one credential set, and their account sets agree (broken out of gh#187, ADR-0015) | `Api/FlattenTierAccountSetIntegrationTests.cs` (3 cases) | ✅ Delivered (this PR) — **found gh#522**: both tiers' existing unit guards for this skip are **vacuous**, because the shared `Venue(...)` helper leaves `GetAccountsAsync` unstubbed. Prove-red: deleting the primary's credential filter turns the new guard red (`PositionReadCount` 0 → 4) while **both unit guards stay green**; deleting the watchdog's does the same on that tier; giving the watchdog a divergent account set reddens the agreement case. Case 4 from the issue (the roster-drop silence pin) was **not delivered** — see the PR for why it needs its own decision first |
| [#511](https://github.com/adammarquette/trading-copilot/issues/511) | The **authorization surface** — every mapped route refuses anonymous access, enumerated from `EndpointDataSource` (broken out of gh#8, R-18) | `Api/AuthorizationSurfaceIntegrationTests.cs` (9 cases) | ✅ Delivered (this PR) — replaces an 11-route hand list covering **no execution route, no kill-switch route and neither venue-truth read**, out of 45 authorized routes. Prove-red: stripping `.RequireAuthorization()` from the orders group reddens both sweeps **and** names all four newly-ungated routes structurally; `ValidateLifetime`, `ValidateIssuer` and `ValidateAudience` each redden exactly one case; emptying the enumeration reddens **only** the anti-vacuity guard while both sweeps pass. Two corrections fell out of the prove-red: `/health` and `/ready` are un-gated by **omission** rather than by `[AllowAnonymous]` (pinned as their own set, since that is the same mechanism as a forgotten gate), and `ValidateIssuerSigningKey=false` does **not** disable signature checking — so the wrong-key guard is stated as guarding *signature verification* and is prove-red'd by adding a second trusted key |
| [#481](https://github.com/adammarquette/trading-copilot/issues/481) | The dedup key's **lifetime** — a delivered incident must be able to page again (gh#458 follow-up) | `Observability/NotificationDurabilityIntegrationTests.cs` (4 new cases → 10 total) | ✅ Delivered (this PR) — **found P1 gh#497**. gh#458 released the key at the **outbox**; the `DedupingNotificationChannel` one layer downstream still holds it, and is re-armed *only* by `ResolveAsync`, which the flatten calls only on `FlattenVerdict.Flat`. An escalation is by definition the path where the close failed, so whoever ended that exposure — the operator by hand, a prop firm's own backstop — never re-armed it: **every repeat escalation for that pair was silent until the process restarted.** Pinned per QA rule 2, with `Outbox_ShouldPageAgain_WhenTheIncidentResolvedBetweenOccurrences` as the isolating counterpart (identical steps plus a resolve → both pages land). **gh#497 has since landed** (`f8aa3a1`) — fixed at the **producer**, not by releasing the dedup key: a flatten pass now re-arms whenever it observes the instrument flat **by any means**. The pinned assertion therefore stayed `ContainSingle()` (two occurrences with no resolve between them are one *continuing* incident — the ADR-0019 §4 noise budget), and the pin was re-worded rather than flipped |
| [#486](https://github.com/adammarquette/trading-copilot/issues/486) | Confirm-before-live gate — unconfirmed triggers stay inert end-to-end, and the migration backfill | `Api/TriggerConfirmationGateIntegrationTests.cs` + `Data/TriggerConfirmationBackfillIntegrationTests.cs` (+ `TestHost/ConfirmationGateTestPostgresFactory.cs`) | ✅ Delivered — QA for gh#470. Reaches what the unit tier cannot: the gate lives in the scan's **query predicates**, plus the `CHECK`, the index and the migration — none of which exist outside Postgres. Every inertness assertion is **paired with a positive** that changes only the confirmation and observes a real fire. The backfill suite migrates to the **preceding** revision first, the only way a backfill is observable |
| [#332](https://github.com/adammarquette/trading-copilot/issues/332) | Telemetry queryable against the real LGTM stack (**post-merge staging**) + Grafana panel proposals; its **pre-merge** pure name-matching companion | `Observability/TelemetryStagingIntegrationTests.cs` (staging) + `Observability/PrometheusMetricNamesTests.cs` (pre-merge) | 🟡 Staging suite written, **not yet run green** (first run is on staging); the pre-merge name-matching guard is ✅ Delivered; panels proposed as gh#366 |
| [#329](https://github.com/adammarquette/trading-copilot/issues/329) | Trace continuity & correlation across the event-log boundary (pre-merge) | `Observability/TelemetryIntegrationTests.cs` | ✅ Delivered (this PR) — QA for gh#230; exemplar gap filed as gh#338 |
| [#330](https://github.com/adammarquette/trading-copilot/issues/330) | Execution-SLI emission, absence signal & bounded cardinality (pre-merge) | `Observability/ExecutionSliIntegrationTests.cs` | ✅ Delivered (this PR) — QA for gh#232/gh#295 |
| [#331](https://github.com/adammarquette/trading-copilot/issues/331) | Instrumentation must never break trading (exporter unreachable; throwing metrics sink) | `Observability/InstrumentationSafetyIntegrationTests.cs` | ✅ Delivered — QA for gh#26/§9; **found gh#343** |
| [#382](https://github.com/adammarquette/trading-copilot/issues/382) | Flip gh#331's three pins onto the shipped decorator — the harness bypassed it, so they passed **vacuously** | `Observability/InstrumentationSafetyIntegrationTests.cs` (+ `TestHost/InstrumentationSafetyPostgresFactory.cs`) | ✅ Delivered (this PR) — regression guards for gh#343, plus a fidelity guard so a harness that swaps the decorator out fails loudly |
| [#269](https://github.com/adammarquette/trading-copilot/issues/269) | Bracket **preservation** across an in-place modify — pre-live gate for gh#259 | `Api/BracketPreservationStagingIntegrationTests.cs` (+ `TestHost/Staging/StagingProjectXGateway.cs`) | 🟡 **Authored** (this PR) — QA for gh#259; real place→reprice→fill→gateway-truth flow, **prove-red'd on the first staging run** (only ProjectX can witness it) |
| [#293](https://github.com/adammarquette/trading-copilot/issues/293) | Bracket **sizing** to the realized fill across a resize (UP/DOWN) — hard pre-live gate for gh#292 | `Api/BracketSizingStagingIntegrationTests.cs` (+ `TestHost/Staging/StagingProjectXGateway.cs`) | 🟡 **Authored** (this PR) — QA for gh#292; reads gateway venue truth (position + protective-stop size), **prove-red'd on the first staging run** |
| [#394](https://github.com/adammarquette/trading-copilot/issues/394) | Consistency target — enforcement postures, the last mile, the day boundary, degenerate windows | `Api/ConsistencyTargetIntegrationTests.cs` | ✅ Delivered (this PR) — QA for gh#380; **found gh#407** (the advisory is computed and never surfaced) |
| [#182](https://github.com/adammarquette/trading-copilot/issues/182) | Send-as-is fast path & default-entry-action — same enforcement floor as `arm → take` | `Api/SendAsIsEndpointsIntegrationTests.cs` | ✅ Delivered — QA for gh#181/#218 |
| [#360](https://github.com/adammarquette/trading-copilot/issues/360) | News-ingestion engine — deduped multi-source store of record (D2·1) | `MarketData/NewsIngestionIntegrationTests.cs` | ✅ Delivered — QA for gh#358 |
| [#361](https://github.com/adammarquette/trading-copilot/issues/361) | News-relevance routing — ticker/text → instruments/topics against global config (D2·2) | `Relevance/RelevanceRoutingIntegrationTests.cs` | ✅ Delivered — QA for gh#359 |
| [#362](https://github.com/adammarquette/trading-copilot/issues/362) | Importance-starring salience (Part A) + salience-is-not-a-risk-control (Part B) | `Signals/NewsSalienceIntegrationTests.cs` + `Signals/SalienceIsNotARiskControlIntegrationTests.cs` | ✅ Delivered — QA for gh#27 |
| [#210](https://github.com/adammarquette/trading-copilot/issues/210) | *(work:code)* connection credential rotation + soft-delete | Coding Agent — `ConnectionEndpoints.cs` | 📝 Open · filed by gh#160 |

---

## 5. Route-map realignment log (gh#160)

The open QA specs were drafted from the audit's *intended* surface, not the *built* one; gh#160 corrected each
against the live route map on `develop` so every remaining test names an endpoint that exists:

- **#142** — `PUT /connections/{id}/credentials` and `DELETE /connections/{id}` do **not** exist; they are missing
  *features*, moved to [#210](https://github.com/adammarquette/trading-copilot/issues/210). The suite now covers
  connection creation, account discovery, and stage resolution/override — all routes that exist. Re-estimated 3 → 2.
- **#132** — `GET /connections/{id}` (by-id) and a gate-decision audit endpoint do **not** exist. Test 1 becomes
  collection-scoping (`GET /connections` must exclude the other operator's connection); Test 4 is parked at the
  `TradingCopilotDbContext` level until a gate-decision read surface exists. Stays **P0**.
- **#143** — declaration is `PUT /accounts/{id}/risk`, not `POST`; its `take` test now **reuses the gh#157
  staged-ladder harness** rather than standing up a second arm/take fixture.

Earlier drift this refresh also cleared: the never-created `OrderExecutionEndpointsIntegrationTests.cs` /
`ProductionSmokeTests.cs` target names (the shipped files are `OrderEndpointsIntegrationTests.cs` /
`SystemSmokeIntegrationTests.cs`); `#134` mislisted as a parent *task* (it is a **PR**); and a stale
"Ready for Development" header while several suites had already shipped and closed.
