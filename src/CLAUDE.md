# CLAUDE.md

The agent contract for this subtree is **`AGENTS.md`** (the **Coding Agent** contract) — follow it plus the root
`AGENTS.md`.

The root [`../AGENTS.md`](../AGENTS.md) also applies. Role contracts — Reviewer, Platform, Coordinator —
never auto-load; open them yourself from the table at the top of the root file.

@AGENTS.md
