<Project Sdk="Microsoft.NET.Sdk.Web">

  <!-- The BFF host (R-18 auth, R-10 API). Minimal API + JWT bearer. Common settings from ../Directory.Build.props. -->

  <ItemGroup>
    <!-- Identity's PasswordHasher comes from the ASP.NET shared framework (Web SDK); only JWT bearer needs a package. -->
    <PackageReference Include="Microsoft.AspNetCore.Authentication.JwtBearer" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\MarqSpec.TradingCopilot.Data\MarqSpec.TradingCopilot.Data.csproj" />
    <!-- The composition root builds venue adapters (R-17); the ProjectX client arrives transitively. -->
    <ProjectReference Include="..\MarqSpec.TradingCopilot.Integration.ProjectX\MarqSpec.TradingCopilot.Integration.ProjectX.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Endpoint handlers are internal; unit tests exercise them directly (integration tests hit the HTTP surface). -->
    <InternalsVisibleTo Include="MarqSpec.TradingCopilot.UnitTests" />
  </ItemGroup>

</Project>
